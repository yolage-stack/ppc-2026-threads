# Вычисление многомерных интегралов методом Симпсона — SEQ

- Студент: Редькина Алина Александровна
- Технология: SEQ
- Вариант: 11

## 1. Контекст

Задача — численно найти значение кратного интеграла по прямоугольнику в R^d составной квадратурой Симпсона.
Последовательная реализация задаёт эталон результата и базовое время (работа p = 1), относительно которых
сравниваются параллельные реализации в общих тестах курса.

## 2. Постановка задачи

**Вход (`InType`):** векторы границ **a**, **b** одинаковой длины **d**; по каждой оси **n_i > 0** и **чётное**
(число отрезков); пользовательская функция **f(x)**.

**Выход (`OutType`):** одно вещественное число — приближение интеграла.

**Ограничения:** **d ≥ 1**; **a_i < b_i**; размерности **a**, **b**, **n** согласованы; **f** определена во всех узлах
сетки; в **ValidationImpl** отклоняются некорректные входы.

**Крайние случаи:** минимальная сетка по оси (чётное **n_i**), разные размерности, разные области интегрирования и
функции с известным аналитическим интегралом.

## 3. Базовый алгоритм

По каждой оси строится **n_i + 1** узлов; всего **∏_i (n_i + 1)** узлов. Обход в лексикографическом порядке
(`AdvanceIndices`: быстрее всего меняется последняя координата). Для узла **(i_1,…,i_d)** вес по оси задаётся правилами
1/4/2 Симпсона; полный вес — произведение одномерных весов; к сумме добавляется **w_prod · f(x)**.

**Время:** **O(∏ (n_i + 1))** вызовов **f** (доминирует вычисление функции).

**Память:** **O(d)** под индексы и точку плюс входные векторы.

**Инвариант:** после полного обхода **sum** совпадает с суммой взвешенных значений **f** по всем узлам; итог умножается
на **(h_1·…·h_d)/3^d**, **h_i = (b_i − a_i)/n_i**.

## 4. Детали реализации

**Файлы:** `seq/include/ops_seq.hpp`, `seq/src/ops_seq.cpp`.

Каркас задачи курса: **`ValidationImpl`** (проверка размеров, **a < b**, чётность **n_i**, наличие **func**);
**`PreProcessingImpl`** — копирование входа, обнуление результата; **`RunImpl`** — двойной цикл по индексам через
`AdvanceIndices` и **`EvaluatePoint`**; **`PostProcessingImpl`** — запись **`result_`** в выход.

Критический фрагмент (идея суммирования):

```cpp
// File: seq/src/ops_seq.cpp (фрагмент)
while (has_next) {
  EvaluatePoint(a_, h, n_, indices, func_, point, sum);
  has_next = AdvanceIndices(indices, n_);
}
result_ = (h_prod / denominator) * sum;
```

## 5. Проверка корректности

Функциональная проверка находится в `tests/functional/main.cpp`. Для SEQ это основной набор, потому что именно
последовательная версия используется как эталон при сравнении остальных реализаций.

В набор входит 20 тестов:

- одномерные случаи: `1`, `x`, `x^2`, `x^3`, `x^4`, `sin(x)`, `exp(x)`;
- двумерные случаи: `1`, `x*y`, `x^2 + y`, `x*y^2`, `exp(x+y)`, `sin(x+y)`, `sin(x)*cos(y)`, `x*sin(y)`;
- трёхмерные случаи: `1`, `x*y*z`, `x^2+y^2+z^2` на `[0,1]^3` и `[-1,1]^3`,
  `sin(x)*cos(y)*exp(z)`.

В тестах явно задано ожидаемое значение интеграла, а результат сравнивается с ним с точностью `1e-6`. Полиномы до третьей
степени дополнительно проверяют, что веса Симпсона выставлены правильно: для них формула должна давать точный результат
уже на минимальной сетке.

## 6. Экспериментальная среда

Функциональные тесты запускались в конфигурации курса (**`settings.json`**: включены seq/omp/tbb/stl/all). Для baseline
времени на интегральном каркасе: CPU AMD Ryzen 7 7840HS, ОС Windows 11, **g++** 13.3.0, сборка **Release** (как в корневом
отчёте). Команды запуска — см. **`report.md`** в корне задачи (раздел «Репродуцируемость»).

## 7. Результаты

| Режим | Размер задачи | Потоки | Время, с |
| ----- | ------------- | ------ | -------- |
| SEQ | 3D Гаусс, `n = {200, 200, 200}` | 1 | 0.108102 |

Узкое место — вычисление **f** в каждом узле; рост числа узлов увеличивает время примерно пропорционально объёму сетки.

## 8. Выводы

**SEQ** — эталон корректности и знаменатель ускорения. Любая параллельная версия обязана совпадать с **SEQ** на общих
тестах; время **SEQ** на выбранном размере задачи используется для **speedup = T_seq / T_par** в сводной таблице.
