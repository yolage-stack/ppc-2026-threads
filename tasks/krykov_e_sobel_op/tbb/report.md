# Выделение рёбер на изображении с использованием оператора Собеля - TBB

- Student: Крюков Е.
- Technology: TBB
- Variant: 27

## 1. Контекст

oneTBB предоставляет высокоуровневые абстракции задачного параллелизма
с автоматическим управлением пулом потоков и балансировщиком нагрузки.
В отличие от OMP, поток управления явно не задаётся: программист описывает
диапазон работы и операцию над ним, а runtime сам решает, как разбить диапазон
на чанки.
Baseline - `seq/report.md`.

## 2. Постановка задачи

Идентична SEQ-версии: `Image{width, height, data}` - `std::vector<int>`.
Полное совпадение с SEQ на всём тестовом наборе.

## 3. Базовый алгоритм

Подробно описан в `seq/report.md`. RGB - grayscale - свёртка ядром Собеля 3×3
по внутренним пикселям - `magnitude = sqrt(Gx² + Gy²)`.

## 4. Схема распараллеливания

Использован примитив `tbb::parallel_for` с диапазоном `tbb::blocked_range<int>`
по строкам изображения:

```cpp
// File: tbb/src/ops_tbb.cpp - RunImpl
tbb::parallel_for(
    tbb::blocked_range<int>(1, h - 1),
    [&](const tbb::blocked_range<int>& range) {
      for (int row = range.begin(); row != range.end(); ++row) {
        for (int col = 1; col < w - 1; ++col) {
          // вычисление gx, gy, magnitude
          output[(row * w) + col] = magnitude;
        }
      }
    });
```

**Диапазон работы:** `blocked_range<int>(1, h-1)` охватывает все внутренние
строки. Каждый чанк - непрерывный поддиапазон строк.

**Grain size:** не задан явно, используется значение по умолчанию
(`grainsize = 1`). TBB runtime выбирает фактический размер чанка динамически
через work-stealing scheduler, адаптируясь к числу доступных потоков.
Для регулярной задачи с равномерной стоимостью строк явный grainsize
не критичен; при желании можно задать `blocked_range<int>(1, h-1, h/nthreads)`
для уменьшения overhead при большом числе потоков.

**Partitioner:** используется `auto_partitioner` (по умолчанию) - он разбивает
диапазон рекурсивно, пока чанки не становятся меньше grainsize.
Это обеспечивает хорошее балансирование без явной настройки.

**Контроль конкуренции:** число рабочих потоков задаётся через
`PPC_NUM_THREADS`. TBB использует `global_control::max_allowed_parallelism`,
устанавливаемое фреймворком курса.

**Синхронизация:** `parallel_for` блокирует вызывающий поток до завершения
всех задач - неявный барьер по завершении. Записи в `output` безопасны:
чанки по строкам не пересекаются.

## 5. Детали реализации

Файлы: `tbb/include/ops_tbb.hpp`, `tbb/src/ops_tbb.cpp`.

Изменения относительно SEQ минимальны: `RunImpl` оборачивает внешний цикл
по строкам в `tbb::parallel_for`. Ядра объявлены как `const` до лямбды
и захватываются по ссылке - они неизменны, поэтому гонок нет.
Локальные переменные `gx`, `gy`, `magnitude` объявлены внутри лямбды
и полностью приватны для каждого потока.

`ValidationImpl`, `PreProcessingImpl`, `PostProcessingImpl` идентичны SEQ.

## 6. Проверка корректности

Все три функциональных теста (ConstantImage, VerticalEdge, HorizontalEdge)
пройдены с результатами, побайтово совпадающими с SEQ.
Работа при числе потоков 1 и 4 даёт одинаковый вывод.

## 7. Экспериментальная среда

| Параметр         | Значение                             |
| ---------------- | ------------------------------------ |
| CPU              | AMD Ryzen 3 3100 (4 физических ядра) |
| ОС               | Windows 10                           |
| Компилятор       | MSVC (Release)                       |
| CMake build type | Release                              |
| `PPC_NUM_THREADS`| 4                                    |

Команда запуска:

```bash
mpiexec -n 4 .\build\bin\ppc_perf_tests.exe --gtest_filter="*KrykovE*"
```

## 8. Результаты

| size | SEQ task_run, с | TBB task_run, с | Speedup | Efficiency |
| ---- | --------------- | --------------- | ------- | ---------- |
| 512  | 0.00738         | 0.00951         | 0.78x   | -          |
| 1024 | 0.03471         | 0.03209         | 1.08x   | 27.1%      |
| 2048 | 0.11435         | 0.11893         | 0.96x   | -          |

| size | SEQ pipeline, с | TBB pipeline, с | Speedup |
| ---- | --------------- | --------------- | ------- |
| 512  | 0.00851         | 0.01235         | 0.69x   |
| 1024 | 0.03374         | 0.04789         | 0.70x   |
| 2048 | 0.12496         | 0.14248         | 0.88x   |

TBB на данной задаче не опередил SEQ и уступил OMP.
Причина - overhead work-stealing планировщика заметен при относительно лёгком
вычислении (memory-bound задача с малой долей арифметики).
При `pipeline`-режиме TBB дополнительно проигрывает из-за инициализации runtime
при каждом запуске теста.

## 9. Выводы

oneTBB удобен для задач с нерегулярным или неизвестным заранее распределением
нагрузки - тогда work-stealing даёт реальный выигрыш.
Для регулярной memory-bound задачи вроде фильтра Собеля накладные расходы
runtime перевешивают выгоду от автоматического балансирования.
