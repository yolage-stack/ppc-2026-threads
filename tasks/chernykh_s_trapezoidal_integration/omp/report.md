# Вычисление многомерных интегралов с использованием многошаговой схемы (метод трапеций). — OMP

- Student: Черных Севастьян Владимирович
- Technology: OMP
- Variant: 10

## 1. Контекст

Задача состоит в вычислении многомерных интегралов с использованием метода трапеций с использованием OpenMP. Необходимо
сравнить время выполнения алгоритма с последовательной версией и исследовать масштабируемость алгоритма.

## 2. Постановка задачи

### Входные данные

- `limits`: Вектор пар [a_i, b_i] - границы интегрирования по каждому измерению.
- `steps`: Вектор целых положительных чисел `n_i`, задающий количество шагов разбиения по каждому измерению.
- `func`: Функция, интеграл которой нужно посчитать, передается в виде лямбда выражения.

### Выходные данные

Значение типа double - результат счета.

### Ограничения и крайние случаи

- Входные вектора `limits` и `steps` должны совпадать по размеру и не должны быть пустыми.
- Входной вектор `steps` должен содержать числа строго больше нуля.

## 3. Базовый алгоритм

Последовательный алгоритм использует один вектор `counters` для многомерного шага внутри цикла `while`. Каждая итерация
зависит от состояния счетчиков предыдущей, что создает явную циклическую зависимость по данным и препятствует прямому
распараллеливанию через `#pragma omp parallel for`.

## 4. Схема распараллеливания

Чтобы обойти ограничение последовательного алгоритма, в OMP-версии применен переход к плоскому линейному пространству
итераций.

### Параллельный участок кода

```cpp
#pragma omp parallel default(none) shared(input, dims, total_points) reduction(+ : total_sum)
{
    std::vector<std::size_t> local_counters(dims);  // Локальный вектор индексов потока
    std::vector<double> local_point(dims);          // Локальный вектор координат точки

#pragma omp for schedule(static)
    for (int64_t j = 0; j < total_points; j++) {
        int64_t temp_j = j;
        for (std::size_t i = 0; i < dims; i++) {
            int64_t point_in_dims = static_cast<int64_t>(input.steps[i]) + 1;
            local_counters[i] = static_cast<std::size_t>(temp_j % point_in_dims);
            temp_j /= point_in_dims;
        }
        double weight = CalculatePointAndWeight(input, local_counters, local_point);
        total_sum += input.func(local_point) * weight;
    }
}
```

### Область параллелизма

Параллельная область открывается `#pragma omp parallel` один раз на весь блок, чтобы выделить память под локальные
векторы `local_counters` и `local_point` один раз для каждого потока.
Внутри региона директива `#pragma omp for` разделяет только сам вычислительный цикл. Это убирает накладные расходы на
постоянное выделение и освобождение памяти на каждой итерации.

### Разделение данных

- **shared** — `input`, `dims`, `total_points` (доступны только на чтение).
- **private (локальные)** — `local_counters`, `local_point`, `j`, `temp_j`, `point_in_dims`, `weight` (создаются внутри
параллельной области и являются индивидуальными для каждого потока).

### Синхронизация

- Использован механизм редукции `reduction(+: total_sum)`. Для устранения гонки данных при обновлении общей суммы.
- Критические секции и атомики отсутствуют.

### Барьеры

- Неявный барьер в конце области `#pragma omp parallel`.

### Распределение работы

- Используется статическое планирование `schedule(static)`. Так как вычислительная сложность каждой итерации неизменна,
при старте OpenMP делит `total_points` на равные блоки и распределяет их по потокам.

## 5. Детали реализации

### Файлы

- `omp/include/ops_omp.hpp`
- `omp/src/ops_omp.cpp`

### Особенности реализации

Главным изменением относительно SEQ стал отказ от последовательного инкремента многомерного счетчика и переход к
математическому восстановлению индексов:

``` cpp
int64_t temp_j = j;
for (std::size_t i = 0; i < dims; i++) {
    int64_t point_in_dims = static_cast<int64_t>(input.steps[i]) + 1;
    local_counters[i] = static_cast<std::size_t>(temp_j % point_in_dims);
    temp_j /= point_in_dims;
}
```

Данный расчет локален для каждой итерации `j`, не зависит от состояния `j-1`, что делает итерации независимыми.

## 6. Проверка корректности

Тестирование параллельной версии OpenMP проводилось на аналогичном последовательной версии (SEQ) наборе функциональных
тестов (`Linear_1D`, `Sum_2D`, `Constant_3D`, `Parabola_1D`, `Sin_1D`, `Zero_Range`), включая проверку на нулевой
отрезок интегрирования, разные размерности пространств и виды самих функций интегрирования. Расхождения в выходных
данных между параллельной и последовательной реализациями отсутствуют: результаты вычислений полностью совпадают в
пределах допустимой погрешности 10^-3. Дополнительно была проверена устойчивость и детерминированность результатов при
запуске алгоритма на 1, 2, 4 и более потоках, что подтверждает корректность логики счёта и полное отсутствие состояний
гонки.

## 7. Экспериментальная среда

- CPU: AMD Ryzen 5 5600X
- RAM: 32 GB
- OS: Ubuntu 24.04.1 LTS на Windows 10
- WSL Kernel: Linux 6.6.87.2-microsoft-standard-WSL2.
- Компилятор: gcc (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0
- Сборка: Release (cmake version 3.28.3)

### Сборка

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
cmake --build build -j$(nproc)      

# Функциональные тесты
OMP_NUM_THREADS=<число потоков> PPC_NUM_THREADS=<число потоков> ./build/bin/ppc_func_tests --gtest_filter=*ChernykhS*

# Тесты производительности
OMP_NUM_THREADS=<число потоков> PPC_NUM_THREADS=<число потоков> ./build/bin/ppc_perf_tests --gtest_repeat=5 
--gtest_filter="*ChernykhS*"
```

### Методика измерений результатов

- 5 запусков на одинаковых входных данных
- используется медианное значение времени

## 8. Результаты

Сетка: 400 x 400 x 400 (всего 64 x 10^6 узловых точек).
Функция интегрирования: `sinx*cosx*e^x`
Результаты ускорения и эффективности сравнивались с последовательной версией (SEQ): 1.464 с., значения времени
использовано из тестов `pipeline`.

| workers | time (s) | speedup | efficiency |
| :------ | :------- | :------ | :--------- |
| 1       | 1.629    | 0.90    | 0.90       |
| 2       | 0.855    | 1.71    | 0.86       |
| 3       | 0.621    | 2.36    | 0.79       |
| 6       | 0.342    | 4.28    | 0.71       |
| 12      | 0.271    | 5.40    | 0.45       |

Наблюдения:

- До 3 потоков почти линейный рост производительности с минимальными накладными расходами.
- От 6 до 12 потоков падение эффективности масштабирования.
- Причины снижения эффективности:
  - Ограничение пропускной способности шины данных при одновременном обращении множества потоков.
  - Неравномерное распределение итераций между воркерами.
  - Аппаратный лимит: Конкуренция 12 логических потоков за ресурсы 6 процессора.

## 9. Выводы

- Реализован параллельный алгоритм многомерного численного интегрирования методом трапеций с использованием технологии
OpenMP
- Корректность подтверждена функциональными тестами
- Получено ускорение до 5.4 раза
- Эффективность держится выше 0.71 до 6 потоков, после 6 падает до 0.45
