# Отчёт по лабораторной работе №3 – Фильтрация изображений с использованием ядра Гаусса (OMP)

**Студент:** Журин Иван Эдуардович  
**Группа:** 3823Б1ПР1  
**Вариант:** 25  
**Технология:** OMP

---

## Введение

**Цель работы:** разработка параллельной реализации линейной фильтрации изображений с использованием OpenMP.

**Актуальность:** ускорение обработки изображений за счёт распараллеливания вычислений
 на многоядерных процессорах.

---

## Постановка задачи

Разработать программу, выполняющую свёртку целочисленного полутонового изображения с ядром Гаусса 3×3:

```cpp
static constexpr std::array<std::array<int, 3>, 3> kKernel = {{
    {1, 2, 1},
    {2, 4, 2},
    {1, 2, 1}
}};
static constexpr int kShift = 4;   // деление на 16
```

**Входные данные (`InType`):**

- ширина `width > 0`,
- высота `height > 0`,
- количество вертикальных полос `parts` (`1 ≤ parts ≤ width`),
- матрица пикселей `image[height][width]`.

**Выходные данные (`OutType`):**

- матрица того же размера, каждый пиксель — результат свёртки, поделённый на 16 (сдвиг вправо на 4 бита).

**Ограничения:**

- границы изображения обрабатываются нулевым дополнением (padding);
- выполняется проверка корректности размеров изображения и значения `parts`.

---

## Описание алгоритма

Алгоритм аналогичен последовательной версии,
 однако обработка вертикальных полос выполняется параллельно с использованием OpenMP.

### Основные этапы

1. Создание расширенной матрицы `padded` размером `(height + 2) × (width + 2)` с нулевыми границами.
2. Копирование исходного изображения в центральную область `padded`.
3. Разбиение изображения на вертикальные полосы:
   - `base_width = width / parts`,
   - `remainder = width % parts`.
4. Параллельная обработка полос.
5. Для каждого пикселя вычисляется свёртка с ядром Гаусса.

Результат нормализуется побитовым сдвигом:

```cpp
result = sum >> kShift;
```

Каждая полоса обрабатывается независимо, поэтому гонки данных отсутствуют.

---

## Детали реализации

**Файлы:**

- `omp/include/ops_omp.hpp`
- `omp/src/ops_omp.cpp`

### Ключевая директива OpenMP

```cpp
#pragma omp parallel for schedule(static) default(none) \
    shared(w, h, base_width, remainder, np, local_padded, local_result)
```

### Особенности реализации

- используется нулевой padding;
- свёртка развёрнута вручную для повышения производительности;
- распараллеливание выполняется по вертикальным полосам;
- применяется статическое распределение нагрузки (`schedule(static)`).

### Валидация входных данных

Проверяются:

- положительные размеры изображения;
- корректность числа полос;
- соответствие размеров матрицы значениям `width` и `height`.

---

## Тестирование

### Среда выполнения

Операционная система: Windows 11  
Компилятор: GCC 13.3.0  
Тип сборки: Release

---

### Функциональное тестирование

Проведено тестирование на изображениях размеров:

- `1×1`,
- `2×2`,
- `3×3`,
- `4×4`.

Проверялись:

- корректность вычисления свёртки;
- обработка границ;
- корректность параллельного выполнения.

Все тесты успешно пройдены.

---

## Тестирование производительности

**Размер изображения:** `4096×2048` (~8.4 млн пикселей)  

| Режим | Количество потоков | task_run (с) | Ускорение |
|-------|--------------------|--------------|-----------|
| OMP   | 1                  | 0.01438      | 1.00      |
| OMP   | 2                  | 0.00808      | 1.78      |
| OMP   | 4                  | 0.00569      | 2.53      |
| OMP   | 8                  | 0.00620      | 2.32      |

Корректность параллельной реализации проверялась сравнением результатов
с последовательной версией SEQ.

Проверка выполнялась для различных размеров изображений и различного числа потоков.
Во всех случаях результаты совпадали с последовательной реализацией.

### Анализ результатов

При увеличении количества потоков время выполнения уменьшается.
Максимальное ускорение достигается при использовании `4` потоков.
При дальнейшем увеличении числа потоков эффективность снижается из-за накладных расходов OpenMP
и ограничений пропускной способности памяти.

---

## Заключение

В ходе работы была разработана параллельная реализация фильтрации изображения с использованием OpenMP.

Распараллеливание обработки вертикальных полос позволило существенно сократить время выполнения
по сравнению с последовательной реализацией. Программа корректно обрабатывает границы изображения
и не содержит гонок данных.

Полученные результаты подтверждают эффективность применения OpenMP для задач обработки изображений.
