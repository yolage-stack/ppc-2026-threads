# Отчёт по лабораторной работе №3 – Фильтрация изображений с использованием ядра Гаусса (SEQ)

**Студент:** Журин Иван Эдуардович  
**Группа:** 3823Б1ПР1  
**Вариант:** 25
**Технология:** SEQ

---

## Введение

**Цель работы:** разработка последовательного алгоритма линейной фильтрации изображений с ядром Гаусса 3×3.  
   kernel = [[1, 2, 1],
            [2, 4, 2],
            [1, 2, 1]]
**Актуальность:** Гауссовское размытие является базовой операцией во многих задачах компьютерного зрения.
 Последовательная реализация служит эталоном корректности и базой для сравнения с параллельными версиями.

---

## Постановка задачи

Разработать программу, выполняющую свёртку целочисленного полутонового изображения с ядром Гаусса 3×3:

**Входные данные (`InType`):**  

- ширина `width > 0`,  
- высота `height > 0`,  
- количество вертикальных полос `parts` (1 ≤ `parts` ≤ `width`),  
- матрица пикселей `image[height][width]`.

**Выходные данные (`OutType`):**  

- матрица того же размера, каждый пиксель – результат свёртки, поделённый на 16 (сдвиг вправо на 4 бита).

**Ограничения:**  

- Границы изображения обрабатываются нулевым дополнением (padding).  
- Валидация проверяет корректность размеров и значения `parts`.

---

## Описание алгоритма

1. Создание расширенной матрицы `padded` размером `(height+2)×(width+2)` с нулевыми границами.  
2. Копирование исходного изображения в центральную область `padded`.  
3. Разбиение на вертикальные полосы (количество полос = `parts`).  
   - Ширина полосы: `part_width = width / parts + (part < remainder ? 1 : 0)`.  
   - Начальная колонка полосы: `x_start` (вычисляется для каждой части).  
4. Для каждого пикселя в полосе вычисляется свёртка с развёрнутым ядром (9 явных умножений и сложений).  
5. Результат сдвигается на 4 бита (`>> kShift`) и сохраняется в выходную матрицу.

---

## Детали реализации

**Файлы:**  

- `seq/include/ops_seq.hpp`  
- `seq/src/ops_seq.cpp`

**Ключевые особенности:**  

- Свёртка развёрнута вручную для производительности.  
- Padding – нулевой.  
- Однопоточное выполнение (эталон).

---

## Тестирование

### Среда выполнения

Операционная система: Windows 11  
Компилятор: GCC 13.3.0  
Тип сборки: Release

---

### Функциональное тестирование

Проведено тестирование на изображениях размеров:

- `1×1`,
- `2×2`,
- `3×3`,
- `4×4`.

Проверялись:

- корректность вычисления свёртки;
- обработка границ;

Все тесты успешно пройдены.

### Тестирование производительности

**Размер изображения:** 4096×2048 (8.4 млн пикселей), `parts = 4`.  

| Режим | pipeline (с) | task_run (с) | Ускорение (отн. SEQ) |
|-------|--------------|--------------|----------------------|
| SEQ   | 0.04062      | 0.01490      | 1.00 (baseline)      |

Последовательная реализация используется в качестве baseline
для сравнения производительности параллельных версий.

Все параллельные реализации сравнивались с результатами SEQ.

---

## Заключение

Последовательная реализация успешно выполняет фильтрацию и служит эталоном корректности.
 Её время выполнения используется для расчёта ускорения параллельных версий.
