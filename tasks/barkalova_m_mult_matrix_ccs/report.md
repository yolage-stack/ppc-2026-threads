# Умножение разреженных матриц. Элементы комплексного типа. Формат хранения матрицы – столбцовый (CCS)

- Student: Баркалова Мария Константиновна, group 3823Б1ПМоп3
- Variant: 7
- Local reports: seq/report.md, omp/report.md, tbb/report.md, stl/report.md, all/report.md

## 1. Контекст

В данной работе решается задача умножения разреженных матриц в формате CCS с комплексными числами. Эта задача выбрана для
сравнения разных моделей параллелизма, потому что:

1) Вычисления независимы - столбцы результирующей матрицы вычисляются независимо, что позволяет легко распараллелить задачу
2) Универсальность - задача решается пятью способами: последовательно (SEQ), с помощью OpenMP, TBB, STL и гибридно (MPI+OpenMP)

Сравнение этих подходов позволяет понять, какая технология лучше подходит для разных условий: простоты реализации,
производительности и масштабируемости.

## 2. Единая постановка задачи

Входные данные
Две разреженные матрицы A и B в формате CCS:

- values - вектор комплексных чисел (ненулевые элементы)
- row_indices - вектор номеров строк для каждого элемента
- col_ptrs - вектор указателей на начало каждого столбца (размер cols + 1)
- rows, cols, nnz - размеры и количество ненулевых элементов

Выходные данные
Матрица C = A × B в том же формате CCS.

Ограничения

- количество столбцов A должно равняться количеству строк B
- все размеры матриц должны быть положительными
- формат CCS должен быть корректным: col_ptrs[0] == 0, размер col_ptrs == cols + 1, nnz == values.size()

Результат считается корректным, если выполняются все условия:

- размеры rows и cols совпадают с ожидаемыми
- количество ненулевых элементов nnz совпадает
- массивы col_ptrs и row_indices полностью идентичны эталону
- значения элементов отличаются не более чем на eps = 10^-10

## 3. Единая методика эксперимента

CPU: AMD Ryzen 5 3500U with Radeon Vega Mobile Gfx 2.10 GHz (4 cores, 8 threads)
RAM: 8 GB
OS: Windows 10 version 22H2
Компилятор: MinGW GCC 6.3.0
build type: Release

### Переменные окружения

| Переменная                    | Назначение                                                                    |
|-------------------------------|-------------------------------------------------------------------------------|
|PPC_NUM_THREADS=N              | Число потоков для OMP, TBB, STL и внутрипроцессного уровня ALL                |
|OMP_NUM_THREADS=N, mpiexec -n P| Число MPI-процессов для гибридной версии                                      |

### Генерация данных

Для функциональных тестов используется 6 предопределённых матриц, покрывающих различные ситуации.
Эти тесты проверяют корректность на матрицах разных размеров (включая прямоугольные), с разной разреженностью
и с комплексными числами.

Для тестов производительности матрицы генерируются случайным образом с помощью функции CreateRandomSparseMatrix():

- размер: 1500×1500
- плотность: 1%
- значения: случайные комплексные числа от 0.1 до 10.0
так же в отчетах приведены таблицы с результатами, где:
Speedup = время_последовательной_версии / время_параллельной_версии
Efficiency = (Speedup / количество_потоков) × 100%
Каждый замер выполняется 5 раз. В таблицах приведено медианное значение времени.

## 4. Сводка корректности

Все реализации (OMP, TBB, STL, ALL) сравниваются с эталонной последовательной версией через метод
CheckTestOutputData().
Совпадение считается достигнутым, если совпадают размеры матриц rows и cols, количество ненулевых элементов
nnz, массивы col_ptrs и row_indices полностью идентичны, а значения элементов отличаются не более
чем на eps = 10^-10

В функциональных тестах используются 6 предопределённых тестовых случаев:

| № | Размер A | Размер B | Особенности                                |
|---|----------|----------|--------------------------------------------|
| 1 |   2×2    |  2×2     |  Диагональные матрицы, комплексные числа   |
| 2 |  3×3     |  3×3     |  Единичная матрица A                       |
| 3 |  2×2     |  2×2     |  Диагональные матрицы, действительные числа|
| 4 |  2×2     |  2×3     |  Прямоугольные матрицы                     |
| 5 |  3×3     |  3×3     |  Разреженные матрицы (по 4 ненуля)         |
| 6 |  3×2     |  2×4     |  Прямоугольные разреженные матрицы         |

Все тесты пройдены для всех реализаций.

## 5. Агрегированные результаты

| Mode          | Count | Time, ms     | Speedup | Efficiency  |
|---------------|-------|--------------|---------|-------------|
| seq (task)    | 1     |   1953       | 1.00    | 100%        |
| seq (pipeline)| 1     |   2390       | 1.00    | 100%        |
| omp (task)    | 2     |    545       | 3.58    | 179%        |
| omp (pipeline)| 2     |    679       | 3.52    | 176%        |
| omp (task)    | 4     |    496       | 3.94    | 98.5%       |
| omp (pipeline)| 4     |    601       | 3.98    | 99.5%       |
| tbb (task)    | 2     |   1045       | 1.87    | 93.5%       |
| tbb (pipeline)| 2     |   1204       | 1.99    | 99.5%       |
| tbb (task)    | 4     |    663       | 2.95    | 73.8%       |
| tbb (pipeline)| 4     |    764       | 3.13    | 78.3%       |
| stl (task)    | 2     |   456        | 4.28    | 214%        |
| stl (pipeline)| 2     |   541        | 4.42    | 221%        |
| stl (task)    | 4     |   469        | 4.16    | 104%        |
| stl (pipeline)| 4     |   554        | 4.31    | 108%        |

MPI + OpenMP

| Mode(ranks × threads) | Count | Time, ms     | Speedup | Efficiency  |
|-----------------------|-------|--------------|---------|-------------|
| seq (task)            | 1     |   1953       | 1.00    | 100%        |
| 1 × 2                 | 2     |   725        | 2.69    | 135%        |
| 1 × 4                 | 4     |   713        | 2.74    | 69%         |
| 2 × 1                 | 2     |   737        | 2.65    | 133%        |
| 2 × 2                 | 4     |   718        | 2.72    | 68%         |
| 2 × 4                 | 8     |   751        | 2.60    | 33%         |

## 6. Интерпретация различий

### SEQ (baseline)

Последовательная версия показывает базовое время выполнения 1953 мс. От этого значения рассчитывается ускорение
всех параллельных версий.

### OMP

Плюсы: простой код (одна директива #pragma omp parallel for), отсутствие лишних накладных расходов,
хорошее ускорение (3.94 на 4 потоках). Слабые стороны: при schedule(static) возможен дисбаланс, если
столбцы сильно различаются по количеству ненулевых элементов.

### TBB

TBB автоматически разбивает диапазон на блоки. grainsize не задавался - использовалось значение по умолчанию.
Это создало дополнительные накладные расходы на управление задачами. Результат: ускорение 2.95 на 4 потоках
(эффективность 73.8%) - хуже, чем у OMP. Вывод: автоматическая балансировка TBB не всегда даёт выигрыш.

### STL

Ручное управление через std::async дало лучший speedup (4.16) и сверхлинейную эффективность (104% на 4 потоках,
214% на 2 потоках). Минус - сложный код: нужно самому делить диапазоны, запускать задачи, ждать через future.get().
Высокий риск ошибок.

### ALL (MPI+OpenMP)

Цена коммуникации оказалась высокой. На 4 потоках ускорение всего 2.74 (эффективность 69%), а при 2×4 (8 потоков) — 2.60
(эффективность 33%).
Гибрид имеет смысл только при работе на нескольких компьютерах или при очень больших объёмах данных.

## 7. Репродуцируемость

### Сборка

```powershell
cmake -S . -B build -D USE_FUNC_TESTS=ON -D USE_PERF_TESTS=ON -D CMAKE_BUILD_TYPE=Release
cmake --build build --config Release --parallel
```

### Функциональные тесты

```powershell
cd build\bin
$env:PPC_NUM_THREADS=4; .\ppc_func_tests --gtest_filter="*barkalova_m_mult_matrix_ccs*"
```

```powershell
cd build\bin
set OMP_NUM_THREADS=2
mpiexec -n 2 .\ppc_func_tests.exe --gtest_filter="*barkalova_m_mult_matrix_ccs_all*"
```

### Тесты производительности

```powershell
cd build\bin
$env:PPC_NUM_THREADS=4; .\ppc_perf_tests --gtest_filter="*barkalova_m_mult_matrix_ccs*"
```

```powershell
cd build\bin
set OMP_NUM_THREADS=2
mpiexec -n 2 .\ppc_perf_tests.exe --gtest_filter="*barkalova_m_mult_matrix_ccs_all*"
```

Для каждого замера выполняется 5 повторений. В таблицы выносится медианное значение времени.

## 8. Заключение

Лучшая версия получилась OpenMP. Она даёт отличное ускорение (3.94), проста в реализации и не имеет лишних накладных расходов.
STL чуть быстрее (4.16), но код сложнее.

Ограничения: замеры только на одном процессоре, при одной плотности (1%) и одном размере матриц (1500×1500). На других данных
результаты могут отличаться.

Можно было бы улучшить добавив schedule(dynamic) для OMP, подобрав оптимальный grainsize для TBB и реализовать динамическую
балансировку для STL.

## 9. Источники

1. Документация курса Parallel Programming Course.
2. Пример реализации из курса - tasks/example_threads из репозитория
3. Документация по библиотеке Intel OneTBB:
<https://www.intel.com/content/www/us/en/docs/onetbb/get-started-guide/2023-0.html>
