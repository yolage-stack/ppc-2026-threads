# Выделение границ изображения с использованием оператора Собеля — ALL (MPI + STL)

- Студент: Баданов Александр Леонидович
- Технология: ALL (MPI + STL)
- Вариант: 27

## 1. Контекст

Для обеспечения максимальной производительности на кластерных системах требуется гибридная модель параллелизма.
MPI обеспечивает межпроцессное взаимодействие (распределение строк изображения между узлами),
а STL-потоки — внутрипроцессный параллелизм на многоядерных узлах.

## 2. Постановка задачи

### Входные данные

Одномерный массив std::vector<uint8_t>, представляющий собой растровое изображение в градациях серого.
Изображение может быть:

- Квадратным: width = height = √(size)
- Неквадратным: интерпретируется как матрица height × width, где width = size, height = 1

### Выходные данные

Бинарное изображение того же размера, где:

- Границы (пиксели с магнитудой градиента выше порога) — белый цвет (255)
- Фон — чёрный цвет (0)

### Ограничения

- Значения пикселей находятся в диапазоне [0, 255]
- Порог threshold — целое число от 0 до 255
- Изображение может быть любого размера, включая 1×1

### Крайние случаи

- Пустое изображение (все пиксели равны 0)
- Изображение размером менее 3×3 пикселя (оператор Собеля не применяется)
- Неквадратные изображения (обрабатываются как одномерные)
- Постоянное изображение (отсутствие градиента)

## 3. Базовый алгоритм

### Принцип работы

Оператор Собеля использует свёртку изображения с двумя ядрами 3×3:

- Gx для обнаружения горизонтальных границ
- Gy для обнаружения вертикальных границ
Магнитуда градиента вычисляется как M = √(Gx² + Gy²). Применяется пороговая фильтрация для получения бинарного результата.

### Пошаговое описание

Предобработка

- Размер изображения интерпретируется как квадратное (width = height = √(size))
- Если изображение не квадратное, обрабатывается как одномерное (width = size, height = 1)

Применение оператора Собеля
Для каждого внутреннего пикселя (строка от 1 до height-2, столбец от 1 до width-2):

- Вычисляется свёртка с ядром Gx для горизонтального градиента
- Вычисляется свёртка с ядром Gy для вертикального градиента
- Вычисляется магнитуда M = √(Gx² + Gy²)
- Сохраняется в массив magnitude
- Отслеживается глобальный максимум max_magnitude

Пороговая обработка
Если max_magnitude > 0:

- Вычисляется коэффициент масштабирования scale = 255 / max_magnitude
- Для каждого пикселя: output[i] = (magnitude[i] * scale > threshold) ? 255 : 0
Иначе: все пиксели выходного изображения равны 0

### Критерий корректности

- Границы изображения (первая/последняя строка, первый/последний столбец) остаются нулевыми
- Для маленьких изображений (height < 3 или width < 3) выходное изображение равно входному
- Для пустого изображения выходное изображение полностью состоит из нулей
- При наличии градиента выходное изображение содержит ненулевые пиксели

## 4. Схема распараллеливания

### Распределение строк между MPI-процессами

```cpp
int rows_per_process = (height - 2) / size_;
rows_per_process = std::max(rows_per_process, 1);

int start_row = 1 + (rank_ * rows_per_process);
int end_row = start_row + rows_per_process;
end_row = std::min(end_row, height - 1);
```

### Агрегация результатов

```cpp
// Глобальный максимум
MPI_Allreduce(&max_magnitude, &global_max, 1, MPI_FLOAT, MPI_MAX, MPI_COMM_WORLD);

// Сбор магнитуд на главный процесс
MPI_Reduce(magnitude.data(), recv_buffer.data(), magnitude_size, MPI_FLOAT, MPI_MAX, 0, MPI_COMM_WORLD);

// Рассылка результата
MPI_Bcast(output.data(), output_size, MPI_BYTE, 0, MPI_COMM_WORLD);
```

## 5. Детали реализации

### Структура файлов

- `all/include/ops_all.hpp` — заголовочный файл
- `all/src/ops_all.cpp` — гибридная реализация

### Обработка граничных случаев

- Для маленьких изображений (height < 3 или width < 3) параллельные вычисления не запускаются
- Неквадратные изображения обрабатываются как одномерные

## 5. Проверка корректности

- Результат работы ALL версии сравнивается с последовательной версией (SEQ). Используются те же тестовые файлы
(test_1.txt — test_11.txt), что и для SEQ версии.

### Функциональные тесты (10 тестовых случаев)

| Тест        | Размер  | Описание          |  Проверяемые элементы                    |
|-------------|---------|-------------------|------------------------------------------|
| test_1.txt  | 4×4     | Простой квадрат   |  4 угловых граничных пикселя             |
| test_2.txt  | 8×8     | Градиент          |  Плавные переходы яркости                |
| test_3.txt  | 16×16   | Диагональная линия|  Диагональные границы                    |
| test_4.txt  | 8×8     | Пустое изображение|  Все пиксели равны 0                     |
| test_5.txt  | 16×16   | Шахматная доска   |  Частые перепады яркости                 |
| test_6.txt  | 16×16   | Крест             |  Вертикальные и горизонтальные границы   |
| test_7.txt  | 1×8     | Неквадратное      |  Одномерный массив                       |
| test_8.txt  | 8×1     | Неквадратное      |  Одномерный массив                       |
| test_9.txt  | 2×2     | Маленькое         |  Возврат входного массива                |
| test_10.txt | 1×1     | Маленькое         |  Возврат входного массива                |
| test_11.txt | 3×2     | Маленькое         |  Возврат входного массива                |

Пример тестового случая (test_1.txt — простой квадрат)
Входное изображение (4×4):

```markdown
0 0 0 0
0 255 255 0
0 255 255 0
0 0 0 0
```

Ожидаемый результат (границы квадрата выделены белым):

```markdown
0 0 0 0
0 255 255 0
0 255 255 0
0 0 0 0
```

Дополнительные тесты

- Тест градиентов (GradientComputationProducesEdges): проверка, что при наличии градиента выходное изображение
содержит ненулевые пиксели
- Тест резкого перехода (EdgeDetectionWorksWithSharpTransition): проверка выделения границы 3×3
- Тест нулевого изображения (AllZeroImageOutputAllZero): проверка, что выходное изображение состоит из нулей
- Тест постоянного изображения (ConstantImageNoEdges): проверка отсутствия границ

## 6. Экспериментальная среда

### Hardware/OS

- процессор: Apple M1 (MacBook Air)
- ядра/потоки: 8 ядер (4 производительных + 4 энергоэффективных)
- оперативная память: 8 GB
- операционная система: macOS 14.3
- архитектура: ARM64

### Toolchain

- компилятор: GCC 15.2.0 (Homebrew)
- стандарт: C++23
- тип сборки: Release
- система сборки: CMake 3.25 + Ninja

### Входные тестовые данные

- Формат: текстовые файлы (`.txt`) с размерами и значениями пикселей
- Расположение: `tasks/badanov_a_select_edge_sobel/data/`
- Размеры тестов: от 1×1 до 3840×2160 (для производительности)

### Метрики производительности

Speedup (ускорение):
`Speedup = T_seq / T_parallel`

Efficiency (эффективность):
`Efficiency = (Speedup / Workers) × 100%`

## 7. Результаты

Замер производительности проводился при размере входных данных:

- изображение 3840×2160 (4K)
- градиентная заливка (col % 256)

| Configuration | Workers | Time, s | Speedup | Efficiency |
|---------------|---------|---------|---------|------------|
|     seq       |   1     | 0.0549  |  1.00   |    100%    |
|   all (2×1)   |   2     | 0.0390  |  1.41   |    70%     |
|   all (2×2)   |   4     | 0.0370  |  1.48   |    37%     |
|   all (4×1)   |   4     | 0.0827  |  0.66   |    17%     |

### Анализ результатов

- 2 процесса × 1 поток: ускорение 1.41×, эффективность 70%
- 2 процесса × 2 потока: ускорение 1.48×, эффективность 37% — лучшая конфигурация
- 4 процесса × 1 поток: ускорение 0.66×, эффективность 17% — производительность ниже последовательной!

При увеличении числа MPI-процессов до 4 на одном узле производительность падает из-за накладных расходов на коммуникации.
Оптимальная конфигурация на одном узле — 2 процесса с 2 потоками.

## 8. Выводы

ALL-версия оправдана только при работе с очень большими изображениями или в кластерных средах.
На одном узле использование более 2 MPI-процессов неэффективно. Для большинства задач на одном узле рекомендуется использовать
чисто потоковые решения (OMP, TBB, STL).
