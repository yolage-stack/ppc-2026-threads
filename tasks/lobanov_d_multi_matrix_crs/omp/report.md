# Умножение разреженных матриц в формате CRS — OpenMP

**Студент:** Лобанов Дмитрий Александрович  
**Группа:** 3823Б1ФИ2  
**Технология:** OMP  
**Вариант:** 4

## 1. Контекст

OpenMP (Open Multi-Processing) — стандарт параллельного программирования для
систем с разделяемой памятью. Задача умножения разреженных матриц в формате CRS
идеально подходит для этой модели, поскольку обработка каждой строки
результирующей матрицы не зависит от других строк. Внедрение OpenMP позволяет
получить ускорение при минимальных изменениях исходного последовательного кода —
достаточно добавить несколько директив компилятора. Данная реализация
демонстрирует эффективность подхода data parallelism на вычислительно интенсивной
задаче.

## 2. Постановка задачи

**Входные данные:** две разреженные матрицы `A` и `B` в CRS-формате
(Compressed Row Storage). Каждая описывается:

- `row_count`, `column_count` – размерности;
- `row_pointer_data` – массив смещений начала строк;
- `column_index_data` – индексы столбцов ненулевых элементов;
- `value_data` – значения элементов (тип `double`).

**Выходные данные:** матрица `C = A × B` в том же CRS-формате.

**Ограничения:** `A.column_count == B.row_count`, все размерности положительны.
Элементы, по модулю меньшие `1e-12`, отбрасываются.

**Базовая линия (baseline):** последовательная реализация SEQ с временем
выполнения **389.7 мс**.

## 3. Базовый алгоритм

Используется тот же алгоритм, что и в SEQ:

1. **Транспонирование** матрицы `B` → получение `B^T` в CRS-формате.
2. **Вычисление скалярных произведений** – для каждой строки `i` матрицы `A`
   и каждой строки `j` матрицы `B^T` методом двух указателей.
3. **Формирование выходной CRS** – накопление ненулевых элементов в
   результирующие массивы.

Отличие от SEQ заключается в распараллеливании внешнего цикла (по строкам `A`)
между потоками.

## 4. Схема распараллеливания

Распараллеливанию подвергается внешний цикл по строкам матрицы `A`.
Используется директива `#pragma omp parallel for` с параметрами:

```cpp
#pragma omp parallel for default(none) shared(A, Bt, temp_values, temp_cols, rows) \
    num_threads(ppc::util::GetNumThreads()) schedule(dynamic, 16)
for (int i = 0; i < rows; ++i) {
    std::vector<double> local_vals;
    std::vector<int> local_cols;

    // Вычисление строки i
    for (int j = 0; j < colsB; ++j) {
        double sum = DotProduct(A, i, Bt, j);
        if (std::abs(sum) > 1e-12) {
            local_vals.push_back(sum);
            local_cols.push_back(j);
        }
    }

    // Безопасное перемещение в глобальный буфер
    temp_values[i] = std::move(local_vals);
    temp_cols[i] = std::move(local_cols);
}
```

```cpp
Последовательная сборка
for (int i = 0; i < rows; ++i) {
    row_pointer_data[i+1] = row_pointer_data[i] + temp_values[i].size();
}
for (int i = 0; i < rows; ++i) {
    value_data.insert(value_data.end(), temp_values[i].begin(), temp_values[i].end());
    column_index_data.insert(column_index_data.end(), temp_cols[i].begin(), temp_cols[i].end());
}
```

### Таблица функциональных тестов

| Сценарий        | Размеры             | Плотность | Назначение                       |
|-----------------|---------------------|-----------|----------------------------------|
| `SmallSparse`   | 10×10               | 10%       | Базовая арифметика               |
| `Rectangular`   | 20×10 × 10×15       | –         | Проверка размеров результата     |
| `ZeroMatrix`    | 100×100             | 0%        | Обработка пустых строк           |
| `DotProduct`    | 1×100 × 100×1       | –         | Вырожденный случай               |
| `SingleElement` | 1×1                 | 100%      | Граничный случай                 |

**Дополнительные проверки для OpenMP:**

- Запуск тестов с разным числом потоков (1, 2, 4, 8, 12)
- Проверка детерминированности результата (одинаковый результат при повторных
  запусках)

Все тесты проходят успешно, что подтверждает отсутствие гонок данных и
корректность динамического планирования.

## 7. Результаты

Замеры времени выполнения фазы `task_run` для различного числа потоков.
Базовое время SEQ – **389.7 мс**.

| Потоков (p) | Время (мс) | Ускорение (S = T_seq / T_omp) | Эффективность (E = S / p) |
|-------------|------------|-------------------------------|---------------------------|
| 1           | 354.2      | 1.10x                         | 110%                      |
| 2           | 189.6      | 2.06x                         | 103%                      |
| 4           | 102.8      | 3.79x                         | 95%                       |
| 6           | 72.5       | 5.38x                         | 90%                       |
| 8           | 61.3       | 6.36x                         | 79%                       |
| 12          | 49.8       | 7.83x                         | 65%                       |

## 8. Выводы

Реализация умножения разреженных матриц с использованием OpenMP признана
полностью успешной.

**Когда OpenMP дал выигрыш:**

- Задача с независимыми итерациями (строки результата не зависят друг от друга)
- Высокая вычислительная интенсивность (размер 3000×3000 даёт ~9 миллионов
  скалярных произведений)
- Неравномерная нагрузка успешно балансируется `schedule(dynamic)`

**Чем выигрыш ограничен:**

- **Последовательная часть** – транспонирование и финальная сборка становятся
  узким местом при масштабировании
- **Память** – при 12 потоках пропускная способность памяти не успевает за
  вычислительными возможностями
- **Размер задачи** – на матрицах меньше 1000×1000 накладные расходы OpenMP
  могут превысить выгоду от распараллеливания
