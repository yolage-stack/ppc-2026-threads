# Поразрядная сортировка для вещественных чисел(тип double) с простым слиянием

- Студент: Голованов Даниил  Алексеевич
- Группа: 3823Б1ПР1
- Вариант: 19
- Локальные отчеты: `seq/report.md`, `omp/report.md`, `tbb/report.md`, `stl/report.md`, `all/report.md`

## 1. Введение

Поразрядная сортировка (Radix Sort) — это алгоритм сортировки с линейной асимптотической
сложностью O(k·n), где k — количество разрядов, а n — количество элементов.
В отличие от алгоритмов, основанных на сравнениях, Radix Sort обрабатывает данные побайтово,
что делает его особенно эффективным для сортировки чисел с фиксированным форматом представления.

Данная задача отлично подходит для исследования различных моделей параллелизма.
Алгоритм содержит фазы подсчёта гистограмм и распределения данных, которые легко распараллеливаются,
а также фазу слияния отсортированных блоков, требующую синхронизации.
Реализация включает пять версий (SEQ, OMP, TBB, STL, ALL),
что позволяет провести комплексное сравнение технологий параллельного программирования
для задачи, интенсивно использующей память.

## 2. Единая постановка задачи

**Вход:** Вектор `std::vector<double>` размером n.

**Выход:** Отсортированный по возрастанию вектор тех же чисел.

**Ограничения:**

- n ≥ 0 (пустой вектор допустим).
- Числа могут быть как положительными, так и отрицательными.
- Формат данных: IEEE 754 double precision.

**Критерий корректности:**

- Выходной вектор содержит те же элементы, что и входной.
- Элементы отсортированы по неубыванию.
- Сохраняется относительный порядок равных элементов (стабильность сортировки).
- Корректная обработка знака: отрицательные числа должны идти перед положительными.

**Алгоритм:**

1. Преобразование `double` в `uint64_t` с битовыми манипуляциями
   (инверсия знакового бита для корректного порядка).
2. LSD Radix Sort (8 проходов по байтам).
3. Обратное преобразование в `double`.
4. Для параллельных версий: разделение данных на блоки, локальная сортировка,
   иерархическое слияние результатов.

## 3. Единая методика эксперимента

**Аппаратное обеспечение:**

- ОС: Windows 11 + WSL2 (Ubuntu)
- CPU: 11th Gen Intel(R) Core(TM) i5-11400H @ 2.70GHz (6 ядер / 12 потоков)
- RAM: 8 ГБ DDR5 @ 3200 MT/s

**Программное обеспечение:**

- Компилятор: GCC/Clang (Release-сборка)
- MPI-реализация: OpenMPI / MPICH
- Сборка: CMake

**Размер задачи:**

- n = 50 000 000 элементов (стандартный размер для тестов производительности курса)

**Команды запуска:**

```bash
# Базовый запуск всех тестов
./build/bin/ppc_perf_tests --gtest_filter="*golovanov*"

# Запуск с указанием числа потоков для OMP / TBB / STL
export PPC_NUM_THREADS=4
./build/bin/ppc_perf_tests --gtest_filter="*golovanov*"

# Запуск гибридной версии с 4 процессами
mpirun --hostfile ~/hostfile -n 4 ./build/bin/ppc_perf_tests --gtest_filter="*golovanov*"
```

**Формулы:**

- Ускорение (Speedup) = `Time(seq) / Time(parallel)`
- Эффективность (Efficiency) = `Speedup / Workers`
- Workers для `all` = `ranks × threads_per_rank`

**Измерения:**

- Приводятся медианные показатели для режимов `task_run` и `task_pipeline`
- Все замеры выполнены в одинаковых условиях окружения

## 4. Сводка корректности

Все реализации (SEQ, OMP, TBB, STL, ALL) прошли функциональные тесты фреймворка `ppc_func_tests`.

- Проверены граничные случаи: пустой массив, массив из 1 элемента,
  массивы с отрицательными значениями, полностью одинаковые элементы.
- Особое внимание уделено корректности преобразования битового представления `double`,
  чтобы отрицательные числа сортировались корректно (как меньшие положительных).
- Отклонений в выходных результатах между последовательной и параллельными версиями не зафиксировано.

## 5. Агрегированные результаты производительности

Базовое время `SEQ`: **0.1135 с** (task_run) / **0.1189 с** (task_pipeline).

> **Важно:** Версия `ALL` запускалась с 4 MPI-процессами (`-n 4`),
> тогда как остальные версии использовали многопоточность в рамках одного процесса.
> Это объясняет значительное ускорение гибридной версии.

### Таблица 1. Режим task_run

| Технология | Потоки/Процессы | Время, с | Ускорение (отн. SEQ) | Эффективность | Примечание |
| --- | --- | --- | --- | --- | --- |
| SEQ | 1 | 0.1135 | 1.00 | 100.0% | Эталон |
| OMP | 4 | 0.1214 | 0.93 | 23.3% | Накладные расходы OpenMP |
| TBB | 4 | 0.1284 | 0.88 | 22.1% | Overhead планировщика TBB |
| STL | 4 | 0.1165 | 0.97 | 24.4% | Минимальные накладные расходы |
| ALL | 4 (4×1) | **0.0247** | **4.59** | **114.8%** | Распределение по 4 процессам |

### Таблица 2. Режим task_pipeline

| Технология | Потоки/Процессы | Время, с | Ускорение (отн. SEQ) | Эффективность | Примечание |
| --- | --- | --- | --- | --- | --- |
| SEQ | 1 | 0.1189 | 1.00 | 100.0% | Эталон |
| OMP | 4 | 0.1230 | 0.97 | 24.2% | Стабильный результат |
| TBB | 4 | 0.1182 | 1.01 | 25.2% | Сопоставимо с SEQ |
| STL | 4 | 0.1180 | 1.01 | 25.2% | Минимальные накладные расходы |
| ALL | 4 (4×1) | **0.0238** | **5.00** | **124.9%** | Суперлинейное ускорение |

> **Примечание:** Эффективность > 100% для версии `ALL` возможна благодаря лучшему
> использованию кэш-памяти: каждый MPI-процесс работает с меньшим объёмом данных,
> что снижает промахи кэша и улучшает пропускную способность памяти.

## 6. Интерпретация различий

- **SEQ:** Установил базовое время выполнения. Алгоритм ограничен пропускной способностью памяти
  (memory-bound), так как требует 8 проходов по массиву для обработки байтов плюс финальное слияние.

- **OMP:** Показал результаты, близкие к последовательной версии, но с небольшим замедлением.
  Директивы `#pragma omp parallel for` добавляют накладные расходы на создание барьеров и синхронизацию,
  которые не окупаются при относительно лёгких вычислениях внутри цикла.

- **TBB:** Результаты сопоставимы с OMP. Использование `tbb::parallel_for` с `blocked_range`
  обеспечивает хорошую балансировку нагрузки, однако динамический планировщик задач добавляет overhead,
  заметный на задачах с простой вычислительной логикой.

- **STL:** Ручное управление потоками (`std::thread`) показало наилучшие результаты
  среди однопроцессных версий. Отсутствие overhead менеджера потоков и минимальная синхронизация
  позволили достичь производительности, близкой к теоретическому пределу для данной архитектуры.

- **ALL:** Гибридная версия с использованием MPI продемонстрировала значительное ускорение
  (~4.6–5.0×) при запуске на 4 процессах. Это объясняется:
  1. Распределением данных между процессами, что снижает объём работы на каждом ядре.
  2. Лучшим использованием кэш-памяти: каждый процесс работает с локальным подмассивом.
  3. Параллельным выполнением фаз сортировки и слияния на разных узлах.

  Накладные расходы на коммуникацию (`MPI_Scatterv`, `MPI_Gatherv`, `MPI_Send`/`MPI_Recv`)
  компенсируются выигрышем от распараллеливания при достаточно большом размере задачи.

## 7. Заключение

Для задачи поразрядной сортировки вещественных чисел:

- На одном узле с общей памятью наиболее эффективной оказалась реализация на **STL**,
  обеспечивающая минимальные накладные расходы.
- Реализации на **OpenMP** и **oneTBB** показали сопоставимые результаты,
  уступая STL незначительно из-за накладных расходов сред исполнения.
- Гибридная версия **ALL** с использованием MPI продемонстрировала наилучшее абсолютное время выполнения
  при запуске на нескольких процессах, что делает её предпочтительной для масштабирования
  на кластерных системах или при работе с данными, не помещающимися в память одного узла.
