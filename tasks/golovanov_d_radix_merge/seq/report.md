# Параллельная сортировка слиянием на основе поразрядной сортировки (Radix Merge) — SEQ

- Студент: Голованов Даниил Алексеевич
- Группа: 3823Б1ПР1
- Вариант: 19
- Технология: SEQ

## 1. Контекст

В данной работе реализована и исследована параллельная сортировка больших массивов чисел
с плавающей запятой двойной точности (`double`). Последовательная версия служит эталонной
реализацией, на которой основываются и с которой сравниваются все последующие параллельные
версии (OpenMP, TBB, `std::thread`, MPI+гибрид). Она задаёт структуру пайплайна
(Validation → PreProcessing → Run → PostProcessing) и обеспечивает базовую метрику
производительности.

## 2. Постановка задачи

**Вход:** Вектор `std::vector<double>` произвольной длины `n ≥ 0`.

**Генерация данных (тесты производительности):**
Массив формируется симметрично: от `-count` до `+count`, где `count = 500 000`.
Итоговый массив имеет `n = 1 000 001` элемент:

[-500000.0, -499999.0, ..., -1.0, 0.0, 1.0, ..., 499999.0, 500000.0]

**Выход:** Отсортированный по неубыванию вектор той же длины.

**Критерий корректности:** `std::ranges::is_sorted(output) == true`.

**Ограничения и крайние случаи:** `n ≥ 0`. При `n = 0` возвращается пустой вектор.
При `n = 1` сортировка не производится.

## 3. Базовый алгоритм

Последовательный алгоритм включает следующие этапы:

1. **Преобразование чисел с плавающей точкой**  
   Число `double` интерпретируется как 64-битное целое (`uint64_t`).
   Для обеспечения правильного порядка отрицательных чисел выполняется преобразование:  
   `ToSortable(bits) = (bits & sign_mask) ? ~bits : (bits ^ sign_mask)`.

2. **LSD-поразрядная сортировка (Radix sort)**  
   - Выполняется 8 проходов (по одному на каждый байт 64-битного числа).  
   - На каждом проходе подсчитывается гистограмма (256 корзин), вычисляются префиксные суммы
     и выполняется перестановка элементов во временный буфер.  
   - После завершения всех проходов выполняется обратное преобразование `FromSortable`.

Асимптотика по времени: **O(k·n)** = **O(8·n)**, где k — число байт (константа).  
Асимптотика по памяти: **O(n)** для временного буфера.  

Инвариант: после каждого байтового прохода массив частично упорядочен по младшим байтам,
а после восьмого прохода полностью отсортирован в порядке возрастания.

## 4. Детали реализации

**Файлы:** `seq/include/ops_seq.hpp`, `seq/src/ops_seq.cpp`

В `ValidationImpl` проверяется неотрицательность входа (при `n = 0` задача считается корректной).  
В `PreProcessingImpl` происходит генерация тестового массива (симметричный диапазон).  
В `RunImpl` вызывается функция `RadixSort::Sort(arr)`, содержащая основную логику алгоритма.  
В `PostProcessingImpl` выполняется финальная проверка (в тестах производительности проверяется
`is_sorted`).

Ключевой фрагмент реализации — цикл по байтам:

```cpp
// Файл: radix_sort.cpp
for (int byte = 0; byte < kBytes; ++byte) {
  std::array<size_t, kRadix> count{};

  // Подсчёт гистограммы
  for (size_t i = 0; i < n; ++i) {
    auto b = static_cast<size_t>((data[i] >> (byte * 8)) & kByteMask);
    count.at(b)++;
  }

  // Префиксные суммы (переход от counts к позициям)
  size_t sum = 0;
  for (auto &c : count) {
    size_t tmp = c;
    c = sum;
    sum += tmp;
  }

  // Перестановка элементов
  for (size_t i = 0; i < n; ++i) {
    auto b = static_cast<size_t>((data[i] >> (byte * 8)) & kByteMask);
    buffer[count.at(b)++] = data[i];
  }

  data.swap(buffer);
}
```

Данный фрагмент демонстрирует ядро LSD radix sort — последовательное выполнение
восьми байтовых проходов.

## 5. Проверка корректности

Результат реализации SEQ принимается за эталон. Корректность подтверждается встроенными
тестами курса `ppc_func_tests`.

| Набор тестов | Входные данные | Ожидаемый результат | Результат |
| --- | --- | --- | --- |
| Набор 1 | `{3.14, -2.71, 0.0, 42.0, -1.5}` | `{-2.71, -1.5, 0.0, 3.14, 42.0}` | ✅ |
| Набор 2 | `{-10.0, -5.0, -3.0, -1.0, -7.0, 6}` | `{-10.0, -7.0, -5.0, -3.0, -1.0, 6}` | ✅ |
| Набор 3 | `{100.5, 1.1, 50.0, 2.2, 0.0001, 12, 2}` | `{0.0001, 1.1, 2, 2.2, 12, 50.0, 100.5}` | ✅ |
| Краевой случай | `n = 0` | пустой вектор | ✅ |
| Краевой случай | `n = 1` | исходный вектор | ✅ |

Для размера `n = 1 000 001` (используется в тестах производительности)
выходной массив полностью отсортирован, что подтверждается вызовом `std::ranges::is_sorted`.

## 6. Экспериментальная среда

**Аппаратное обеспечение:**

| Компонент | Характеристики |
| --- | --- |
| **Процессор** | 11th Gen Intel(R) Core(TM) i5-11400H @ 2.70 GHz |
| **Физические ядра** | 6 |
| **Логические потоки** | 12 (Hyper-Threading) |
| **Базовая частота** | 2.70 ГГц (макс. turbo: 4.50 ГГц) |
| **Оперативная память** | 8 ГБ DDR4 @ 3200 МТ/с |
| **ОС** | Windows 11 Pro + WSL2 (Ubuntu) |
| **Компилятор** | MSVC 19.50 (Release сборка, `/O2`) |

**Размер задачи:** `n = 1 000 001` элементов (`≈8 МБ`).

**Команды запуска:**

```powershell
# Последовательная версия
.\ppc_perf_tests --gtest_filter="*RadixMergePerf*"

# С указанием числа потоков
$env:PPC_NUM_THREADS = "1"
.\ppc_perf_tests --gtest_filter="*RadixMergePerf*"
```

## 7. Результаты

Для последовательной версии были произведены замеры времени для режимов `task_run` и `task_pipeline`.
Поскольку технология использует только 1 поток, результаты не зависят от `PPC_NUM_THREADS`.

### Таблица 1. Режим task_run

| Потоки | Время, с | Ускорение (отн. себя) | Эффективность |
| --- | --- | --- | --- |
| 1 | 0.1135 | 1.00 | 100.0% |

### Таблица 2. Режим pipeline

| Потоки | Время, с | Ускорение (отн. себя) | Эффективность |
| --- | --- | --- | --- |
| 1 | 0.1189 | 1.00 | 100.0% |

## 8. Выводы

Данная последовательная реализация демонстрирует абсолютную корректность на детерминированных
тестах и формирует базовое время исполнения:

- **`task_run`**: **0.1135 с**
- **`pipeline`**: **0.1189 с**

Эти метрики служат знаменателем для расчёта ускорения во всех многопоточных (OMP, TBB, STL)
и процессной (ALL) версиях алгоритма. Благодаря линейной асимптотике O(8·n) и компактной
работе с памятью, последовательная версия демонстрирует высокую производительность уже на
одном ядре, что затрудняет получение значительного ускорения на параллельных реализациях
(эффект «хорошего последовательного алгоритма»).
