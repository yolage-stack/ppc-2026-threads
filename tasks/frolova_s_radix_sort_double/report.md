# Сводный отчет: исследование эффективности параллельных версий Radix Sort для double

- Student: Фролова Софья Юрьевна, 3823Б1ФИ3
- Technology: OpenMP, oneTBB, `std::thread`, ALL (OpenMP + TBB)
- Variant: 19
- Local reports: `seq/report.md`, `omp/report.md`, `tbb/report.md`, `stl/report.md`, `all/report.md`

---

## 1. Введение

В данной работе мною исследуется задача высокопроизводительной поразрядной сортировки (Radix Sort) вещественных чисел
двойной точности (`double`). Сортировка массивов больших объемов данных является классической Memory Bound операцией,
скорость выполнения которой критически зависит от пропускной способности памяти, эффективного использования кэш-уровней
(L1/L2/L3) и минимизации накладных расходов на синхронизацию параллельных потоков.

Целью работы является проектирование, программная реализация и сравнительный анализ эффективности пяти вычислительных
архитектур на базе одного алгоритмического ядра:

- SEQ — последовательный baseline-алгоритм;
- OMP — макро-блочное распараллеливание на базе OpenMP;
- TBB — мелкозернистое распараллеливание с применением `oneapi::tbb::parallel_for` и расслабленных атомарных операций;
- STL — низкоуровневое ручное управление потоками через модель `std::thread`;
- ALL — комбинированная каскадная схема (OpenMP + TBB), использующая независимую декомпозицию данных на чанки и их
  последующее многопоточное деревовидное слияние.

---

## 2. Единая постановка задачи

### Входные данные

На вход подается неупорядоченный одномерный массив вещественных чисел типа `double`:

$$A \in \mathbb{R}^{N}  $$

Контейнер передачи — `std::vector<double>`. Размерность задачи $N$ варьируется от 10 элементов (для валидации граничных
ситуаций) до $10^7$ элементов (для нагрузочного тестирования производительности).

### Выходные данные

Выходной контейнер должен представлять собой вектор $B \in \mathbb{R}^{N}$, содержащий те же элементы,
перераспределенные в строгом соответствии с критерием упорядоченности по возрастанию:

$$B[i] \le B[j], \quad \forall \,\, i < j$$

### Алгоритмические ограничения

Классический алгоритм LSD (Least Significant Digit) Radix Sort разработан для беззнаковых целых чисел
(`unsigned integer`). Для работы с вещественными числами `double` (64 бита) алгоритм адаптирован следующим образом:

1. Выполняется побайтовая интерпретация бит вещественного числа с использованием `std::bit_cast<std::uint64_t>(val)`.
2. Алгоритм выполняет ровно 8 последовательных проходов (пасов) по байтам (каждый разряд имеет диапазон значений от 0 до
   255, $kRadix = 256$).
3. Так как битовое представление знака и экспоненты в стандарте IEEE 754 нарушает линейный порядок сортировки для
   отрицательных чисел, в конце алгоритма выполняется обязательная коррекция порядка: отрицательные числа извлекаются,
   их относительный порядок инвертируется, после чего они сливаются с положительными.

---

## 3. Единая методика эксперимента

### Аппаратное окружение

- CPU: AMD Ryzen 5 1600 Six-Core Processor
- RAM: 16 GB DDR4
- OS: Windows 11

### Программное окружение

- Compiler: GCC 15.2.0
- Build type: `Debug`
- C++ Standard: `C++20`.

### Параметры параллелизма

Число задействованных ядер/потоков контролируется динамически на этапе исполнения программы:

- Для OpenMP-компонентов: через переменную окружения `export OMP_NUM_THREADS=X`;
- Для TBB-компонентов и `std::thread`: средствами автоматического определения `std::thread::hardware_concurrency()` с
  жестким лимитированием верхнего порога по размеру задачи.

### Генерация данных

- Для функциональной валидации применялись фиксированные файловые выборки `test1.txt` – `test10.txt` (особые случаи
  распределения данных, отрицательные числа, дубликаты, экспоненциальные формы вроде `1e-10` или `3e8`).
- Для бенчмарков использовался генератор псевдослучайных чисел с равномерным распределением в широком вещественном
  диапазоне. Размер нагрузочного вектора зафиксирован на отметке **$N = 10^7$** элементов.

---

## 4. Сводка корректности

Проверка вычислительной корректности параллельных реализаций выполнена через фреймворк Google Test. Разработано 10
функциональных тестов, полностью покрывающих специфику работы со знаковыми типами данных стандарта IEEE 754.

Каждый тест осуществляет:

1. Загрузку/генерацию неотсортированного массива;
2. Прохождение фазы валидации (`ValidationImpl()`) и предобработки;
3. Выполнение сортировки исследуемым бэкендом;
4. Сравнение "элемент-в-элемент" с результатом последовательного выполнения алгоритма стандартной библиотеки
   `std::sort`.

Все 5 бэкендов во всех параллельных конфигурациях успешно прошли тестирование, что доказывает математическую
эквивалентность предложенных схем параллелизма исходному алгоритму.

---

## 5. Агрегированные результаты

### Сводная таблица производительности ($N = 10^7$)

| Backend | Конфигурация / Схема параллелизма        | Workers (Потоки) | Time (s) | Speedup | Efficiency |
| ------- | ---------------------------------------- | ---------------- | -------- | ------- | ---------- |
| SEQ     | Последовательный базовый алгоритм        | 1                | 0.4820   | 1.00    | 100.0%     |
| OMP     | Макро-декомпозиция на чанки              | 2                | 0.2835   | 1.70    | 85.0%      |
| OMP     | Макро-декомпозиция на чанки              | 4                | 0.1785   | 2.70    | 67.5%      |
| TBB     | `parallel_for` + `std::atomic_add`       | 2                | 0.3544   | 1.36    | 68.0%      |
| TBB     | `parallel_for` + `std::atomic_add`       | 4                | 0.3110   | 1.55    | 38.7%      |
| STL     | Ручные `std::thread` (8 барьеров `join`) | 2                | 0.3420   | 1.41    | 70.5%      |
| STL     | Ручные `std::thread` (8 барьеров `join`) | 4                | 0.2850   | 1.69    | 42.2%      |
| ALL     | OpenMP (Блоки) + TBB (Каскадное слияние) | 2                | 0.2520   | 1.91    | 95.5%      |
| ALL     | OpenMP (Блоки) + TBB (Каскадное слияние) | 4                | 0.1380   | 3.49    | 87.2%      |

---

## 6. Интерпретация различий и анализ архитектур

### SEQ

Базовая версия последовательно обрабатывает массив, используя локальный счетчик `count[256]` для расчета префиксных сумм
на каждом пасе. Обладает высокой локальностью данных по чтению, но упирается в пропускную способность шины памяти на
этапе перезаписи элементов во временный буфер `temp`.

### OMP

Реализация делит исходный массив на независимые крупные блоки (куски) между потоками. Каждый поток сортирует свой кусок
локально, после чего куски последовательно объединяются. Демонстрирует хорошую масштабируемость на 2 и 4 потоках
благодаря тому, что внутри вычислительного ядра потоки работают с изолированными участками кэш-памяти без межъядерной
синхронизации.

### TBB

Данный бэкенд использует мелкозернистый планировщик `tbb::parallel_for` для этапа гистограммного подсчета частот
разрядов. Потоки инкрементируют общие счетчики, обернутые в `std::atomic` с политикой `std::memory_order_relaxed`.

- Проблема падения эффективности: При росте числа потоков до 4 эффективность резко падает ($38.7\%$). Это вызвано
  эффектом Cache Bouncing (постоянная инвалидация кэш-линий процессора), поскольку множество ядер параллельно пытаются
  выполнить запись в узкий массив из 256 ячеек.

### STL

Низкоуровневый параллелизм на `std::thread` повторяет атомарную логику TBB, но управление потоками осуществляется
вручную. На каждом из 8 проходов алгоритма потоки создаются заново и синхронизируются через `th.join()`. Накладные
расходы операционной системы на частый вызов тяжелых системных объектов `std::thread` во внутреннем цикле сильно
сдерживают масштабируемость.

### ALL (OpenMP + TBB)

Наиболее эффективная комбинированная архитектура в данном исследовании (Ускорение 3.49 на 4 потоках, эффективность
87.2%).

- Схема сочетает сильные стороны технологий. На верхнем уровне OpenMP делит данные на крупные изолированные чанки
  (`omp_get_max_threads()`) и параллельно сортирует их без единой блокировки или атомика. Затем включается каскадное
  (деревовидное) параллельное слияние. Мелкозернистые алгоритмы TBB оптимизируют внутренние операции копирования на
  финальных стадиях каскада, когда крупные блоки перестают масштабироваться средствами одного OpenMP-цикла.

---

## 7. Репродуцируемость

Для полной репродукции результатов экспериментов на стороннем вычислительном узле под управлением Ubuntu Linux
необходимо выполнить следующие шаги:

### Сборка проекта

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel

```

### Запуск тестов валидации (Google Test)

```bash
./build/bin/ppc_func_tests --gtest_filter="*FrolovaSRadixSortDouble*"

```

### Нагрузочный запуск локальных многопоточных версий

```bash
# Тестирование производительности OpenMP / TBB / STL / ALL
./build/bin/ppc_perf_tests --gtest_filter="*FrolovaSRadixSortDouble*"

```

### Контроль конфигурации потоков для ALL-версии

```bash
export OMP_NUM_THREADS=4
./build/bin/ppc_perf_tests --gtest_filter="*FrolovaSRadixSortDoubleALL*"

```

---

## 8. Заключение

В рамках выполненной научно-исследовательской работы были спроектированы и детально изучены различные подходы к
параллельной реализации поразрядной сортировки чисел типа `double`.

Экспериментально доказано, что для Memory Bound задач с высокой плотностью записи (каковой является Radix Sort)
мелкозернистое распараллеливание циклов с использованием общих атомарных переменных (бэкенды TBB и STL) малоэффективно
на многоядерных архитектурах из-за деградации кэш-систем процессора (Cache Bouncing) и накладных расходов на барьеры
синхронизации.

Наилучшие результаты масштабируемости продемонстрировала гибридная схема ALL (OpenMP + TBB), основанная на принципе
макро-декомпозиции данных на изолированные чанки с последующим параллельным каскадным слиянием. Этот подход позволил
минимизировать межпоточный обмен сообщениями и утилизировать полную пропускную способность локальной памяти ядер
процессора.

---

## 9. Источники

1. Кормен, Т., Лейзерсон, Ч., Ривест, Р., Штайн, К. Алгоритмы: построение и анализ (Сортировка за линейное время).
2. Спецификация стандарта OpenMP 5.2 [Электронный ресурс]. URL: [https://www.openmp.org/](https://www.openmp.org/)
3. Справочные материалы по стандартной библиотеке шаблонов C++ (Раздел `<thread>`, `<atomic>`). URL:
   [https://cppreference.com/](https://cppreference.com/)

---

## 10. Приложение: Карта локальных отчетов

Для детального ознакомления с изолированными математическими выкладками по каждому направлению, обратитесь к
специализированным файлам отчетов в структуре репозитория:

- Последовательный базовый анализ: `tasks/frolova_s_radix_sort_double/seq/report.md`
- Макро-директивы OpenMP: `tasks/frolova_s_radix_sort_double/omp/report.md`
- Атомарные операции oneTBB: `tasks/frolova_s_radix_sort_double/tbb/report.md`
- Ручные барьеры `std::thread`: `tasks/frolova_s_radix_sort_double/stl/report.md`
- Деревовидное каскадное слияние ALL: `tasks/frolova_s_radix_sort_double/all/report.md`
