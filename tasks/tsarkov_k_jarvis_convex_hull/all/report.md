# Построение выпуклой оболочки – проход Джарвиса — ALL

- Student: Царьков Клим Александрович
- Technology: ALL
- Variant: 23

## 1. Контекст

В данной работе была реализована гибридная версия алгоритма Джарвиса
 с использованием MPI и потокового распараллеливания внутри процесса.

Гибридная схема позволяет одновременно использовать:

- межпроцессное распараллеливание;
- внутрипроцессное распараллеливание.

Такой подход применяется в задачах высокопроизводительных вычислений,
где вычисления распределяются между несколькими process rank-ами,
а внутри каждого процесса дополнительно используются потоки.

В данной реализации MPI используется для организации работы между rank-ами,
а внутри процесса применяются OpenMP, oneTBB и STL threads.

## 2. Постановка задачи

Необходимо реализовать гибридную версию алгоритма построения выпуклой оболочки множества точек на плоскости.

Полная постановка задачи, ограничения и описание входных данных приведены:

- в `report.md`;
- в `seq/report.md`.

Последовательная версия используется как baseline для проверки корректности и вычисления ускорения.

## 3. Базовый алгоритм

Последовательная версия алгоритма Джарвиса:

- находит крайнюю левую точку;
- выполняет поиск следующей вершины оболочки;
- на каждой итерации выполняет полный проход по множеству точек;
- завершает построение после возврата в стартовую точку.

Наиболее затратной частью алгоритма является поиск следующей точки оболочки.

## 4. Межпроцессная схема

### Использование MPI

Для организации межпроцессного взаимодействия используется:

```cpp
MPI_Comm_rank
```

Данный вызов определяет rank текущего процесса внутри:

```cpp
MPI_COMM_WORLD
```

Каждый process rank выполняет собственную часть вычислений.

### Роли rank-ов

В реализации используется модель с несколькими MPI process rank-ами.

Каждый rank:

- получает доступ к набору точек;
- выполняет локальные вычисления;
- участвует в синхронизации результатов.

Отдельный управляющий rank в реализации не выделяется - все rank-и выполняют одинаковую вычислительную работу.

### Распределение данных

Набор точек логически разделяется между process rank-ами.

Каждый rank обрабатывает собственную часть диапазона точек при поиске следующей вершины оболочки.

Такой подход позволяет уменьшить объём вычислений, выполняемых одним процессом.

### Используемые MPI-вызовы

В реализации используются:

- `MPI_Comm_rank`
- `MPI_Comm_size`
- `MPI_Barrier`

### MPI_Comm_rank

Используется для получения номера текущего rank-а.

### MPI_Comm_size

Используется для получения общего количества process rank-ов.

### MPI_Barrier

Используется для синхронизации процессов.

```cpp
MPI_Barrier(MPI_COMM_WORLD);
```

Барьер гарантирует, что все process rank-и завершили текущий этап вычислений перед продолжением работы.
Это необходимо для согласованности вычислений между процессами.

## 5. Внутрипроцессная схема

### Используемые thread-технологии

Внутри process rank-а используются:

- OpenMP;
- oneTBB;
- STL threads.

Каждая технология выполняет локальное распараллеливание поиска следующей точки оболочки.

### Разделение работы между потоками

Внутри rank-а диапазон точек дополнительно делится между потоками.

Каждый поток:

- обрабатывает собственный диапазон;
- вычисляет локальный лучший результат;
- не изменяет данные других потоков.

### Локальные результаты

Для каждого потока используется собственный локальный объект:

```cpp
BestPointState
```

После завершения вычислений локальные результаты объединяются внутри process rank-а.

### Объединение результатов

Сначала объединяются результаты потоков внутри процесса.
После этого выполняется синхронизация MPI process rank-ов.

## 6. Детали реализации

Используемые файлы:

- `all/include/ops_all.hpp`
- `all/src/ops_all.cpp`

### Используемые backend-ы

В ALL-версии реализованы:

- OpenMP backend;
- oneTBB backend;
- STL threads backend.

Выбор backend-а зависит от конфигурации запуска.

### Барьерная синхронизация

Синхронизация процессов выполняется через:

```cpp
MPI_Barrier(MPI_COMM_WORLD);
```

Барьер используется после завершения локальных вычислений process rank-ов.

### Потенциальные узкие места

Основными узкими местами реализации являются:

- MPI-коммуникации;
- синхронизация между rank-ами;
- запуск нескольких процессов;
- overhead потокового runtime внутри rank-а.

На небольших размерах задач стоимость синхронизации и коммуникаций
становится сопоставимой со стоимостью самих вычислений.

## 7. Проверка корректности

Корректность ALL-версии проверялась сравнением результатов:

- с SEQ-реализацией;
- с OMP, TBB и STL backend-ами.

Использовались следующие functional tests:

- одна точка;
- две точки;
- треугольник;
- квадрат с внутренними точками;
- коллинеарные точки;
- повторяющиеся точки;
- ромб с внутренними точками.

Тестирование выполнялось при:

- 2 MPI rank-ах;
- 1, 2 и 4 потоках внутри процесса.

Во всех случаях результаты совпадали с SEQ-версией.

Дополнительно проверялась согласованность результатов между process rank-ами после синхронизации.

## 8. Экспериментальная среда

### Конфигурация системы

- OS: Microsoft Windows 10 Home Single Language
- CPU: Intel Core i3-1115G4
- RAM: 8 GB
- Compiler: MSVC 19.40.33811.0
- Build type: Release
- IDE: Visual Studio 2022
- MPI: Microsoft MPI

### Переменные окружения

Количество process rank-ов задавалось через:

```powershell
mpiexec -n <N>
```

Количество потоков внутри процесса задавалось через:

```powershell
$env:PPC_NUM_THREADS=<N>
```

### Конфигурация ranks × threads

В работе использовались конфигурации:

- 1 × 1
- 2 × 1
- 2 × 2

### Команды запуска

Запуск functional tests:

```powershell
mpiexec -n 2 .\ppc_func_tests.exe --gtest_filter=*tsarkov*
```

Запуск performance tests:

```powershell
$env:PPC_NUM_THREADS=2
mpiexec -n 2 .\ppc_perf_tests.exe --gtest_filter=*tsarkov*
```

## 9. Результаты

### Pipeline mode

| Ranks | Threads per rank | Total workers | Time (s) | Speedup | Efficiency |
|-------|------------------|---------------|----------|---------|------------|
| 1     | 1                | 1             | 0.02158  | 0.36    | 0.36       |
| 2     | 1                | 2             | 0.02584  | 0.30    | 0.15       |
| 2     | 2                | 4             | 0.03034  | 0.27    | 0.07       |

### Task_run mode

| Ranks | Threads per rank | Total workers | Time (s) | Speedup | Efficiency |
|-------|------------------|---------------|----------|---------|------------|
| 1     | 1                | 1             | 0.01966  | 0.39    | 0.39       |
| 2     | 1                | 2             | 0.02481  | 0.32    | 0.16       |

Эффективность вычислялась относительно общего числа workers:

```text
workers = ranks × threads_per_rank
```

Полученные результаты показывают, что на небольших размерах задач
 гибридная схема создаёт значительные накладные расходы.

Основными причинами снижения производительности стали:

- MPI-коммуникации;
- синхронизация между process rank-ами;
- запуск нескольких процессов;
- overhead потокового runtime внутри rank-а.

Наиболее заметное влияние оказывает MPI_Barrier и стоимость синхронизации процессов.

## 10. Выводы

Гибридная ALL-реализация позволила объединить:

- MPI межпроцессное распараллеливание;
- потоковое распараллеливание внутри process rank-а.

Основными преимуществами реализации стали:

- поддержка нескольких уровней параллелизма;
- возможность масштабирования на несколько процессов;
- независимое распараллеливание внутри rank-а.

Основным ограничением производительности оказался overhead коммуникаций и синхронизации.

На используемых размерах задач стоимость MPI-коммуникаций превышала выигрыш от дополнительного распараллеливания.

Гибридная схема становится более оправданной на:

- больших наборах данных;
- большем количестве вычислений;
- многоузловых системах;
- системах с большим числом вычислительных ядер.
