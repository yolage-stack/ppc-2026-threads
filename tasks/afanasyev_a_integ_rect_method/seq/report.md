# Вычисление многомерных интегралов с использованием многошаговой схемы (метод прямоугольников) — SEQ

- **Student**: Афанасьев Александр Евгеньевич
- **Technology**: SEQ
- **Variant**: 9

## 1. Контекст

Решается задача численного вычисления трёхмерного интеграла методом прямоугольников. Последовательная
версия служит базовым эталоном для оценки ускорения при использовании параллельных технологий (OpenMP,
STL, TBB, All).

## 2. Постановка задачи

Входные данные - n — целое положительное число, количество разбиений по каждому измерению
Выходные данные - приближённое значение интеграла
Ограничения - n > 0 (проверяется в ValidationImpl), Размерность фиксирована - k_dim = 3

## 3. Базовый алгоритм

1 - Инициализация
   1.1 Получаем n из входных данных
   1.2 Вычисляем шаг сетки h = 1.0 / n
   1.3 Создаем массив индексов idx[k_dim] = {0, 0, 0}
   1.4 Создаем массив координат x[k_dim] = {0, 0, 0}
   1.5 Обнуляем сумму sum = 0.0
2 - Обход всех узлов сетки
   2.1 Для текущего набора индексов вычисляем координаты центров ячеек x[dim] = (idx[dim] + 0.5) × h
   2.2 Вычисляем значение подынтегральной функции exp(-(x^2 + y^2 + z^2))
   2.3 Значение добавляем к сумме
   2.4 Инкрементируем индексы с переносом разряда
     2.4.1 Увеличивается idx[0]
     2.4.2 Если idx[0] == n, сброс в 0 и увеличение idx[1]
     2.4.3 Если idx[1] == n, сброс в 0 и увеличение idx[2]
     2.4.4 Если idx[2] == n, цикл завершается
3 - Получение финального результата
   3.1 Объём ячейки volume = h^3
   3.2 Результат result = sum × volume

Сложност по времени - O(n^3) — прямо пропорционально количеству ячеек сетки
Сложность по памяти - O(1) - дополнительной памяти

## 4. Детали реализации

seq/include/ops_seq.hpp — объявление класса AfanasyevAIntegRectMethodSEQ
seq/src/ops_seq.cpp — реализация

ValidationImpl() - Проверяет n > 0. Возвращает false для недопустимых входных данных
PreProcessingImpl() - Не требует дополнительной подготовки, всегда true
RunImpl() - Выполняет основной алгоритм: обход сетки, суммирование, умножение на объём
PostProcessingImpl() - Не требует постобработки, всегда true

Граничные случаи: при n <= 0 валидация возвращает false, задача не выполняется; при n = 1
обрабатывается корректно (одна итерация цикла).

```cpp
std::vector<int> idx(k_dim, 0);  // массив индексов (i,j,k)
std::vector<double> x(k_dim, 0.0);
double sum = 0.0;
bool done = false;

while (!done) {
  // вычисление координат центра ячейки
  for (int dim = 0; dim < k_dim; ++dim) {
    x[dim] = (static_cast<double>(idx[dim]) + 0.5) * h;
  }

  sum += ExampleIntegrand(x);  // накопление суммы
  // ручной инкремент индексов с переносом разряда
  for (int dim = 0; dim < k_dim; ++dim) {
    idx[dim]++;
    if (idx[dim] < n) break;
    idx[dim] = 0;
    if (dim == k_dim - 1) done = true;
  }
}
```

## 5. Проверка корректности

Результат реализации SEQ принимается за эталон. Корректность подтверждается встроенными тестами курса
ppc_func_tests. Проверены крайние случаи: n = 0, n = 1, а также n = 10, n = 30 и n = 80;
отрицательные n отсекаются валидацией.

## 6. Экспериментальная среда

CPU - Apple Silicon M1 pro
RAM - 16 ГБ
OS - macOS Tahoe 26.0.1
Компилятор - Clang
Размер задачи - 8 000 000 итераций
Команды запуска:

Обычный запуск — `.\ppc_perf_tests --gtest_filter=*afanasyev_a_integ_rect_method*`

Запуск с потоками — `mpirun -n N .\ppc_perf_tests --gtest_filter=*afanasyev_a_integ_rect_method*`

(подставьте число процессов вместо N).

## 7. Результаты

Результаты измерения производительности task_run и task_pipeline. Поскольку технология использует лишь
один поток, результаты не зависят от PPC_NUM_THREADS:

Режим task_run

| Потоки | Время, с | Ускорение | Эффективность |
| --- | --- | --- | --- |
| 1 | 0.0217 | 1.00 | 100% |
| 2 | 0.0221 | 0.98 | 49.0% |
| 4 | 0.0229 | 0.95 | 23.8% |
| 6 | 0.0227 | 0.96 | 16.0% |
| 8 | 0.0231 | 0.94 | 11.8% |

Режим task_pipeline

| Потоки | Время, с | Ускорение | Эффективность |
| --- | --- | --- | --- |
| 1 | 0.0216 | 1.00 | 100% |
| 2 | 0.0221 | 0.98 | 49.0% |
| 4 | 0.0227 | 0.95 | 23.8% |
| 6 | 0.0225 | 0.96 | 16.0% |
| 8 | 0.0231 | 0.94 | 11.8% |

самый дорогой фрагмент алгоритма — вычисление подынтегральной функции ExampleIntegrand

## 8. Выводы

Последовательная версия используется как эталон для измерения ускорения параллельных реализаций
благодаря простоте, корректности и воспроизводимости; базовое время (0.0217 с) служит опорой для
оценки ускорения в многопоточных вариантах.
