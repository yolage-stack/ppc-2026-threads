# Вычисление многомерных интегралов с использованием многошаговой схемы (метод прямоугольников) — OMP

- **Student**: Афанасьев Александр Евгеньевич
- **Technology**: OMP
- **Variant**: 9

## 1. Контекст

Переносится вычисление трёхмерного интеграла методом прямоугольников из последовательной версии в OpenMP

## 2. Постановка задачи

Вычисление трёхмерного интеграла функции exp(-(x^2 + y^2 + z^2)) по единичному кубу методом прямоугольников

## 3. Базовый алгоритм (SEQ)

1 - Инициализация
   1.1 Получаем n из входных данных
   1.2 Вычисляем шаг сетки h = 1.0 / n
   1.3 Создаем массив индексов idx[k_dim] = {0, 0, 0}
   1.4 Создаем массив координат x[k_dim] = {0, 0, 0}
   1.5 Обнуляем сумму sum = 0.0
2 - Обход всех узлов сетки
   2.1 Для текущего набора индексов вычисляем координаты центров ячеек x[dim] = (idx[dim] + 0.5) × h
   2.2 Вычисляем значение подынтегральной функции exp(-(x^2 + y^2 + z^2))
   2.3 Значение добавляем к сумме
   2.4 Инкрементируем индексы с переносом разряда
     2.4.1 Увеличивается idx[0]
     2.4.2 Если idx[0] == n, сброс в 0 и увеличение idx[1]
     2.4.3 Если idx[1] == n, сброс в 0 и увеличение idx[2]
     2.4.4 Если idx[2] == n, цикл завершается
3 - Получение финального результата
   3.1 Объём ячейки volume = h^3
   3.2 Результат result = sum × volume

## 4. Схема распараллеливания (OMP)

Вся итеративная часть распараллеливается. В отличие от SEQ, в OMP используются явные вложенные циклы
for (i) for (j) for (k) для естественного распараллеливания. Для синхронизации используются
reduction(+ : sum) (приватные копии sum и финальное сложение) и collapse (тройной цикл как один
логический диапазон).

Переменные n, h, k_dim — shared; индексы i, j, k — private; sum — reduction.

## 5. Детали реализации

omp/include/ops_omp.hpp — объявление класса AfanasyevAIntegRectMethodOMP
omp/src/ops_omp.cpp — реализация

```cpp
#pragma omp parallel for collapse(3) reduction(+ : sum) default(none) shared(n, h, k_dim)
for (int i = 0; i < n; ++i) {
  for (int j = 0; j < n; ++j) {
    for (int k = 0; k < n; ++k) {
      std::vector<double> x = {(static_cast<double>(i) + 0.5) * h,
                               (static_cast<double>(j) + 0.5) * h,
                               (static_cast<double>(k) + 0.5) * h};
      sum += ExampleIntegrand(x);
    }
  }
}
```

Устранены сразу несколько рисков гонки:
1 - Гонка sum — устранена reduction(+)
2 - Гонка x — каждый поток создаёт собственный `std::vector<double> x`, гарантированно private
3 - Гонка индексов — i, j, k приватны благодаря parallel for

## 6. Проверка корректности

Результат идентичен последовательной версии включая граничные случаи

## 7. Экспериментальная среда

CPU - Apple Silicon M1 pro
RAM - 16 ГБ
OS - macOS Tahoe 26.0.1
Компилятор - Clang
Размер задачи - 8 000 000 итераций
Команды запуска:

Обычный запуск — `.\ppc_perf_tests --gtest_filter=*afanasyev_a_integ_rect_method*`

Запуск с потоками — `mpirun -n N .\ppc_perf_tests --gtest_filter=*afanasyev_a_integ_rect_method*`

(подставьте число процессов вместо N).

## 8. Результаты

Результаты измерения производительности task_run и task_pipeline для различного
количества потоков:
Режим task_run

| Потоки | Время, с | Ускорение | Эффективность |
| --- | --- | --- | --- |
| 1 | 0.0067 | 3.24 | 323.9% |
| 2 | 0.0081 | 2.67 | 133.5% |
| 4 | 0.0185 | 1.17 | 29.0% |
| 6 | 0.0278 | 0.78 | 13.0% |
| 8 | 0.0356 | 0.60 | 7.6% |

Режим task_pipeline

| Потоки | Время, с | Ускорение | Эффективность |
| --- | --- | --- | --- |
| 1 | 0.0067 | 3.24 | 323.9% |
| 2 | 0.0085 | 2.55 | 127.6% |
| 4 | 0.0173 | 1.25 | 31.3% |
| 6 | 0.0220 | 0.99 | 16.5% |
| 8 | 0.0322 | 0.67 | 8.4% |

Узкие места реализации: накладные расходы OpenMP при большом числе потоков — синхронизация и
распределение работы заметно дорожают. Создание std::vector на каждую итерацию добавляет стоимость:
при n = 200 это порядка 8 млн созданий вектора на поток.

## 9. Выводы

При малом размере задачи (n = 200, около 8 млн итераций) накладные расходы OpenMP на создание
потоков, распределение итераций и синхронизацию превышают выигрыш от параллелизации. Версия с OMP
оказалась неэффективной на 6 и 8 потоках для данного сценария.
