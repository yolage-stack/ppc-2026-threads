# Умножение плотных матриц (алгоритм Фокса) — OMP

- Student: Якимов Илья Владимирович
- Technology: OMP
- Variant: 2
- Group: 3823Б1ПР2

## 1. Контекст

В данной реализации используется технология OpenMP для распараллеливания алгоритма Фокса на общей памяти.
Цель — достичь ускорения за счет одновременного выполнения независимых этапов и умножений блоков матрицы.

## 2. Постановка задачи

Полностью совпадает с постановкой в `seq/report.md`. Последовательная версия (`SEQ`) используется как
эталон для проверки корректности и расчета ускорения.

## 3. Базовый алгоритм

Базовый алгоритм полностью совпадает с последовательной версией: используется блочный алгоритм Фокса для
квадратных матриц и тройной цикл для неквадратных.

## 4. Схема распараллеливания

Основная идея заключается в распараллеливании итераций по этапам (`stage`) и блокам строк (`i`).

- **Параллелизуемая область:** Циклы по `stage` и `i` являются независимыми, так как их перестановка не
  влияет на конечный результат. Для их распараллеливания используется директива `#pragma omp parallel for collapse(2)`.
- **Переменные:**
  - `shared(a, b, result, num_blocks, block_size)`: Все матрицы и параметры задач являются общими для
  всех потоков.
  - `private(stage, i, broadcast_block, j)`: Переменные цикла и промежуточные вычисления являются локальными
  для каждого потока.
- **Синхронизация:** Ключевым моментом является операция накопления результата в `result(...) += sum`.
  Несколько потоков могут одновременно пытаться обновить один и тот же элемент `result`. Для обеспечения
  атомарности этой операции используется `#pragma omp atomic`.
- **Выбор schedule:** Директива `collapse(2)` объединяет два цикла в один, итерации которого статически
  распределяются между потоками. Это подходит, так как вычислительная нагрузка между итерациями циклов
  `stage` и `i` примерно одинакова.
- **Барьеры:** Неявный барьер в конце `parallel for` гарантирует, что все потоки завершат все этапы,
  прежде чем функция завершится.

### Листинг ключевого фрагмента (ядро распараллеливания)

```cpp
void FoxAlgorithmImpl(...) {
  // ...
  #pragma omp parallel for collapse(2) default(none) shared(a, b, result, num_blocks, block_size)
  for (int stage = 0; stage < num_blocks; ++stage) {
    for (int i = 0; i < num_blocks; ++i) {
      int broadcast_block = (i + stage) % num_blocks;
      for (int j = 0; j < num_blocks; ++j) {
        MultiplyBlock(a, b, result, i * block_size, j * block_size, block_size,
                      broadcast_block * block_size, j * block_size);
      }
    }
  }
}

void MultiplyBlock(...) {
  // ...
  #pragma omp atomic
  result(row_start + i, col_start + j) += sum;
}```

Директива `parallel for` распределяет итерации между потоками. Директива `atomic` внутри `MultiplyBlock` гарантирует, что накопление в 
результирующую матрицу будет происходить без гонок данных.

## 5. Детали реализации

*   **Файлы:** `omp/include/ops_omp.hpp`, `omp/src/ops_omp.cpp`
*   **Класс:** `YakimovIMultOfDenseMatricesFoxAlgorithmOMP`
*   **Изменения относительно SEQ:** Основное изменение — добавление директив OpenMP для распараллеливания циклов. Логика предобработки 
(`PreProcessingImpl`) и постобработки (`PostProcessingImpl`) осталась без изменений, за исключением подбора размера блока (фиксированный 
64 для OMP, TBB, STL). `RunImpl` вызывает `FoxAlgorithmImpl` с распараллеливанием.
*   **Управление числом потоков:** Число потоков задается через переменную окружения `OMP_NUM_THREADS`, которая также устанавливается 
раннером через `PPC_NUM_THREADS`.

## 6. Проверка корректности

Корректность проверена путем сравнения результатов работы версии OpenMP с результатами последовательной версии (`SEQ`) на всех 
функциональных тестах. Расхождений не обнаружено. Дополнительно, при помощи ThreadSanitizer была подтверждена свобода от гонок данных.

## 7. Экспериментальная среда

*   **Окружение:** Аналогично `seq/report.md`.
*   **Переменные окружения:** `PPC_NUM_THREADS` (1, 2, 4, 8, 12).
*   **Команда запуска:** `scripts/run_tests.py --running-type=performance`

## 8. Результаты

Замеры производительности для размера матрицы **512 x 512** (наиболее показательный).

| Число потоков | Медианное время (сек) | Ускорение (Speedup) | Эффективность (Efficiency) |
|---------------|-----------------------|---------------------|----------------------------|
| 1 (SEQ)       | 9.482                 | 1.00                | 100%                       |
| 2             | 4.839                 | 1.96                | 98%                        |
| 4             | 2.486                 | 3.81                | 95.3%                      |
| 8             | 1.541                 | 6.15                | 76.9%                      |
| 12            | 1.470                 | 6.45                | 53.8%                      |

**Комментарий:** Наблюдается хорошее ускорение до 8 потоков. Дальнейшее увеличение числа потоков (12) дает незначительный прирост из-за 
накладных расходов на синхронизацию (директива `atomic`) и ограничений по пропускной способности памяти.

## 9. Выводы

Реализация на OpenMP демонстрирует значительное ускорение, особенно при использовании количества потоков, не превышающего число 
физических ядер (6). `collapse(2)` эффективно распределяет работу, а `atomic` предотвращает гонки данных, но вносит свой вклад в издержки 
на больших потоках. OpenMP является хорошим выбором для данной задачи благодаря простоте реализации и хорошей масштабируемости в пределах 
одного узла.
