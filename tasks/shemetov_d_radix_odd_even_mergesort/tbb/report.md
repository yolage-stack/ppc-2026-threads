# Поразрядная сортировка для целых чисел с четно-нечетным слиянием Бэтчера (TBB)

- Студент: Шеметов Даниил Олегович
- Группа: 3823Б1ПР3
- Технология: TBB
- Вариант: 18

## 1. Контекст

Технология `Intel Threading Building Blocks (TBB)` переводит алгоритм на задачно-ориентированную модель,
где диспетчеризацией пула потоков и балансировкой нагрузки `work-stealing` занимается внутренний runtime.

## 2. Схема распараллеливания

1. **Поразрядная сортировка:** Вместо ручного разбиения на множество чанков (как в OMP),
TBB-реализация использует `tbb::parallel_invoke`, параллельно запуская `RadixSort` для левой и правой половин массива.
2. **Слияние Бэтчера:** Вложенные циклы вынесены в функции `ApplyFirstPass(...)` и `ApplyMainPass(...)`.
Обе функции используют `tbb::parallel_for` поверх `tbb::blocked_range<size_t>`. Явный размер зерна `grainsize` не
задается, поэтому используется `auto_partitioner` по умолчанию. Таким образом `TBB` самостоятельно решает,
как нарезать диапазон `num_blocks` на задачи.

## 3. Детали реализации

Файлы: `tbb/include/ops_tbb.hpp`, `tbb/src/ops_tbb.cpp`

```cpp
void ShemetovDRadixOddEvenMergeSortTBB::ApplyMainPass(std::vector<int> &array, size_t start_offset, size_t segment, size_t padding) {
  size_t step = padding * 2;
  size_t num_blocks = (segment - padding) / step;

  tbb::parallel_for(tbb::blocked_range<size_t>(0, num_blocks), [&](const tbb::blocked_range<size_t> &range) {
    for (size_t block = range.begin(); block != range.end(); ++block) {
      // Логика сети Бэтчера
    }
  });
}
```

Такой подход избавляет код от жесткой привязки к количеству потоков и позволяет планировщику TBB оптимально загружать ядра.

## 4. Проверка корректности

Отсутствие гонок обеспечивается тем, что при каждом шаге `ApplyMainPass(...)` разные блоки обращаются к строго
непересекающимся индексам массива. Функциональные тесты пройдены успешно.

## 5. Экспериментальная среда

- CPU: AMD Ryzen 7 5800x
- OS: Windows 11
- Compiler: MSVC
- Build Type: Release

Задача генерируется динамически размером $N = 2^{17}$ с элементами из равномерного распределения $[-1000000, 1000000]$.

## 6. Результаты

### Режим pipeline

| Потоки | Время, с | Ускорение | Эффективность |
| --- | --- | --- | --- |
| 1 | 0.0047 | 0.83 | 83.0% |
| 2 | 0.0030 | 1.30 | 65.0% |
| 3 | 0.0026 | 1.50 | 50.0% |
| 4 | 0.0025 | 1.56 | 39.0% |

### Режим task_run

| Потоки | Время, с | Ускорение | Эффективность |
| --- | --- | --- | --- |
| 1 | 0.0042 | 0.88 | 88.0% |
| 2 | 0.0026 | 1.42 | 71.0% |
| 3 | 0.0028 | 1.32 | 44.0% |
| 4 | 0.0025 | 1.48 | 37.0% |

## 7. Выводы

`TBB` показывает наилучший результат среди всех реализаций с общей памятью. Оптимизированный планировщик `TBB`
с минимальным оверхедом управляет `blocked_range` в коротких циклах слияния Бэтчера,
что дало стабильное ускорение около `1.48x – 1.56x` по сравнению с базовой `SEQ` версией.
