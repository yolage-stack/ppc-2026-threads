# Повышение контраста полутонового изображения посредством линейной растяжки гистограммы — ALL

- Student: Отческов Семён Андреевич
- Technology: ALL
- Variant: 28

## 1. Контекст

Гибридная версия (MPI + потоки) необходима для распараллеливания задачи на кластерах с распределённой памятью,
где каждый узел может иметь несколько ядер.

- MPI обеспечивает обмен данными между независимыми процессами (узлами).
- STL (`std::jthread`) используется для внутриузлового распараллеливания на всех ядрах одного процесса.

Такая иерархическая схема позволяет обрабатывать очень большие изображения,
которые не помещаются в память одного узла, и эффективно использовать все уровни параллелизма.

## 2. Постановка задачи

Полная постановка задачи приведена в [`seq/report.md`](../seq/report.md#2-постановка-задачи).

- Вход: `std::vector<uint8_t>` (пиксели полутонового изображения) – хранится только на процессе с рангом 0.
- Выход: `std::vector<uint8_t>` той же длины – собирается на процессе 0.
- Алгоритм:
  1. Найти `min` и `max` во входном векторе.
  2. Если `min == max` – скопировать вход в выход.
  3. Иначе для каждого пикселя `p`:
     - $output[i]=\frac{(input[i] - min_I) * (255 - 0)}{max_I-min_I}$
     - Привести к `[0,255]`.
- Гибридная версия должна давать результат,
идентичный SEQ, и обеспечивать масштабируемость как по числу MPI-процессов,
так и по числу потоков внутри каждого процесса.

## 3. Базовый алгоритм

Базовый алгоритм полностью совпадает с SEQ ([см. seq/report.md](../seq/report.md#3-базовый-алгоритм)).

Основные этапы:

1. Поиск глобальных `min` и `max` во входном векторе.
2. Если `min == max` – копирование входного вектора в выходной.
3. Иначе – преобразование каждого пикселя по формуле.

## 4. Межпроцессная схема

### 4.1. Роли процессов

- **Ранг 0 (главный процесс):**
  - Хранит исходное изображение (GetInput()).
  - Выполняет MPI_Scatterv для распределения частей изображения всем процессам.
  - После обработки собирает результат через MPI_Gatherv в GetOutput().

- **Остальные процессы (ранги 1 … size-1):**
  - Получают свою локальную часть данных.
  - Обрабатывают её независимо.
  - Отправляют обработанную часть главному процессу.

### 4.2. Распределение данных

Исходное изображение разбивается на непрерывные блоки (по количеству процессов) с помощью статического равномерного разбиения:

```cpp
int base = global_size / size_;
int rem = global_size % size_;
for (int i = 0; i < size_; ++i) {
  counts[i] = base + (i < rem ? 1 : 0);
  displs[i] = (i * base) + std::min(i, rem);
}
```

- `counts[i]` – размер блока для процесса i.
- `displs[i]` – смещение в глобальном массиве.

### 4.3. Используемые MPI-вызовы

- `MPI_Bcast` – для рассылки:
  - результата валидации (is_valid_) от процесса 0 всем остальным.
  - глобального размера изображения (global_size).
- `MPI_Scatterv` – распределение исходных данных: каждый процесс получает свой блок `local_input`.
- `MPI_Allreduce` – поиск глобальных `min` и `max` по всем процессам:
  - `MPI_MIN` и `MPI_MAX` объединяют локальные результаты.
  - Результат (`global_min`, `global_max`) становится известен каждому процессу.
- `MPI_Gatherv` – сбор обработанных блоков обратно на процесс 0 в правильном порядке (с учётом смещений).

**Синхронизация:** неявно используются в коллективных вызовах.

### 4.4. Обработка однородного изображения

Если глобальный минимум равен максимуму (`global_min == global_max`),
то каждый процесс копирует свой локальный блок в local_output.
Главный процесс собирает копии. Это работает без дополнительных коммуникаций.

## 5. Внутрипроцессная схема

Внутри каждого MPI-процесса используется ручное управление потоками через `std::jthread` (полный функционал расписан в [STL-версии](../stl/report.md#4-схема-распараллеливания)).

### 5.1. Разбиение локального диапазона

Локальный вектор `local_input` размера `local_size` разбивается на `num_threads` поддиапазонов:

```cpp
const size_t num_threads = std::min<size_t>(ppc::util::GetNumThreads(), local_size);
const size_t block = local_size / num_threads;
```

- Статическое разбиение, последний поток получает остаток.
- Каждый поток работает с непрерывной областью памяти – хорошая локальность.

### 5.2. Параллельные этапы внутри процесса

#### 5.2.1. Поиск локальных min/max (`ComputeMinMax`)

- Каждый поток вычисляет min и max на своём поддиапазоне через std::ranges::minmax_element.
- Результаты сохраняются в массив local[tid].
- После завершения всех потоков (автоматический join через деструктор std::jthread)
выполняется последовательное объединение локальных результатов.
- Потоки не конкурируют за общие данные (пишут в разные ячейки).

#### 5.2.2. Параллельное копирование (`CopyInput`)

При однородном изображении каждый поток копирует свой блок из `local_input` в `local_output`.

- Используется тот же шаблон разбиения.
- Потоки не синхронизируются между собой – каждый пишет в свою часть выходного буфера.

#### 5.2.3. Параллельное преобразование (`LinearStretch`)

- Каждый поток применяет формулу растяжения к элементам своего поддиапазона и записывает результат в `local_output`.
- Все потоки читают один и тот же `local_input` (без записи) – безопасно.

### 5.3. Управление потоками и автоматический `join`

- Используется `std::jthread`, который вызывает `join()` в деструкторе.
- В `ComputeMinMax` потоки создаются внутри отдельного блока,
чтобы вектор `threads` уничтожился до объединения локальных результатов.
- В `CopyInput` и `LinearStretch` отдельный блок не нужен – деструктор вызывается при выходе из функции.

## 6. Детали реализации

### 6.1 Файлы

- `all/include/ops_all.hpp` – объявление класса `OtcheskovSContrastLinStretchALL`.
- `all/src/ops_all.cpp` – реализация.

### 6.2. Структура класса

```cpp
class OtcheskovSContrastLinStretchALL : public BaseTask {
 public:
  static constexpr ppc::task::TypeOfTask GetStaticTypeOfTask() {
    return ppc::task::TypeOfTask::kALL;
  }
  explicit OtcheskovSContrastLinStretchALL(const InType &in);

 private:
  int rank_{};
  int size_{};
  bool is_valid_{};
  struct MinMax {
    uint8_t min{255};
    uint8_t max{0};
  };

  bool ValidationImpl() override;
  bool PreProcessingImpl() override;
  bool RunImpl() override;
  bool PostProcessingImpl() override;

  static MinMax ComputeMinMax(const InType &input);
  static void CopyInput(const InType &input, OutType &output);
  static void LinearStretch(const InType &input, OutType &output, int min_i, int range);
};
```

### 6.3. Использование MPI-вызовов

Ниже приведены ключевые фрагменты кода с вызовами MPI-функций, их назначение и место в логике алгоритма.

#### 6.3.1. Валидация – `MPI_Bcast`

```cpp
if (rank_ == 0) {
  is_valid_ = !GetInput().empty();
}
MPI_Bcast(&is_valid_, 1, MPI_C_BOOL, 0, MPI_COMM_WORLD);
return is_valid_;
```

- `MPI_Bcast` рассылает флаг `is_valid_` от процесса 0 всем остальным.
- Все процессы узнают, пуст ли вход, чтобы принять единое решение о продолжении вычислений.
Без этого процессы с рангом ≠ 0 не знали бы результата валидации.

#### 6.3.2. Передача глобального размера – `MPI_BCAST`

```cpp
int global_size = 0;
if (rank_ == 0) {
  global_size = static_cast<int>(input.size());
}
MPI_Bcast(&global_size, 1, MPI_INT, 0, MPI_COMM_WORLD);
```

- Второй MPI_Bcast передаёт размер исходного изображения всем процессам.
- Каждый процесс узнаёт общий размер, чтобы вычислить свои смещения и количество элементов для Scatterv/Gatherv.

#### 6.3.3. Распределение данных – `MPI_Scatterv`

```cpp
MPI_Scatterv(rank_ == 0 ? input.data() : nullptr,
             counts.data(), displs.data(), MPI_UINT8_T,
             local_input.data(), static_cast<int>(local_size), MPI_UINT8_T,
             0, MPI_COMM_WORLD);
```

- Данный вызов позволяет передавать данные блоками разного размера и смещениями.
- Процесс 0 рассылает свои данные, остальные получают свои блоки.
- Каждый процесс получает только ту часть изображения, которую будет обрабатывать.

#### 6.3.4. Поиск глобальных min/max – `MPI_Allreduce`

```cpp
uint8_t global_min{};
uint8_t global_max{};

MPI_Allreduce(&local.min, &global_min, 1, MPI_UINT8_T, MPI_MIN, MPI_COMM_WORLD);
MPI_Allreduce(&local.max, &global_max, 1, MPI_UINT8_T, MPI_MAX, MPI_COMM_WORLD);
```

- `MPI_Allreduce` выполняет глобальную редукцию и результат рассылает всем процессам.
- `MPI_MIN` – операция взятия минимума, `MPI_MAX` – максимума.
- Каждый процесс получает глобальные минимум и максимум
для применения линейной растяжки к своему блоку.
- Без `Allreduce` пришлось бы сначала собрать значения на процессе 0,
а затем разослать их обратно.
`Allreduce` выполняет это за один шаг.

#### 6.3.5. Сбор результата – `MPI_Gatherv`

```cpp
MPI_Gatherv(local_output.data(), static_cast<int>(local_size), MPI_UINT8_T,
            rank_ == 0 ? output.data() : nullptr,
            counts.data(), displs.data(), MPI_UINT8_T,
            0, MPI_COMM_WORLD);
```

- `MPI_Gatherv` – обратная операция к `Scatterv`.
Процесс 0 собирает все локальные блоки в правильном порядке (с учётом размера блока и смещения).
- Нужен, чтобы выходное изображение было восстановлено на процессе 0 для дальнейшего использования.

### 6.4. Потенциально узкие места

- Последовательные `MPI_Allreduce` – два отдельных вызова увеличивают задержку;
можно было бы объединить `min` и `max` в одну структуру и использовать пользовательскую операцию,
но для простоты оставлено как есть.
- `MPI_Scatterv` / `MPI_Gatherv` – на больших объёмах данных могут стать узким местом.
- `STL-потоки` – статическое разбиение оптимально, но при сильном дисбалансе потребовалась бы динамическая балансировка.

## 7. Проверка корректности

Используются те же тестовые наборы,
что и для SEQ (см. [seq/report.md](../seq/report.md#5-проверка-корректности)), с применением критерия `CheckRange`:

```cpp
bool CheckRange(const OutType &data) {
  if (data.empty()) return false;
  auto [min_it, max_it] = std::ranges::minmax_element(data);
  return (*min_it == 0 && *max_it == 255) || (*min_it == *max_it);
}
```

### 7.1. Используемые тестовые наборы

- **Валидационные:** пустой вход → `ValidationImpl` возвращает `false`.
- **Функциональные:** синтетические изображения 1×1, 2×2, 3×3, 100×100, 500×500
с низкой контрастностью → `CheckRange` истинно (min=0, max=255).
- **Однородные изображения:** 10×10 и 501×501, заполненные нулями → `CheckRange` истинно (min==max).
- **Реальное изображение:** `data/grayimg.jpg` → после обработки `CheckRange` истинно.

### 7.2. Дополнительные проверки

- При `size_ = 1` и `num_threads = 1` результат идентичен SEQ.
- При любых комбинациях `ranks × threads` выходной вектор на процессе 0 одинаков – проверено сравнением для нескольких конфигураций.

### 7.3. Согласованность между процессами

- Все процессы получают одинаковые `global_min` и `global_max` через `MPI_Allreduce`.
- Каждый процесс обрабатывает свой блок независимо; сборка через `Gatherv` сохраняет порядок пикселей.

## 8. Экспериментальная среда

### 8.1. Тестовая инфраструктура

| Параметр   | Значение                                             |
| ---------- | ---------------------------------------------------- |
| CPU        | Intel Core i5 12400F (6 cores, 12 threads, 2500 MHz) |
| RAM        | 32 GB DDR4 (3200 MHz)                                |
| OS         | Windows 10 (10.0.19045)                              |
| Компилятор | MSVC 19.42.34435, Release Build                      |
| MPI        | Microsoft MPI (MS-MPI) v10.0.12498.5                 |

### 8.2. Общие настройки

- Число MPI-процессов задаётся через `PPC_NUM_PROC` (влияет на `MPI_Comm_size`).
- Число потоков на процесс задаётся через `PPC_NUM_THREADS` (используется `ppc::util::GetNumThreads()`).

#### Размер задачи

- Синтетическое изображение 10000×10000 (100 млн пикселей),
генерируемое программно со случайными значениями в диапазоне [100, 149] по-умолчанию.

#### Команда запуска (пример для 2 процессов и 4 потока)

```bash
set PPC_NUM_PROC=2
set PPC_NUM_THREADS=4
mpiexec -n 2 ./bin/ppc_perf_tests --gtest_filter="*Otcheskov*all*"
```

Аналогично для других значений процессов и потоков.

#### Режимы

- **pipeline** — запуск и измерение времени всех этапов алгоритма (`ValidationImpl -> PreProcessingImpl -> RunImpl -> PostProcessingImpl`).
- **task_run** — измерение только этапа `RunImpl`.

## 9. Результаты

- В таблице представлено время выполнения в режиме **task_run**.
- Выполнено 5 повторных запусков, приведено среднее времени выполнения.

Базовое время **SEQ**: `T_seq = 1.667792` c.

### 9.1. Таблица времени выполнения `T` (в секундах) для различных конфигураций

|  Процессов \ Потоков  |     1     |     2    |     4    |     6    |    12    |
| --------------------- | --------- | -------- | -------- | -------- | -------- |
| 1                     | 1.575896\*| 0.857683 | 0.509090 | 0.418120 | 0.335814 |
| 2                     | 0.826363  | 0.505115 | 0.374587 | 0.317000 | 0.327544 |
| 4                     | 0.508691  | 0.352931 | 0.321151 | 0.316407 | 0.293311 |
| 6                     | 0.391759  | 0.294099 | 0.301815 | 0.308289 | 0.305241 |
| 12                    | 0.299546  | 0.302253 | 0.297904 | 0.295419 | 0.295576 |

*\*Время для 1×1 получено в том же сеансе измерений, что и все гибридные конфигурации;*
*небольшое расхождение с baseline из seq/report.md связано с естественной вариативностью*

**Ускорение** (`Speedup = T_seq / T`) и
**эффективность** (`Efficiency = Speedup / (ranks * threads)`) – рассчитаны для наиболее интересных конфигураций.

- В таблице оставлены только конфигурации с `total_workers ≤ 16`.
Остальные (с 24 и более работниками) исключены по следующим причинам:

- Превышение числа логических ядер (12 на тестовой системе) приводит к oversubscription.
Это увеличивает накладные расходы на переключение контекстов
и усиливает конкуренцию за ресурсы.
- Резкое падение эффективности: ниже 30% при 24 workers,
ниже 15% при 48 и более.
Дополнительные workers почти не ускоряют выполнение,
несмотря на резкий рост числа потоков/процессов.
- Задача становится зависимой от в пропускной способности памяти, а не в вычислительные ядра.
- Накладные расходы на создание потоков и MPI-коммуникации при большом числе работников не окупаются выигрышем во времени.

Таким образом, для анализа масштабируемости достаточно рассмотреть конфигурации до 16 workers,
где ещё виден рост ускорения и приемлемая эффективность.

### 9.2. Таблица ускорения и эффективности для выбранных конфигураций

| Процессов | Потоков | Рабочих (workers) | Время, s | Ускорение | Эффективность |
| --------- | ------- | ----------------- | -------- | --------- | ------------- |
| 1         | 1       | 1                 | 1.575896 | 1.0583\*  | 105.8% \*     |
| 1         | 2       | 2                 | 0.857683 | 1.9445    | 97.2%         |
| 1         | 4       | 4                 | 0.509090 | 3.2760    | 81.9%         |
| 1         | 6       | 6                 | 0.418120 | 3.9888    | 66.5%         |
| 1         | 12      | 12                | 0.335814 | 4.9664    | 66.8%         |
| --------- | ------- | ----------------- | -------- | --------- | ------------- |
| 2         | 1       | 2                 | 0.826363 | 2.0182    | 101%          |
| 2         | 2       | 4                 | 0.505115 | 3.3018    | 82.3%         |
| 2         | 4       | 8                 | 0.374587 | 4.4523    | 55.6%         |
| 2         | 6       | 12                | 0.317000 | 5.2612    | 43.8%         |
| --------- | ------- | ----------------- | -------- | --------- | ------------- |
| 4         | 1       | 4                 | 0.508691 | 3.2786    | 81.9%         |
| 4         | 2       | 8                 | 0.352931 | 4.7255    | 59.1%         |
| 4         | 4       | 16                | 0.321151 | 5.1932    | 32.5%         |
| --------- | ------- | ----------------- | -------- | --------- | ------------- |
| 6         | 1       | 6                 | 0.391759 | 4.2572    | 70.9%         |
| 6         | 2       | 12                | 0.294099 | 5.6709    | 47.2%         |
| --------- | ------- | ----------------- | -------- | --------- | ------------- |
| 12        | 1       | 12                | 0.299546 | 5.5677    | 46.4%         |

*\*Эффективность выше 100% может быть следствием кэширования данных или погрешностей измерений.*

### 9.3. Комментарий о цене коммуникации

- Наименьшее время среди отобранных конфигураций – `0.2941 с` (`6×2 = 12 workers`).
- Лучшая эффективность – `>80%` для конфигураций с 2–4 workers.
- При переходе от 12 к 16 workers эффективность падает ниже 33%,
что указывает на начало oversubscription (логических ядер 12).
- Дальнейшее увеличение workers (более 16) не даёт значимого прироста производительности и исключено из таблицы.

## 10. Выводы

### 10.1. Гибридная схема MPI+STL оправдана когда

- Изображение очень большое и не помещается в память одного узла – распределение данных между узлами обязательно.
- Доступен кластер с быстрой сетью, где накладные расходы на `Scatterv`/`Gatherv` не доминируют.
- Необходимо использовать все ядра нескольких узлов – гибридный подход даёт лучшее масштабирование,
чем чистый MPI (когда каждый MPI-процесс загружает одно ядро),
потому что внутри узла потоки работают с общей памятью и не требуют дополнительных коммуникаций.

### 10.2. Гибридная версия неэффективна в следующих ситуациях

- На одном узле чистая STL-версия даёт лучшее время без оверхеда на передачу данных.
- При большом числе процессов и малом размере задачи
накладные расходы на коммуникацию становятся сопоставимы с вычислениями.
- При `ranks * threads` >> `физических ядер` (oversubscription) эффективность падает до неприемлемых значений.
