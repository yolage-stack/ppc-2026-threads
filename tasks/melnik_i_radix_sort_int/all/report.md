# Поразрядная сортировка целых чисел с простым слиянием — ALL

- Student: Мельник Иван Вадимович
- Technology: ALL (MPI + STL Threads)
- Variant: 17

## 1. Контекст

Гибридная версия (ALL) необходима для эффективного масштабирования задачи на кластерных архитектурах с
распределенной памятью, а внутри каждого процесса используется разделяемая память (Shared Memory) многоядерных
процессоров. Это позволяет преодолеть ограничения масштабируемости как чистого MPI, так и чистых потоков.

## 2. Постановка задачи

Реализовать многопоточную и распределенную поразрядную сортировку 32-битных знаковых целых чисел по
неубыванию. В качестве единого эталона производительности и корректности для всей иерархии используется
базовая реализация [SEQ](../seq/report.md). Сводная таблица и нормировки описаны в
[корневом отчете](../report.md).

## 3. Базовый алгоритм

Последовательная логика не изменилась: LSD Radix Sort со смещением отрицательных чисел (от младшего байта к
старшему) и простое слияние отсортированных чанков.

## 4. Межпроцессная схема

Архитектура строится на паттерне Master-Worker:

- **Роли rank-ов**: Процесс с рангом `0` (Master) берет на себя управляющую роль. Он владеет исходным
  массивом, вычисляет границы декомпозиции для всех процессов, а в конце выполняет финальное объединение
  результатов. Остальные процессы (`rank > 0`) играют роль вычислителей (Workers).
- **Распределение данных**:
    1. `rank 0` вычисляет смещения (`displacements`) и размеры чанков (`send_counts`) для каждого процесса.
    2. Эти метаданные рассылаются всем процессам с помощью `MPI_Bcast`.
    3. Исходный массив физически распределяется по процессам с помощью `MPI_Scatterv`. Каждый процесс получает
       свой `local` массив.
- **Сборка данных**:
    1. После локальной сортировки отсортированные куски собираются обратно на `rank 0` с помощью
       `MPI_Gatherv`.
    2. `rank 0` выполняет финальное глобальное слияние.
    3. Финальный отсортированный массив рассылается всем процессам через `MPI_Bcast` (так как по требованиям
       интерфейса все процессы должны завершить работу с валидным массивом).
- **Барьеры**: Явный `MPI_Barrier` в самом алгоритме отсутствует, так как коллективные операции (`Scatterv`,
  `Gatherv`, `Bcast`) содержат неявную синхронизацию внутри коммуникатора `MPI_COMM_WORLD`.

## 5. Внутрипроцессная схема

Внутри каждого ранга (как Master, так и Worker) для утилизации многоядерности используется `std::thread`.

- **Деление работы**: Локальный массив, полученный процессом из `MPI_Scatterv`, снова нарезается на
  `num_threads` независимых блоков. Каждый поток (`std::thread`) параллельно выполняет LSD Radix Sort на своем
  блоке.
- **Объединение результатов**: Отсортированные потоками блоки сливаются в один отсортированный массив уровня
  процесса с помощью параллельного иерархического слияния на базе потоков (`MergeSortedRangesParallel`),
  барьером для которого служат циклы с вызовом `join()`.

## 6. Детали реализации

Файлы: `all/include/ops_all.hpp`, `all/src/ops_all.cpp`

Потенциальные узкие места гибридной схемы:

1. **Трафик**: `MPI_Scatterv` и `MPI_Gatherv` создают большие накладные расходы MPI при пересылке всего
   массива. На сверхбольших массивах это может стать "бутылочным горлышком".
2. **Асимметрия нагрузки на финише**: В то время как локальная сортировка выполняется параллельно всеми
   рангами, финальное дерево слияний чанков, собранных через `Gatherv`, выполняется **только на `rank 0`** (в
   функции `GlobalMergeChunks`). Несмотря на то, что это слияние внутри `rank 0` распараллелено на
   `std::thread`, остальные ранги в этот момент простаивают в ожидании финального `MPI_Bcast`.

## 7. Проверка корректности

- **Сравнение**: В функциональных тестах результаты `ALL` побитово сравниваются с `SEQ`.
- **Согласованность rank-ов**: Благодаря финальному `MPI_Bcast(output.data(), ...)` гарантируется, что после
  выхода из `RunImpl` все процессы обладают идентичной, отсортированной копией массива, что валидируется на
  каждом ранге.
- Тесты стабильно проходят как на средних размерах, так и на смешанных конфигурациях процессов и потоков.

## 8. Экспериментальная среда

- **CPU**: Intel Core i5 11400F (6 cores, 6 threads (Hypethreading off), 2.5 GHz (up to 4.2))
- **RAM**: 32 GB DDR4 (3200 MHz, 2-channel)
- **OS**: Windows 10 Pro 22H2 19045.5965 + WSL2 (Ubuntu 13.3.0)
- **Compiler**: GNU 14.2.0
- **CMake build type**: Release
- **Команды запуска**:

```bash
cmake -S . -B build -D USE_FUNC_TESTS=ON -D USE_PERF_TESTS=ON -D CMAKE_BUILD_TYPE=Release
cmake --build build --parallel
export PPC_NUM_THREADS=4
mpirun -n 2 --allow-run-as-root /workspaces/ppc-2026-threads/build/bin/ppc_perf_tests --gtest_filter="*melnik_i*"
```

## 9. Результаты

В таблице представлены данные замеров на массиве размера 10'000'000. Для расчета эффективности берется общее
количество воркеров: `total_workers = ranks * threads_per_rank`. Baseline берется от однопоточного `SEQ`.

| Ranks | Threads per rank | Total Workers | Median Time (s) | Speedup | Efficiency | Notes                  |
| ----- | ---------------- | ------------- | --------------- | ------- | ---------- | ---------------------- |
| 1     | 1                | 1             | 0.1826267222    | 1.00    | 100%       | Имитация SEQ через ALL |
| 2     | 2                | 4             | 0.1296914394    | 1.40    | 35%        | -                      |
| 4     | 2                | 8             | 0.0952527348    | 1.91    | 23%        | -                      |
| 2     | 4                | 8             | 0.1376392362    | 1.32    | 16%        | -                      |

Накладные расходы на `MPI_Scatterv` / `MPI_Gatherv` и создание/джоин `std::thread` внутри процессов
значительны. Ускорение достигается только когда вычислительная сложность сортировки и системные аллокации
потоков.

## 10. Выводы

Гибридная схема (MPI + `std::thread`) оправдана при обработке колоссальных массивов данных. Накладные расходы
MPI (`Scatterv`, `Gatherv`) окупаются за счет привлечения больших вычислительных мощностей. При тестовом
запуске гибридная версия ожидаемо может проигрывать чистым thread-based реализациям (TBB, OMP) из-за накладных
расходов MPI и простоя рабочих рангов во время финального слияния на Master-процессе.
