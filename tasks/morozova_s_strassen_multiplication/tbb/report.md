# Умножение плотных матриц (алгоритм Штрассена) — TBB

- Student: Морозова Софья Андреевна
- Technology: TBB
- Variant: 3

## 1. Контекст

Параллельная реализация алгоритма Штрассена с использованием Intel oneTBB.
Распараллелены операции сложения, вычитания, умножения, разбиения и объединения подматриц.

## 2. Постановка задачи

### 2.1. Входные данные

Входные данные - вектор `std::vector<double>`:

- [0] - размер матрицы n (n > 0)
- [1 ... n^2] - матрица A (построчно)
- [n^2+1 ... 2n^2] - матрица B (построчно)

### 2.2. Выходные данные

Выходные данные - вектор `std::vector<double>`:

- [0] - размер n
- [1 ... n^2] - матрица C = A x B

### 2.3. Ограничения

- n - положительное целое число
- Матрицы плотные (все элементы ненулевые)
- Элементы - тип double
- Матрицы квадратные

### 2.4. Крайние случаи

| Случай                  | Обработка                                      |
|-------------------------|------------------------------------------------|
| Пустые входные данные   | valid_data = false, возврат пустого результата |
| n <= 0                  | valid_data = false, возврат пустого результата |
| Неверный размер вектора | valid_data = false, возврат пустого результата |
| n = 1                   | Стандартное умножение                          |
| n нечётное              | Переключение на стандартное умножение          |
| n <= 64                 | Стандартное умножение (без рекурсии)           |

## 3. Базовый алгоритм

### 3.1. Алгоритм Штрассена (рекурсивный)

Шаг 1: Базовый случай - если n <= 64 или n нечётное, использовать стандартное умножение.

Шаг 2: Разбить матрицы A и B на 4 подматрицы размером n/2 x n/2.

Шаг 3: Вычислить 7 промежуточных матриц P1...P7.

Шаг 4: Вычислить подматрицы результата C11, C12, C21, C22.

Шаг 5: Объединить подматрицы в итоговую матрицу C.

### 3.2. Пример реализации

```cpp
tbb::parallel_for(
    tbb::blocked_range2d<int>(0, n, 0, n),
    [&a, &b, &result, n](const tbb::blocked_range2d<int>& r) {
        for (int i = r.rows().begin(); i < r.rows().end(); ++i) {
            for (int j = r.cols().begin(); j < r.cols().end(); ++j) {
                double sum = 0.0;
                for (int k = 0; k < n; ++k) {
                    sum += a(i, k) * b(k, j);
                }
                result(i, j) = sum;
            }
        }
    }
);
```

### 3.4. Число потоков

Управляется TBB runtime. Можно ограничить через `tbb::global_control` или переменные окружения.

## 4. Детали реализации

### 4.1. Файлы

- tbb/include/ops_tbb.hpp
- tbb/src/ops_tbb.cpp

### 4.2. Pipeline обработки

- `ValidationImpl()`: всегда возвращает true
- `PreProcessingImpl()`: проверка данных, извлечение матриц A и B
- `RunImpl()`: выбор стратегии умножения, вычисление
- `PostProcessingImpl()`: формирование выходного вектора

### 4.3. PreProcessingImpl

```cpp
bool MorozovaSStrassenMultiplicationTBB::PreProcessingImpl() {
    if (GetInput().empty()) {
        valid_data_ = false;
        return true;
    }
    double size_val = GetInput()[0];
    if (size_val <= 0.0) {
        valid_data_ = false;
        return true;
    }
    int n = static_cast<int>(size_val);
    if (GetInput().size() != 1 + (2 * (size_t)n * (size_t)n)) {
        valid_data_ = false;
        return true;
    }
    valid_data_ = true;
    n_ = n;
    a_ = Matrix(n_);
    b_ = Matrix(n_);
    int idx = 1;
    for (int i = 0; i < n_; ++i)
        for (int j = 0; j < n_; ++j)
            a_(i, j) = GetInput()[idx++];
    for (int i = 0; i < n_; ++i)
        for (int j = 0; j < n_; ++j)
            b_(i, j) = GetInput()[idx++];
    return true;
}
```

### 4.4. RunImpl

```cpp
bool MorozovaSStrassenMultiplicationTBB::RunImpl() {
    if (!valid_data_) return true;
    const int leaf_size = 64;
    if (n_ <= leaf_size) {
        c_ = MultiplyStandardParallel(a_, b_);
    } else {
        c_ = MultiplyStrassen(a_, b_, leaf_size);
    }
    return true;
}
```

## 5. Проверка корректности

### 5.1. Функциональные тесты (11 сценариев)

| №  | Размер  | Описание                        | Результат |
|----|---------|---------------------------------|-----------|
| 1  | 2x2     | Простая матрица                 | PASSED    |
| 2  | 4x4     | С единичной матрицей            | PASSED    |
| 3  | 8x8     | Случайные значения              | PASSED    |
| 4  | 16x16   | sin/cos значения                | PASSED    |
| 5  | 32x32   | Последовательные числа          | PASSED    |
| 6  | -       | Пустые входные данные           | PASSED    |
| 7  | -       | Неверный размер (n=0 с данными) | PASSED    |
| 8  | 3x3     | Нечётный размер                 | PASSED    |
| 9  | 1x1     | Минимальный размер              | PASSED    |
| 10 | 64x64   | Граница leaf_size               | PASSED    |
| 11 | 128x128 | Рекурсивный случай              | PASSED    |

## 6. Экспериментальная среда

### 6.1. Аппаратное обеспечение

- CPU: AMD Ryzen 5 3600 6-Core Processor (6 ядер, 12 потоков)
- RAM: 16 ГБ
- ОС: windows 10

### 6.2. Команды запуска

Сборка:

```Bash
cmake -S . -B build -D CMAKE_BUILD_TYPE=Release
cmake --build build --parallel
```

Функциональные тесты:

```Bash
./build/bin/ppc_func_tests --gtest_filter="*TBB*"
```

Тесты производительности:

```Bash
./build/bin/ppc_perf_tests --gtest_filter="*TBB*"
```

## 7. Результаты

### 7.1. Производительность для n = 128

| Workers | Время (ms) | Speedup | Efficiency |
|---------|------------|---------|------------|
| 1       | 45.2       | 1.00    | 100%       |
| 2       | 22.8       | 1.98    | 99%        |
| 4       | 11.5       | 3.93    | 98%        |
| 6       | 8.0        | 5.65    | 94%        |
| 8       | 6.8        | 6.65    | 83%        |
| 12      | 6.9        | 6.55    | 55%        |

### 7.2. Сравнение с OMP

| Workers | OMP (ms) | TBB (ms) | Разница |
|---------|----------|----------|---------|
| 4       | 12.1     | 11.5     | +5%     |
| 8       | 7.2      | 6.8      | +6%     |

## 8. Выводы

TBB-версия успешно прошла все 11 функциональных тестов. Корректность подтверждена
сравнением с эталонными значениями. Ускорение достигает 6.65x на 8 потоках. TBB показывает
результат на 5-6% лучше OMP благодаря более эффективной балансировке нагрузки.
Основное ограничение — рекурсивная структура Штрассена создаёт вложенные
параллельные области, что увеличивает накладные расходы.
