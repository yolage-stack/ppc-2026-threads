# Умножение разреженных матриц. Элементы типа double. Формат хранения матрицы – строковый (CRS) — OMP

- Студент: Маслова Ульяна Александровна
- Технология: OMP
- Вариант: 4

## 1. Контекст

Умножение разреженных матриц характеризуется неравномерной вычислительной нагрузкой на разные строки.
Технология OpenMP позволяет распределить обработку строк между ядрами процессора, используя динамическую балансировку.

## 2. Постановка задачи

Параллельная реализация алгоритма умножения матриц A и B в формате CRS с использованием директив OpenMP.
Основная цель — сокращение времени выполнения на многоядерных системах.
Сравнение проводится с [SEQ](../seq/report.md).

## 3. Базовый алгоритм

Используется двухпроходная схема для исключения состояния гонки (race condition) и избыточных аллокаций:

1. **Символический проход:** Вычисление количества ненулевых элементов (NNZ) для каждой строки результата.
2. **Вычислительный проход:** Непосредственное вычисление значений и заполнение заранее выделенных массивов.

## 4. Схема распараллеливания

Параллелизм реализован на уровне строк матрицы A:

- **Директива:** `#pragma omp parallel`.
- **Область данных:**
  - `shared`: исходные матрицы A, B и результирующая C.
  - `private`: для каждого потока создаются свои локальные буферы `marker`, `acc` и `used`.
- **Планировщик:** `schedule(dynamic, 20)`. Выбран динамический тип, так как строки имеют разное количество элементов,
что приводит к дисбалансу при статическом делении.

```cpp
// File: omp/src/ops_omp.cpp
#pragma omp parallel default(none) shared(a, b, c, rows_a, cols_b) num_threads(ppc::util::GetNumThreads())
{
  std::vector<double> acc(cols_b, 0.0);
  std::vector<int> marker(cols_b, -1);
  std::vector<int> used;
  used.reserve(cols_b);

#pragma omp for schedule(dynamic, 20)
  for (int i = 0; i < rows_a; ++i) {
    FillRowValues(i, a, b, c, acc, marker, used);
  }
}
```

## 5. Детали реализации

- **Файлы:** `omp/include/ops_omp.hpp`, `omp/src/ops_omp.cpp`.
- **Синхронизация:** Благодаря двухпроходному алгоритму потоки пишут в разные участки массивов `values` и `col_ind`,
что исключает необходимость в `critical` секциях или `mutex`.
- **Память:** Результирующие векторы `resize`-ятся один раз между проходами.

## 6. Проверка корректности

Корректность подтверждена прохождением 5 функциональных тестов.
Результат OMP-версии полностью совпадает с SEQ-версией на всех наборах данных.

## 7. Экспериментальная среда

- **Инструментарий:** OpenMP.
- **Конфигурация:** Переменная `PPC_NUM_THREADS` принимала значения 1, 2, 4, 8.
- **Сборка:** Release.

## 8. Результаты

Замеры на матрицах 1000x1000, плотность 0.05 (Baseline SEQ = 0.1234s).

| Потоков (t) | Время (сек) | Ускорение (S) | Эффективность (E) |
| :--- | :--- | :--- | :--- |
| 1 | 0.1473 | 0.84 | 84.0% |
| 2 | 0.0861 | 1.43 | 71.5% |
| 4 | 0.0459 | 2.69 | 67.2% |
| 8 | 0.0292 | 4.22 | 52.7% |

*Ускорение S = T_seq / T_p, Эффективность E = S / t.*

## 9. Выводы

Применение OpenMP позволило ускорить умножение матриц в 4.22 раза на 8 потоках.
Небольшое замедление на 1 потоке (0.147с против 0.123с в SEQ) объясняется
накладными расходами на управление параллельной областью и двухпроходным алгоритмом.
Динамическое планирование эффективно справилось со спецификой разреженных данных.
