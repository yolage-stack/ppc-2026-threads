# Умножение разреженных матриц. Элементы типа double. Формат хранения матрицы – строковый (CRS) — STL

- Студент: Маслова Ульяна Александровна
- Технология: STL
- Вариант: 4

## 1. Контекст

Использование стандартной библиотеки потоков C++ (`std::thread`) позволяет реализовать параллелизм
без зависимости от внешних библиотек (OpenMP, TBB).
Эта версия демонстрирует навыки ручного управления жизненным циклом потоков и явного разделения данных между ними.

## 2. Постановка задачи

Реализовать параллельное умножение матриц в формате CRS, используя `std::thread`.
Проверить масштабируемость алгоритма при увеличении числа потоков и сравнить результаты
с последовательным эталоном [SEQ](../seq/report.md).

## 3. Базовый алгоритм

Для обеспечения потокобезопасности без использования блокировок (mutex) применен двухпроходный алгоритм:

1. **Первый проход (Символический):** Потоки параллельно вычисляют количество ненулевых
элементов в отведенных им строках. Результаты заносятся в локальные инкременты `row_ptr`.
2. **Префиксная сумма:** В основном потоке выполняется последовательный расчет указателей
строк для формирования структуры матрицы.
3. **Второй проход (Вычислительный):** Потоки вычисляют сами значения элементов и индексы столбцов,
записывая их в заранее выделенные участки общей памяти.

## 4. Схема распараллеливания

- **Разбиение:** Матрица A делится на блоки строк (chunks). Каждый блок обрабатывается отдельным потоком.
Границы вычисляются как `start = t * chunk` и `end = (t + 1) * chunk`.
- **Управление потоками:** Создается вектор `std::vector<std::thread>`. Потоки запускаются через лямбда-функции.
- **Синхронизация:** Основной поток ожидает завершения всех рабочих потоков с помощью вызова `.join()` после каждого прохода.
- **Локальные данные:** Каждый поток владеет собственными буферами `marker`, `acc` и `used`, что исключает гонки данных.

## 5. Детали реализации

- **Файлы:** `stl/include/ops_stl.hpp`, `stl/src/ops_stl.cpp`.
- **Вспомогательные методы:** `ComputeRowPtrSTL` (запуск 1-го прохода) и `ComputeValuesSTL` (запуск 2-го прохода).
- **Особенности:** Для снижения когнитивной сложности логика запуска потоков вынесена в отдельные статические методы класса.

```cpp
// File: stl/src/ops_stl.cpp
void MaslovaUMultMatrSTL::ComputeValuesSTL(int rows_a, int cols_b, int num_threads) {
  std::vector<std::thread> threads;
  int chunk = rows_a / num_threads;
  for (int thread_idx = 0; thread_idx < num_threads; ++thread_idx) {
    int start = thread_idx * chunk;
    int end = (thread_idx == num_threads - 1) ? rows_a : (thread_idx + 1) * chunk;
    if (start < end) {
      threads.emplace_back([&, start, end, cols_b]() {
        std::vector<double> acc(cols_b, 0.0);
        std::vector<int> marker(cols_b, -1);
        std::vector<int> used;
        for (int i = start; i < end; ++i) {
          FillRowValues(i, matrix_a, matrix_b, matrix_c, acc, marker, used);
        }
      });
    }
  }
  for (auto &thread_item : threads) thread_item.join();
}
```

## 6. Проверка корректности

Корректность реализации подтверждена набором функциональных тестов (плотные, треугольные матрицы, векторы).
Результаты работы STL-версии побитово совпадают с последовательной реализацией.

## 7. Экспериментальная среда

- **Инструментарий:** `std::thread`.
- **Конфигурация:** Тестирование проводилось на 1, 2, 4 и 8 потоках.
- **Сборка:** Release.

## 8. Результаты

Замеры на матрицах 1000x1000, плотность 0.05 (Baseline SEQ = 0.1234s).

| Потоков (t) | Время (сек) | Ускорение (S) | Эффективность (E) |
| :--- | :--- | :--- | :--- |
| 1 | 0.1445 | 0.85 | 85.3% |
| 2 | 0.0741 | 1.66 | 83.2% |
| 4 | 0.0590 | 2.09 | 52.3% |
| 8 | 0.0583 | 2.12 | 26.5% |

## 9. Выводы

Версия на `std::thread` показала стабильное ускорение до 4 потоков.
При переходе к 8 потокам время практически перестало уменьшаться (ускорение 2.12x),
что объясняется значительными накладными расходами на создание и уничтожение потоков.
