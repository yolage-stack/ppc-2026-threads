# Cортировка Хоара с простым слиянием — STL

- Студент: Розенберг Алексей Светославович
- Технология: STL
- Вариант: 13

## 1. Контекст

STL-версия реализует ту же декомпозицию "сортировка чанков параллельно — слияние последовательно", что и OMP и TBB, но
без каких-либо сторонних библиотек параллелизма: каждый поток создаётся явно через `std::thread`, а синхронизация
сводится к `join`. Это делает схему максимально прозрачной: нет планировщика задач, нет неявных барьеров, нет директив
компилятора — только явное создание потоков, явное ожидание их завершения и явная связка потоков с диапазонами данных.

## 2. Постановка задачи

**Краткая постановка задачи:** необходимо отсортировать массив целых чисел в порядке неубывания.

Baseline для сравнения — [`seq/report.md`](../seq/report.md) c временем выполнения $T_{\text{seq}}$ при $p = 1$ на том же
наборе данных и в том же окружении.

## 3. Базовый алгоритм

Последовательная основа — итеративный алгоритм сортировки Хоара, включающий в себя выбор опорного элемента из середины,
разбиение массива относительно опорного элемента и итеративную сортировку каждого подмассива с явным стеком подмассивов.

## 4. Схема распараллеливания

**Ручное разбиение диапазонов.** Массив длины $N$ делится на $p$ чанков: размер каждого равен $⌊\frac{N}{p}⌋$, последний
чанк поглощает остаток $N \mod p$. Чанк $i$ охватывает индексы $[i \cdot ⌊\frac{N}{p}⌋, (i+1) \cdot ⌊\frac{N}{p}⌋ − 1]$.
Границы диапазонов вычисляются заранее и фиксируются отдельно в памяти до запуска потоков. Каждый поток получает ровно
один чанк — явная, статическая карта "поток $-$ блок данных".

**Связка потоков с блоками данных.** Поток `i` создаётся с лямбдой, захватывающей `data`, `borders` и значение `i` по
значению. Лямбда вызывает метод сортировки на диапазоне `[borders[i], borders[i+1]-1]`. Таким образом, каждый поток
однозначно знает свой диапазон до начала выполнения, и этот диапазон не меняется в процессе работы — балансировки
нагрузки нет.

**Локальные буферы.** Локальный стек разбиений создаётся внутри метода сортировки на стеке каждого потока и не
разделяется между потоками. Временный буфер в методе слияния также локален. Таким образом, никакие промежуточные
структуры не являются разделяемыми.

**`join` и синхронизация.** После запуска всех $p$ потоков главный поток последовательно вызывает `t.join()` для каждого
из них. `join` блокирует вызывающий поток до завершения соответствующего рабочего потока. Только после того, как все
`join` завершены, начинается фаза последовательного слияния. Гонок нет: каждый поток пишет только в свой диапазон
`[borders[i], borders[i+1]−1]`, `borders` только читается. `atomic` и `mutex` не требуются.

**Деградация при малых данных.** Если $N \lt 2p$, чанки вырождаются. В этом случае весь массив сортируется однопоточно
без создания `std::thread`.

**Ограничение ускорения.** Последовательное слияние $p$ чанков стоит $O(N \cdot p)$ и является последовательной частью.
Дополнительно, отсутствие балансировки нагрузки (статическое разбиение, отсутствует механизм "work stealing") означает,
что при неравномерной стоимости сортировки чанков быстрые потоки простаивают, ожидая медленных у `join`.

## 5. Детали реализации

**Файлы:** [`stl/include/ops_stl.hpp`](include/ops_stl.hpp), [`stl/src/ops_stl.cpp`](src/ops_stl.cpp)

**Изменения относительно SEQ.**

- [`Quicksort`](src/ops_stl.cpp#L80): расширен параметрами `low`, `high`, что позволяет запускать метод на произвольном
диапазоне.
- Добавлен метод [`Merge(data, left, mid, right)`](src/ops_stl.cpp#L100) $-$ стандартное слияние двух упорядоченных
подмассивов `[left..mid]` и `[mid+1..right]` через временный буфер размером `right − left + 1` с обратной записью
результата в `data[left..right]`.
- В [`RunImpl`](src/ops_stl.cpp#L126) добавлены: определение числа потоков через `ppc::util::GetNumThreads()`, вектор
`threads`, вектор границ `borders`, цикл запуска потоков, цикл `join` и цикл последовательного слияния.

**Запуск потоков:**

```cpp
// File: stl/src/ops_stl.cpp
for (int i = 0; i < num_threads; i++) {
  threads.emplace_back([&data, &borders, i]() {
    rozenberg_a_quicksort_simple_merge::RozenbergAQuicksortSimpleMergeSTL::Quicksort(
        data, borders[i], borders[i + 1] - 1);
  });
}
```

> Примечание: Лямбда захватывает `data` и `borders` по ссылке, а `i` — по значению. Захват `i` по значению принципиален:
> если бы `i` захватывался по ссылке, к моменту выполнения лямбды значение `i` в главном потоке уже изменилось бы.
> `data` захвачен по ссылке — все потоки работают с одним вектором, но в непересекающихся диапазонах. `borders` захвачен
> по ссылке и используется только для чтения.

**Ожидание завершения:**

```cpp
// File: stl/src/ops_stl.cpp
for (auto &t : threads) {
  if (t.joinable()) {
    t.join();
  }
}
```

> Примечание: Проверка `joinable()` перед `join()` защищает от двойного join на уже завершённом потоке. После этого цикла
> все чанки гарантированно отсортированы, и главный поток приступает к слиянию.

**Определение числа потоков:**

```cpp
// File: stl/src/ops_stl.cpp
int num_threads = ppc::util::GetNumThreads();
```

> Примечание: `GetNumThreads()` читает переменную окружения `PPC_NUM_THREADS`, обеспечивая единое управление числом
> потоков для всех backend-ов в инфраструктуре курса.

**Последовательное слияние:**

```cpp
// File: stl/src/ops_stl.cpp
for (int i = 1; i < num_threads; i++) {
  Merge(data, 0, borders[i] - 1, borders[i + 1] - 1);
}
```

> Примечание: На каждой итерации `i` накопленный отсортированный префикс `[0, borders[i] − 1]` сливается с чанком
> `[borders[i], borders[i+1] − 1]`. После `p − 1` слияний весь массив отсортирован.

**Устранённые риски гонок.** Каждый поток пишет только в диапазон `[borders[i], borders[i+1]−1]` — диапазоны
непересекаются. `borders` только читается внутри потоков. Локальный стек `Quicksort` и буфер `Merge` создаются на стеке
каждого потока и не разделяются. `atomic` и `mutex` отсутствуют намеренно — они не нужны при данной структуре доступа к
данным.

## 6. Проверка корректности

Корректность проверяется аналогично [SEQ-версии](../seq/report.md#5-проверка-корректности). Дополнительно проверяется
устойчивость результата при различном числе потоков и отсутствие гонок  — результаты должны совпадать между собой и с SEQ.

## 7. Экспериментальная среда

| Параметр | Значение |
| --- | --- |
| CPU | AMD Ryzen 7 7800X3D, 8 ядер, 16 потоков |
| RAM | DDR5 32 GB, 6000 MHz |
| ОС | Windows 11 Home, 25H2 |
| Compiler | MSVC v143 |
| CMake build type | Release |
| PPC_NUM_THREADS | 1 / 2 / 4 / 8 / 16 |

**Команды запуска:**

```bash
# Задать число потоков
$env:PPC_NUM_THREADS=<число потоков>

# Функциональные тесты
.\build\bin\ppc_func_tests.exe --gtest_filter="*Rozenberg*stl*"

# Тесты производительности
.\build\bin\ppc_perf_tests.exe --gtest_filter="*Rozenberg*stl*"
```

## 8. Результаты

### Режим pipeline

**Эталонное время:** $T_{\text{seq}}^{\text{pipeline}} = 0.68213172$ с

| Потоки (p) | Время, с | Ускорение (speedup) | Эффективность (efficiency) |
| --- | --- | --- | --- |
| 1 | 0.67833316 | 1.01 | 100.6% |
| 2 | 0.37369408 | 1.83 | 91.3% |
| 4 | 0.2538207 | 2.69 | 67.2% |
| 8 | 0.2137592 | 3.19 | 39.9% |
| 16 | 0.2461356 | 2.77 | 17.3% |

### Режим task

**Эталонное время:** $T_{\text{seq}}^{\text{task}} = 0.677372$ с

| Потоки (p) | Время, с | Ускорение (speedup) | Эффективность (efficiency) |
| --- | --- | --- | --- |
| 1 | 0.67590966 | 1.00 | 100.2% |
| 2 | 0.37333902 | 1.81 | 90.7% |
| 4 | 0.24529156 | 2.76 | 69% |
| 8 | 0.2090206 | 3.24 | 40.5% |
| 16 | 0.24428314 | 2.77 | 17.3% |

**Накладные расходы на создание и синхронизацию потоков:** Так как на каждую итерацию параллельной
фазы операционная система вынуждена
выполнять полноценную процедуру порождения потока, при переходе к $p=16$ потокам
затраты времени ОС на переключение контекста, системные вызовы создания и уничтожения 16 независимых
потоков начинают превосходить полезное время работы самого алгоритма над уменьшающимися чанками данных.
Это наглядно иллюстрируется деградацией времени выполнения падением ускорения до 2.77 по сравнению с ускорением
при $p=8$ (3.24).

**Балансировка нагрузки:** Из-за отсутствия встроенного планировщика, готовые потоки не могут "украсть"
работу у отстающих. В итоге время параллельной фазы определяется самым "медленным" чанком. Накладные расходы `join`
усугубляют эту проблему: мастер-поток может простаивать на первом же вызове `.join()`, ожидая завершения застрявшего
потока, даже если остальные уже успешно выполнили свою задачу.

## 9. Выводы

STL при $p=8$ показывает наилучшее ускорение среди
поточных backend-ов (3.19–3.24 task/pipeline), незначительно превосходя TBB.
При $p=16$ STL (2.77) лучше OMP (2.27–2.28), что может объясняться меньшими
накладными расходами на управление потоками в MSVC по сравнению с MSVC
OpenMP runtime. При этом для ручной реализации характерно отсутствие
переиспользования потоков: при каждом вызове `RunImpl` создаётся и
уничтожается $p$ потоков, что даёт overhead при многократных запусках.
