# Линейная фильтрация изображений (горизонтальное разбиение). Ядро Гаусса 3x3 — TBB

- Student: Бузулукский Данила Сергеевич
- Technology: TBB
- Variant: 24

## 1. Контекст

Параллельная версия с использованием библиотеки Intel oneAPI Threading Building Blocks (oneTBB) разработана для
ускорения базового алгоритма линейной
фильтрации за счет применения продвинутого планировщика задач (Task Scheduler) и концепции абстракции потоков. В
отличие от сырых потоков или жесткого
секционирования циклов, TBB оперирует понятием диапазонов и задач, что позволяет эффективно балансировать нагрузку
(Work Stealing) на многоядерных процессорах со
сложной кэш-архитектурой.

## 2. Постановка задачи

Краткая постановка полностью совпадает с seq/report.md. Выходные данные TBB-версии проходят обязательное
попиксельное сравнение с последовательным
baseline-ядром (SEQ, T_1 = 2.660470 сек), что гарантирует абсолютную корректность и детерминированность результатов
фильтрации.

## 3. Базовый алгоритм

Последовательная версия обходит матрицу пикселей во вложенных циклах, вычисляет значение свёртки матрицы 3x3 по
всем трем каналам RGB с clamp-обработкой
крайних пикселей окрестности и записывает результат в выходной буфер кадра.

## 4. Схема распараллеливания

Распараллеливание вычислительного ядра реализовано с помощью алгоритма tbb::parallel_for.

- Примитив: Выбран tbb::parallel_for, так как итерации по строкам изображения полностью независимы друг от друга и
не
содержат перекрестных зависимостей по данным.
- Диапазон: Используется класс ``tbb::blocked_range<int>(0, height_)``, который адаптивно разбивает вертикальный
размер кадра на
независимые поддиапазоны.
- Размер чанка (grainsize): Специально оставлен по умолчанию. Это указывает рантэйму TBB применять рекурсивное
деление диапазона
до тех пор, пока накладные расходы на диспетчеризацию задач не начнут превышать полезную вычислительную работу.
Такой подход защищает
систему от избыточного overhead на мелких задачах.
- Разбиение (Partitioner): Применяется дефолтный tbb::auto_partitioner, который автоматически подбирает оптимальный
размер поддиапазонов
строк в зависимости от текущей аппаратной загрузки CPU воркерами.
- Конкуренция: Количество активных рабочих потоков динамически регулируется фреймворком курса через глобальные
настройки рантэйма, контролирующие
выполнение пула задач внутри фабрики MakeAllPerfTasks посредством механизмов tbb::global_control.

### Листинг ключевого фрагмента (ядро распараллеливания)

```cpp
bool BuzulukskiDGausGorizontalTBB::RunImpl() {
  const int h = height_;
  const int w = width_;
  const uint8_t *in_ptr = input_image_.data();
  uint8_t *out_ptr = output_image_.data();

  tbb::parallel_for(tbb::blocked_range<int>(0, h), [&](const tbb::blocked_range<int> &r) {
    for (int py = r.begin(); py < r.end(); ++py) {
      for (int px = 0; px < w; ++px) {
        for (int ch = 0; ch < kChannels; ++ch) {
          size_t out_idx = (((static_cast<size_t>(py) * static_cast<size_t>(w)) + static_cast<size_t>(px)) * kChannels) + static_cast<size_t>(ch);
          out_ptr[out_idx] = CalculatePixelTBB(in_ptr, py, px, w, h, ch);
        }
      }
    }
  });
  return true;
}
```

## 5. Детали реализации

Файлы: tbb/include/ops_tbb.hpp, tbb/src/ops_tbb.cpp

В реализации строго разграничена логика инфраструктурных этапов фреймворка. Метод RunImpl содержит параллельное
ядро TBB. Использование захвата по
ссылке `[&]` внутри лямбда-функции в `tbb::parallel_for` обеспечивает разделяемый доступ потоков-воркеров к общим
неизменяемым структурам и указателям данных (`in_ptr`, `out_ptr`, `w`). При этом все
локальные переменные, объявляемые непосредственно внутри тела вложенных циклов (`py`, `px`, `ch`, `out_idx`),
автоматически размещаются в индивидуальном стеке каждого конкретного потока-воркера.
Это гарантирует полную потокобезопасность (thread-safety) без использования ресурсоёмких блокировок или атомарных
операций. Потоки работают со своими изолированными поддиапазонами строк общего
 массива `output_image_` (благодаря интервалам `r.begin()` и `r.end()`), что минимизирует риски ложного разделения
 кэша (false sharing) на стыках обрабатываемых блоков. В работе используется
 автоматический планировщик `tbb::auto_partitioner` и дефолтный расчет `grainsize`, что позволяет рантайму TBB
 самостоятельно адаптировать размер подблоков строк под текущую нагрузку
 процессора.

## 6. Проверка корректности

Для верификации TBB-версии разработан и запущен набор функциональных тестов на базе фреймворка Google Test (тесты
tbb_size_3 и tbb_size_10). Тесты проверяют поведение системы
на масштабах данных 3x3 и 10x10, а также равенство результатов с SEQ. Попиксельное сравнение подтверждает полную
корректность работы распределения задач TBB.

## 7. Экспериментальная среда

- Ограничение конкуренции воркеров задается рантэймом через переменную окружения PPC_NUM_THREADS (1, 4, 6, 12).
- Сборка: GCC 13.2.0, Build Type: Release (-O3).
- Команда запуска: ./build/bin/ppc_perf_tests.exe --gtest_filter=*Buzulukski*tbb*
- Размер задачи: Фиксированный масштабируемый параметр кадра N = 1500 в rámci тестов производительности.

## 8. Результаты

Замеры производительности вычислительного ядра (task_run) на различных конфигурациях активных воркеров рантэйма:

| Число воркеров | Время выполнения (сек) | Ускорение (Speedup) | Эффективность (Efficiency) |
| :---: | :---: | :---: | :---: |
| 1 (SEQ Baseline) | 2.660470 | 1.00 | 100.0% |
| 4 | 0.750172 | 3.55 | 88.7% |
| 6 | 0.614978 | 4.32 | 72.1% |
| 12 | 0.400395 | 6.64 | 55.3% |

Метрики рассчитываются по стандартным формулам: Ускорение S = T_1 / T_p, Эффективность E = S / p.

Комментарий: Intel oneTBB показывает стабильное масштабирование производительности на независимых итерациях строк
изображения.
Использование встроенного планировщика auto_partitioner позволяет эффективно распределять нагрузку без явного
указания размера чанка.
Точка перегиба эффективности наблюдается на конфигурации из 6 воркеров, что соответствует количеству физических
вычислических ядер процессора
AMD Ryzen 5 5600H. Дальнейший рост до 12 воркеров задействует логические потоки Hyper-Threading и демонстрирует
падение удельной эффективности из-за
конкуренции за кэш-память.

## 9. Выводы

Реализация линейной фильтрации Гаусса на базе Intel oneTBB полностью корректна, сохраняет строгую
детерминированность результатов и демонстрирует высокую
вычислительную эффективность. Задачно-ориентированная абстракция TBB позволила избавиться от явных примитивов
синхронизации и исключить риски возникновения
гонок данных за счет полной и независимой изоляции обрабатываемых поддиапазонов строк в памяти. Автоматический
балансировщик нагрузки эффективно нивелирует накладные
расходы рантэйма.
