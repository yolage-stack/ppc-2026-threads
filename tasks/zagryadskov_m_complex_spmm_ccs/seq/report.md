# Умножение разреженных матриц. Элементы комплексного типа. Формат хранения матрицы – столбцовый (CСS)

- Студент: Загрядсков М.А.  
- Группа: 3823Б1ПМоп3
- Технология: SEQ  
- Вариант: 7  

---

## 1. Контекст

Рассматривается задача умножения двух разреженных матриц с комплексными элементами, представленных в
формате **CCS (Compressed Column Storage)**.  

Последовательная реализация используется как:

- эталон корректности,
- база для сравнения ускорений в параллельных версиях.

---

## 2. Постановка задачи

Необходимо для двух матриц в формате **CCS** вычислить их произведение и затем сохранить его в формате **CCS**.
Произведение двух матриц A = a[i, j], B = b[j, k] (i = 1, m; j = 1, n; k = 1, p; a[i, j] ∈ **C**; b[i, j] ∈ **C**)
равно матрице C со элементами, выражающимися по формуле: C = c[i, k] = Σ(a[i, j] * b[j, k]), j ∈ 1, n.

### Условия корректности

- Проверяется совместимость размеров: **A.n = B.m**
- Результат должен быть в формате **CCS**
- Допустимая погрешность: **1e-14**

### Формат CCS [1]

Матрица хранится в трёх массивах:

- `values` — значения ненулевых элементов  
- `row_ind` — индексы строк  
- `col_ptr` — указатели начала столбцов  

---

## 3. Базовый алгоритм

Используется двухфазный алгоритм:

### Символьная фаза (Symbolic)

- Определяется структура результата
- Формируется массив `col_ptr`
- Используется массив `marker` для отслеживания уникальных строк

### Численная фаза (Numeric)

- Вычисляются значения элементов
- Используются:
  - `acc` — аккумулятор
  - `rows` — активные индексы строк
  - `marker` — предотвращение повторов
- Результат записывается в уже выделенную память итоговой матрицы

### Асимптотика

- Время: зависит от структуры разреженности  
- Память: O(m + nnz(C))

### Инварианты

- Повторная обработка одной позиции исключена
- Аккумулятор очищается после использования
- Структура результата соответствует CCS

---

## 4. Детали реализации

Файлы:

- `seq/include/ops_seq.hpp`
- `seq/src/ops_seq.cpp`

Pipeline реализации:

- **ValidationImpl**
  - проверка условия `A.n == B.m`

- **PreProcessingImpl**
  - входные данные уже готовы

- **RunImpl**
  - вызов:
    - `SpMMSymbolic`
    - `SpMMNumeric`

- **PostProcessingImpl**
  - результат уже сформирован

### Особенности

- Используется двухфазный подход
- Отсутствуют динамические аллокации внутри ядра вычислений
- Засчёт `marker` каждый столбец обрабатывается единожды

---

## 5. Проверка корректности

Корректность проверялась:

- сравнением с эталонной реализацией
- функциональными тестами:
  - малые размеры
  - граничные случаи
  - случайные разреженные матрицы

Сравнение выполняется с точностью **1e-14**.

---

## 6. Экспериментальная среда

- CPU: Intel Core 6-core i5-12400F  
- RAM: 64 GB  
- OS: Windows 11  
- Компилятор: MSVC  
- Сборка: Release  

### Сборка

```bash
cmake -S . -B build -D USE_FUNC_TESTS=ON -D USE_PERF_TESTS=ON -D CMAKE_BUILD_TYPE=Release
cmake --build build --config Release --parallel
cd build/bin

# Функциональные тесты
.\ppc_func_tests.exe --gtest_repeat=5 --gtest_filter="*zagryadskov_m_complex_spmm_ccs_seq_enabled*"

# Производительность
.\ppc_perf_tests.exe --gtest_repeat=5 --test_filter="*task_run_zagryadskov_m_complex_spmm_ccs_seq_enabled"
```

## 7. Результаты

Параметры эксперимента:

- Размер матриц: 40000 × 40000  
- Тип: ленточные разреженные матрицы  
- Ширина ленты: 100  

Результат измерения (baseline):

| workers | time (s) | speedup | efficiency |
| -------- | --------- | -------- | ------------ |
| 1 | 0.786 | 1.0 | 1.0 |

Наблюдения:

- Основное время выполнения сосредоточено в численной фазе алгоритма  
- Производительность ограничена пропускной способностью памяти и разреженной структурой данных  
- Полученное время используется как базовое для сравнения с параллельными версиями  

---

## 8. Выводы

- Реализован последовательный алгоритм умножения разреженных матриц в формате CCS  
- Корректность подтверждена функциональными тестами с учётом погрешности вычислений  
- Получено базовое время выполнения (baseline), необходимое для анализа ускорения  
- Используемая двухфазная схема (symbolic + numeric) устраняет лишние аллокации и подготавливает алгоритм
к эффективному распараллеливанию  

---

## Список литературы

1. Лекция кафедры ВВСП о хранении разреженных матриц в формате презентации:
<https://hpc-education.unn.ru/files/courses/optimization/2_3_SparseDS_Lect.pdf>

---

## Приложение

Фрагмент показывает базовое ядро, соответствующее численной части SpMM. Здесь последовательно
выполняется произведение строк матрицы B на матрицу A, результаты накапливаются в массивах
`acc` и `marker`.

```cpp
void ZagryadskovMComplexSpMMCCSSEQ::SpMMKernel(const CCS &a, const CCS &b, CCS &c, const std::complex<double> &zero,
                                               std::vector<int> &rows, std::vector<std::complex<double>> &acc,
                                               std::vector<int> &marker, int j) {
  rows.clear();
  int write_ptr = c.col_ptr[j];

  for (int k = b.col_ptr[j]; k < b.col_ptr[j + 1]; ++k) {
    std::complex<double> tmpval = b.values[k];
    int b_row = b.row_ind[k];
    for (int zp = a.col_ptr[b_row]; zp < a.col_ptr[b_row + 1]; ++zp) {
      int a_row = a.row_ind[zp];
      acc[a_row] += tmpval * a.values[zp];
      if (marker[a_row] != j) {
        marker[a_row] = j;
        rows.push_back(a_row);
      }
    }
  }

  for (int r_idx : rows) {
    c.row_ind[write_ptr] = r_idx;
    c.values[write_ptr] = acc[r_idx];
    ++write_ptr;
    acc[r_idx] = zero;
  }
}
```
