# Умножение разреженных матриц. Элементы комплексного типа. Формат хранения матрицы – столбцовый (CСS)

- Студент: Загрядсков М.А.  
- Группа: 3823Б1ПМоп3
- Технология: OMP
- Вариант: 7  

---

## 1. Контекст

Рассматривается параллельная реализация умножения разреженных матриц в формате **CCS** [1]
с использованием OpenMP [2].

Цель:

- уменьшить время выполнения по сравнению с SEQ
- исследовать масштабируемость алгоритма

---

## 2. Постановка задачи

Необходимо для двух матриц в формате **CCS** вычислить их произведение и затем сохранить его в формате **CCS**.
Произведение двух матриц A = a[i, j], B = b[j, k] (i = 1, m; j = 1, n; k = 1, p; a[i, j] ∈ **C**; b[i, j] ∈ **C**)
равно матрице C со элементами, выражающимися по формуле: C = c[i, k] = Σ(a[i, j] * b[j, k]), j ∈ 1, n.

### Условия корректности

- Проверяется: **A.n = B.m**
- Формат результата: CCS
- Погрешность: **1e-14**
- Сравнение с SEQ

---

## 3. Базовый алгоритм

Используется тот же двухфазный алгоритм, что и в SEQ:

1. Symbolic phase — для каждого столбца правой матрицы определяется структура соответствующего столбца результата:

- Выполняется обход ненулевых элементов и формируется множество индексов строк
- Вычисляется массив **col_ptr** результирующей матрицы

1. Numeric phase — для каждого столбца правой матрицы вычисляется столбец итоговой матрицы:

- Для каждого столбца используются локальные для потоков вектора **acc** аккумулирования результата и **marker**
- Численный результат записывается в заранее выделенные векторы результирующей матрицы

Параллелизм вводится на уровне обработки столбцов матрицы B.

---

## 4. Схема распараллеливания

### Область параллелизма

Параллелизуется цикл по столбцам:

- каждый поток обрабатывает свой диапазон `[jstart, jend)`

### Разделение данных

- `a`, `b`, `c` — **shared**
- `marker`, `acc`, `rows` — **private (локальные внутри функций)**

### Синхронизация

- отсутствует (каждый поток пишет в свой диапазон `c`)
- нет `critical`, `atomic`, `reduction`

### Барьеры

- неявный барьер в конце `#pragma omp parallel`

### Распределение работы

Используется **ручное разбиение диапазона**:

```cpp
jstart = (tid * n) / num_threads
jend   = ((tid + 1) * n) / num_threads
```

## 5. Детали реализации

Файлы:

- `seq/include/ops_omp.hpp`
- `seq/src/ops_omp.cpp`

Pipeline реализации:

- **ValidationImpl**
  - проверка условия `A.n == B.m`

- **PreProcessingImpl**
  - входные данные уже готовы

- **RunImpl**
  - параллельный вызов:
    - `SpMMSymbolic`
    - `SpMMNumeric`

- **PostProcessingImpl**
  - результат уже сформирован

### Особенности

- Используется двухфазный подход, засчёт которого избегаются гонки данных
- Каждый поток работает со своим диапазоном столбцов B

## 6. Проверка корректности

Корректность проверялась:

- сравнением с реализацией SEQ на следующих данных:
  - граничные случаи
  - случайные разреженные матрицы

Сравнение выполняется с точностью **1e-14**.

---

## 7. Экспериментальная среда

- CPU: Intel Core 6-core i5-12400F
- RAM: 64 GB  
- OS: Windows 11  
- Компилятор: MSVC  
- Сборка: Release  

### Сборка

```bash
cmake -S . -B build -D USE_FUNC_TESTS=ON -D USE_PERF_TESTS=ON -D CMAKE_BUILD_TYPE=Release
cmake --build build --config Release --parallel
cd build/bin

$env:PPC_NUM_THREADS="<threads>"
.\ppc_func_tests.exe --gtest_filter="*zagryadskov_m_complex_spmm_ccs_omp_enabled*"

$env:PPC_NUM_THREADS="<threads>"
.\ppc_perf_tests.exe --gtest_repeat=5 --gtest_filter="*task_run_zagryadskov_m_complex_spmm_ccs_omp_enabled*"
```

### Методика измерений

- 5 повторов
- используется **медиана** по результатам
- входные данные одинаковы

## 8. Результаты

Параметры эксперимента:

- Размер матриц: 40000 × 40000  
- Тип: ленточные разреженные матрицы  
- Ширина ленты: 100  

Результат измерения (baseline):

| workers | time (s) | speedup | efficiency |
| -------- | --------- | -------- | ------------ |
| 1 | 0.977 | 0.87 | 0.87 |
| 2 | 0.536 | 1.78 | 0.89 |
| 3 | 0.394 | 2.42 | 0.81 |
| 6 | 0.253 | 3.76 | 0.63 |
| 12 | 0.166 | 5.73 | 0.48 |

Наблюдения:

- Почти линейный рост до 3 потоков
- Начиная с 6 потоков эффективность падает  
- Причины:
  - конкуренция за память (ограничение шины данных)
  - плохая балансировка нагрузки

---

## 9. Выводы

- Реализован параллельный алгоритм умножения разреженных матриц в формате CCS с использованием технологии OpenMP
- Корректность подтверждена функциональными тестами с учётом погрешности вычислений
- Получено ускорение до 5.73 раз
- Эффективность высокая при малом числе потоков, но падает при увеличении их количетва
- Алгоритм хорошо масштабируется до числа физических ядер
- Основным ограничением для дальнейшего роста производительности является память
(ограничение шины данных, разреженность структуры матрицы)

---

## Список литературы

1. Лекция кафедры ВВСП о хранении разреженных матриц в формате презентации:
<https://hpc-education.unn.ru/files/courses/optimization/2_3_SparseDS_Lect.pdf>
2. Документация к технологии OpenMP:
<https://www.openmp.org/wp-content/uploads/OpenMP-RefGuide-6.0-OMP60SC24-web.pdf>

---

## Приложение

Фрагмент показывает верхний уровень SpMM, на котором иллюстрируется принцип распределения
данных по потокам. Здесь сперва для каждого потока вычисляются индексы столбцов матрицы B,
затем выполняются фазы symbolic и numeric, каждая вычисляется параллельно.

```cpp
void ZagryadskovMComplexSpMMCCSOMP::SpMM(const CCS &a, const CCS &b, CCS &c) {
  c.m = a.m;
  c.n = b.n;
  const int num_threads = ppc::util::GetNumThreads();

  std::complex<double> zero(0.0, 0.0);
  c.col_ptr.assign(c.n + 1, 0);

#pragma omp parallel default(none) shared(num_threads, a, b, c) num_threads(ppc::util::GetNumThreads())
  {
    int tid = omp_get_thread_num();
    int jstart = (tid * b.n) / num_threads;
    int jend = ((tid + 1) * b.n) / num_threads;
    SpMMSymbolic(a, b, c.col_ptr, jstart, jend);
  }

  for (int j = 0; j < c.n; ++j) {
    c.col_ptr[j + 1] += c.col_ptr[j];
  }
  int nnz = c.col_ptr[b.n];
  c.row_ind.resize(nnz);
  c.values.resize(nnz);
#pragma omp parallel default(none) shared(num_threads, a, b, c, zero) num_threads(ppc::util::GetNumThreads())
  {
    int tid = omp_get_thread_num();
    int jstart = (tid * b.n) / num_threads;
    int jend = ((tid + 1) * b.n) / num_threads;
    SpMMNumeric(a, b, c, zero, jstart, jend);
  }
}
```
