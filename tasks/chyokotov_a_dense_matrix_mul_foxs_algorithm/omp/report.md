# Умножение плотных матриц. Элементы типа double. Блочная схема, алгоритм Фокса — OMP

- Student: Чёкотов Алексей Павлович
- Technology: OMP
- Variant: 2

## 1. Контекст

Из последовательной версии (SEQ) в OpenMP переносится блочный алгоритм умножения плотных матриц с
целью ускорения вычислений за счёт параллельного выполнения итераций на нескольких потоках.

## 2. Постановка задачи

Реализовать параллельное умножение квадратных матриц A × B = C с использованием OpenMP
Baseline: последовательная реализация в seq/ops_seq.cpp, служащая эталоном для проверки
корректности и измерения ускорения

## 3. Базовый алгоритм

Последовательная версия реализует блочный алгоритм:

1. Разбивает матрицы на блоки размером sqrt(n)
2. В трёх вложенных циклах по блокам перемножает соответствующие блоки
3. Внутренняя функция Matmul() выполняет стандартный тройной вложенный цикл для перемножения блоков
с накоплением результата в выходной матрице

Асимптотика: O(n^3) по времени, O(n^2) по памяти.

## 4. Схема распараллеливания

Параллелизация происходит для внутренних циклов Matmul()

### Переменные: shared / private

- Shared:
a, b, n, istart, iend, jstart, jend, kstart, kend - Чтение
GetOutput() -  Записывание, требуется синхронизация
- Private:
i, j, k, sum  -  Каждый поток имеет свою копию

### atomic

Так как разные потоки могут одновременно обновлять одну и ту же ячейку GetOutput()\[index] при
обработке разных блоков, то необходимо гарантировать что операция выполняется как неделимая

### Shedule

Выбран статический shedule по умолчанию (благодаря collapse(2)), так как нет необходимости в
 динамическом распределении, так как вычислительная нагрузка однородна

## 5. Детали реализации

### Относительно последовательной версии

- Подключение заголовочного файла
- Добавление OMP директив в функции Matmul()

### Устранённые риски гонок

- Одновременная запись в GetOutput() из разных потоков разрешена #pragma omp atomic
- Чтение/запись sum между потоками sum - автоматически private
- Доступ к общим неизменяемым данным (a, b) shared - безопасно, так как используется только чтение

## 6. Проверка корректности

Результат OpenMP-версии сравнивается с ожидаемым результатом в функциональных тестах аналогичных
для последовательной версии с точностью 0.0001

### Характерные случаи

Характерные случаи

- Корректность atomic: при одновременном обновлении одной ячейки разными блоками результат не
теряется
- Границы блоков: неполные блоки на границах матрицы корректно обрабатываются через std::min()
- Пустая матрица: ранний выход без ошибок

## 7. Экспериментальная среда

- CPU - AMD Ryzen 5 5500U with Radeon Graphics(2.10 GHz)
- 6 cores, 12 treads.
- OS: Windows 11, 25Н2 version
- compiler: clang version 21.1.0
- build type Release.

- Запуск

```bash
set PPC_NUM_THREADS=2
.\build\bin\ppc_perf_tests.exe --gtest_filter="\*Chyokotov*"
```

- Размер матриц для теста на производительность 625х625

## 8. Результаты

| Потоков | Время (с),  | Ускорение (Speedup)| Эффективность (Efficiency) |
| ------- | ----------- | -------------------| -------------------------- |
| 1       | 1.8012      | 1.00               | 100%                       |
| 2       | 0.9483      | 1.9                | 95.0%                      |
| 6       | 0.3924      | 4.59               | 76.5%                      |

## 9. Выводы

OpenMP даёт заметный выигрыш на регулярных вычислительных задачах с большим объёмом работы,
обеспечивая ускорение до 4-5 раз при 6 потоках за счёт простоты распараллеливания циклов. Однако
этот выигрыш ограничен накладными расходами на синхронизацию, а также при мелких задачах или
неоднородной нагрузке overhead может свести преимущества многопоточности к нулю.
