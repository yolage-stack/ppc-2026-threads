# Умножение плотных матриц. Элементы типа double. Блочная схема, алгоритм Фокса - SEQ

- Student: Чёкотов Алексей Павлович
- Technology: SEQ
- Variant: 2

## 1. Контекст

Реализовано последовательное умножение плотных матриц (алгоритм Фокса). Последовательная версия
служит эталоном для проверки корректности параллельных реализаций и базой для сравнения
производительности.

## 2. Постановка задачи

### Входные данные

InType = std::pair\<std::vector\<double>, std::vector\<double>> — две матрицы A и B, хранящиеся в
виде одномерных векторов построчно.

### Выходные данные

OutType = std::vector\<double> — матрица C = A × B (тоже построчно)

### Ограничения

- Матрицы квадратные (размер n × n)
- Размер вычисляется как sqrt(a.size())
- Элементы — double

### Крайние случаи

- Пустая матрица (n = 0) — корректно обрабатывается
- Матрица 1×1
- Произвольные размеры, не кратные размеру блока

## 3. Базовый алгоритм

1. Вычисляется размер блока block_size = sqrt(n)
2. Вычисляется количество блоков count_block = ceil(n / block_size)
3. Тройной вложенный цикл по блокам ic, jc, kc:
4. Определяются границы блоков по строкам i, j, k
5. Вызывается Matmul() для перемножения текущих блоков, внутри Matmul() — стандартный тройной
вложенный цикл

Асимптотика по времени: O(n^3) - классическое умножение матриц
Асимптотика по памяти: O(n^2) - хранение трёх матриц (A, B, C)
Критерий корректности: результат совпадает с прямым умножением с точностью до 0.0001

## 4. Детали реализации

- ValidationImpl() - Проверяет, что размеры матриц A и B совпадают (first.size() == second.size())
- PreProcessingImpl() - Очищает и выделяет память под выходную матрицу (resize(n*n, 0.0))
- RunImpl() - Основной алгоритм блочного умножения. Вычисляет блоки.
- Вспомогательные методы:
CalculateBlockSize() — sqrt(n)
CountBlock() — (n + size - 1) / size
Matmul() — перемножение блоков с аккумуляцией в GetOutput()

### Обработанные исключительные случаи

n = 0 -> ранний return true
Неполные блоки на границах — через std::min(iend, n)

## 5. Проверка корректности

### Сравнение результатов

- В функциональных тестах заранее вычисляется эталонный результат прямым тройным циклом и
сохраняется в expected_output_
- В CheckTestOutputData() выполняется поэлементное сравнение expected_output_ и реального выхода

### Функциональные тесты

- Пустая матрица
- 1х1
- 2х2
- 4х4 единичная

## 6. Экспериментальная среда

- CPU - AMD Ryzen 5 5500U with Radeon Graphics(2.10 GHz)
- 6 cores, 12 treads.
- OS: Windows 11, 25Н2 version
- compiler: clang version 21.1.0
- build type Release.

- Запуск

```bash
.\build\bin\ppc_func_tests.exe --gtest_filter="\*Chyokotov*"
.\build\bin\ppc_perf_tests.exe --gtest_filter="\*Chyokotov*"
```

- Размер матриц для теста на производительность 625х625

## 7. Результаты

Время (с) = 1.678

## 8. Выводы

Последовательная версия служит эталоном, потому что она проста, детерминирована и не имеет
накладных расходов на синхронизацию, что позволяет объективно измерить ускорение параллельных
реализаций
