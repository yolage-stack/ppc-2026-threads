# Поразрядная сортировка для целых чисел с четно-нечетным слиянием Бэтчера — OMP

- Student: Хруев Антон
- Group: 3823Б1ФИ1
- Technology: OMP
- Variant: 18

## 1. Контекст

Перенос последовательной реализации на технологию OpenMP для ускорения
вычислений на системах с общей памятью (многоядерные процессоры).

## 2. Постановка задачи

Необходимо распараллелить эталонный алгоритм из `seq`, сохранив его семантику и
корректность на тех же входных данных. Ограничения и граничные случаи аналогичны
SEQ-версии.

## 3. Базовый алгоритм

Алгоритм дополняет массив до степени двойки, сортирует левую и правую половины
поразрядной сортировкой, а затем объединяет их через сеть слияния Бэтчера.

## 4. Схема распараллеливания

Распараллеливание выполнено на двух уровнях:

1. **Сортировка половин:** Используется директива `#pragma omp parallel
   sections` для независимой сортировки левой и правой частей массива.
2. **Слияние Бэтчера:** Циклы внутри шагов сети слияния распараллелены
   директивой `#pragma omp parallel for`. Так как на каждом шаге итерации
   независимы (каждый поток читает и пишет строго в свои индексы `i` и `j`),
   гонки данных (Data Races) отсутствуют. Используется `default(none)` для
   строгого контроля видимости переменных.

## 5. Детали реализации

Файлы: `omp/include/ops_omp.hpp`, `omp/src/ops_omp.cpp`

```cpp
// File: omp/src/ops_omp.cpp
#pragma omp parallel for shared(a, n, po) default(none)
for (size_t i = po; i < n - po; i += 2 * po) {
  for (size_t j = 0; j < po; ++j) {
    CompareExchange(a, i + j, i + j + po);
  }
}
```

Фрагмент показывает распараллеливание этапа слияния. Переменные `a`, `n`, `po`
передаются как `shared`, так как массив `a` модифицируется безопасно, а `n` и
`po` только читаются. Неявный барьер в конце области `parallel for` гарантирует,
что следующий шаг слияния не начнется до завершения текущего.

## 6. Проверка корректности

Сравнение с результатами SEQ-версии. Все модульные тесты
(`khruev_a_radix_sorting_int_bather_merge_omp`) успешно проходят, что
подтверждает отсутствие состояний гонки и корректность распараллеленной логики.

## 7. Экспериментальная среда

- **CPU:** AMD Ryzen 3 7320U (**Cores:** 4, **Threads:** 2)
- **RAM:** 8
- **OS:** Linux
- **Compiler:** gcc 14.2.0
- **build type:** Release

## 8. Результаты

**task_run**:

| Mode | Count | Time, s  | Speedup | Efficiency |
|------|-------|----------|---------|------------|
| seq  | 1     | 0.044561 | 1.000   | N/A        |
| omp  | 2     | 0.035524 | 1.253   | 62.7%      |
| omp  | 4     | 0.034875 | 1.271   | 31.8%      |
| omp  | 8     | 0.029626 | 1.503   | 18.7%      |

**task_pipeline**:

| Mode | Count | Time, s  | Speedup | Efficiency |
|------|-------|----------|---------|------------|
| seq  | 1     | 0.046392 | 1.000   | N/A        |
| omp  | 2     | 0.034286 | 1.353   | 67.6%      |
| omp  | 4     | 0.033278 | 1.383   | 34.5%      |
| omp  | 8     | 0.030479 | 1.523   | 19.0%      |

## 9. Выводы

OpenMP демонстрирует масштабируемость вплоть до 8 потоков, показав максимальное
ускорение 1.52x (в режиме pipeline). Ускорение достигается за счет
распараллеливания итераций внутри сети слияния Бэтчера. Однако эффективность
резко падает с ~67% на 2 потоках до 19% на 8 потоках.
