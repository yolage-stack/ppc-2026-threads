# Общий отчет по лабораторным работам: Поразрядная сортировка для целых чисел с простым слиянием

- **Студент:** Вотинцев Д. С.
- **Группа:** 3823Б1ФИ3
- **Технологии:** SEQ, OMP, TBB, STL, ALL
- **Вариант:** 17
- **Локальные отчеты:** seq/report.md, omp/report.md, tbb/report.md, stl/report.md, all/report.md

## 1. Введение

Сортировка данных является одной из фундаментальных задач в области информатики. Поразрядная сортировка (Radix Sort)
позволяет достичь линейной сложности относительно количества элементов при фиксированном диапазоне значений. Данная
работа объединяет реализацию и анализ эффективности алгоритма на базе пяти различных технологий: последовательной
(SEQ), OpenMP (OMP), Intel Threading Building Blocks (TBB), механизмов стандартной библиотеки C++ (STL) и гибридной
модели MPI + OpenMP (ALL).

## 2. Постановка задачи

Необходимо реализовать и сравнить алгоритмы поразрядной сортировки (LSD) для вектора 32-битных целых чисел.

- **Входные данные**: Неотсортированный вектор `std::vector<int32_t>`.
- **Выходные данные**: Отсортированный по неубыванию вектор.
- **Ограничения**: Корректная обработка отрицательных чисел, стабильность локальных сортировок, эффективное
  использование вычислительных ресурсов.

## 3. Описание базового алгоритма

Во всех реализациях в качестве основы используется следующий последовательный алгоритм:

1. Определение минимального и максимального элементов.
2. Сдвиг всех значений в положительную область (вычитание минимума, если он меньше нуля) для корректной
   поразрядной обработки.
3. Поразрядный обход (LSD - Least Significant Digit) с использованием стабильной сортировки подсчетом.
4. Обратный сдвиг к исходным значениям.

## 4. Схемы распараллеливания

- **SEQ (Последовательная версия):** Базовый эталон. Все вычисления выполняются последовательно на одном ядре
  процессора без дополнительных накладных расходов на синхронизацию.
- **OMP (OpenMP):** Входной массив делится на $N$ равных частей. Каждый поток независимо выполняет полную локальную
  сортировку своего участка, после чего происходит параллельное древовидное слияние с использованием барьерной
  синхронизации (`#pragma omp barrier`).
- **TBB (Threading Building Blocks):** Используются алгоритмы `tbb::parallel_reduce` (для поиска минимума) и
  `tbb::parallel_for` (для сдвига). Слияние отсортированных блоков организовано в виде дерева задач через
  планировщик TBB, обеспечивающий динамическую балансировку нагрузки.
- **STL (std::async):** Распараллеливание на основе асинхронных задач. Массив делится на блоки, каждый сортируется в
  отдельном `std::async`. Древовидное слияние также выполняется асинхронно парами, синхронизация обеспечивается через
  вызов метода `.get()` объектов `std::future`.
- **ALL (MPI + OpenMP):** Двухуровневая гибридная схема. Корневой процесс (MPI) распределяет блоки данных между узлами
  через `MPI_Scatterv`. Внутри каждого процесса используется OpenMP для параллельной локальной сортировки.
  Отсортированные блоки собираются на нулевом процессе через `MPI_Gatherv`, после чего выполняется финальное слияние.

- **Смысл MPI-синхронизации**:
  - `MPI_Bcast`: Выступает начальной точкой синхронизации.
  Гарантирует, что все запущенные процессы одновременно получат точный размер исходного вектора `n`
  (от корневого процесса (rank 0) )
  - `MPI_Scatterv` и `MPI_Gatherv`:
  Осуществляют синхронную пересылку блоков данных между процессами.
  - `Глобальная синхронизация`:
  Операция `MPI_Gatherv` служит барьером сбора данных.
  Финальное слияние отсортированных кусков в один массив выполняется на master процессе строго после завершения сбора
  (что гарантирует непротиворечивость и корректность итогового результата)
  Внутри каждого MPI-процесса параллелизм дополнительно ускоряется на уровне ядер процессора
  (с помощью OpenMP-директив без взаимных блокировок между процессами)
  
## 5. Окружение

- **Оборудование**:
  - ЦП: Intel(R) Core(TM) i5-8400 CPU @ 2.80GHz (6 ядер)
  - ОЗУ: 16 ГБ
- **ПО**: Windows 10, компилятор MSVC (Visual Studio 2022), Microsoft MPI, Intel TBB.
- **Тип сборки**: Release.
- **Данные**: Вектор из 1 000 000 псевдослучайных элементов типа `int32_t`.

## 6. Результаты экспериментов

### 6.0 Воспроизводимость

Для запуска используются команды:

1) Конфигурация и билд
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel
2) Func тесты
python scripts/run_tests.py --running-type=processes --counts 1 2 3 4
python scripts/run_tests.py --running-type=threads --counts 1 2 3 4
3) Perf тесты
$env:PPC_NUM_THREADS = "4"
$env:PPC_NUM_PROC = "2"
python scripts/run_tests.py --running-type="performance"

### 6.1 Корректность

Для всех технологий проведено функциональное тестирование с использованием **Google Test**. Проверены различные
краевые случаи: инвертированные массивы, массивы с дубликатами, исключительно отрицательные числа, однородные
массивы и граничные значения. Результаты всех пяти реализаций успешно совпали с эталонной сортировкой
`std::ranges::sort`.

### 6.2 Производительность (Сводная таблица)

Количество элементов в массиве: 6 000 000

Время замеров - среднее. Режим task (не pipeline).

| Технология | Ranks (MPI) | Threads per Rank | Всего ресурсов | Время, сек | Ускорение | Эффективность |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| **SEQ** | 1 | 1 | 1 | 2.414 | 1.00 | 100.0% |
| **OMP** | 1 | 2 | 2 | 1.700 | 1.42 | 71.0% |
| **OMP** | 1 | 4 | 4 | 1.300 | 1.86 | 46.5% |
| **TBB** | 1 | 2 | 2 | 1.710 | 1.41 | 70.5% |
| **TBB** | 1 | 4 | 4 | 1.315 | 1.84 | 46.0% |
| **STL** | 1 | 2 | 2 | 1.690 | 1.43 | 71.5% |
| **STL** | 1 | 4 | 4 | 1.280 | 1.89 | 47.3% |
| **ALL** | 2 | 1 | 2 | 1.631 | 1.48 | 74.0% |
| **ALL** | 2 | 2 | 4 | 1.305 | 1.85 | 46.2% |

## 7. Заключение

В ходе работы были реализованы и исследованы 5 версий алгоритма поразрядной сортировки (LSD) со слиянием.
Все параллельные реализации продемонстрировали прирост производительности относительно последовательного эталона (SEQ)

1. **Наилучшую производительность на одном узле показала STL-версия** (ускорение 1.89x на 4 потоках).
Это обусловлено тем, что механизм `std::async(std::launch::async, ...)` эффективно использует потоки операционной
системы и минимизирует накладные расходы на логику планирования.
Локальные операции выполняются над независимыми непрерывными участками памяти
(что обеспечивает отличную локальность данных в кэш-памяти процессора)
2. **Результаты TBB (1.84x) и OpenMP (1.86x) оказались крайне близки**,
поскольку обе технологии сталкиваются со схожими ограничениями.
В OpenMP используется фиксированная барьерная синхронизация на каждом шаге слияния (`#pragma omp barrier`),
из-за чего потоки вынуждены простаивать в ожидании самого медленного ядра (эффект дисбаланса на финальных стадиях).
TBB сглаживает этот дисбаланс за счет механизма кражи задач (work-stealing).
Но накладные расходы самого runtime TBB на управление объектами `blocked_range` на данном
объеме данных уравнивают его эффективность с OpenMP.
3. **Гибридная версия ALL (MPI+OpenMP)**
на одном узле показала результаты на уровне локальных версий (только MPI или только OMP).
Ее масштабируемость ограничена затратами на межпроцессное взаимодействие
(копирование данных `MPI_Scatterv` / `MPI_Gatherv` и синхронизация `MPI_Bcast`)

## 8. Список литературы

1. Сысоев А. В. Курс лекций "Параллельное программирование".
2. Документация OpenMP (<www.openmp.org>).
3. Документация Intel® Threading Building Blocks (TBB).
4. Стандарт C++ (многопоточность в C++11/C++17/C++20).
5. Официальная документация MPI 3.1.
