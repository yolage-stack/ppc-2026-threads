# Вычисление многомерных интегралов с использованием многошаговой схемы (метод Симпсона)

- Студент: Власова Арина Сергеевна, группа <3823Б1ПР2>
- Технология: OMP
- Вариант: <11>

## 1. Введение

Задача — приближенно вычислить значение кратного интеграла по d-мерному гиперпараллелепипеду,
используя составную квадратурную схему Симпсона.

## 2. Постановка задачи

Требуется вычислить приближенное значение многомерного определенного интеграла от
заданной функции на гиперпрямоугольной области.

**Входные данные:**

- Вектор `a` типа `std::vector<double>` — нижние пределы интегрирования по каждому измерению.
- Вектор `b` типа `std::vector<double>` — верхние пределы интегрирования по каждому измерению.
- Вектор `n` типа `std::vector<int>` — количество подинтервалов разбиения по каждому измерению
(каждое значение должно быть четным и положительным).
- Объект `func` типа `std::function<double(const std::vector<double>&)>` — подынтегральная функция.

**Выходные данные:**

- Значение типа `double` — приближенное значение интеграла.

**Ограничения:**

- Размерности векторов `a`, `b` и `n` должны быть одинаковыми и больше нуля.
- Для каждого измерения `i` должно выполняться `a[i] < b[i]`.
- Каждый элемент `n[i]` должен быть четным положительным числом.

## 3. Схема распараллеливания

В параллельной реализации с использованием OpenMP применяется декомпозиция по данным.

Вычисляется общее количество узлов сетки `total_points` как произведение всех элементов
`dimensions_[i]`(количество точек по каждому измерению). Каждое измерение имеет размерность `n[i] + 1`,
поэтому общее количество узлов равно `(n₁+1) × (n₂+1) × ... × (n_d+1)`.

Итерации по всем узлам распределяются между потоками с помощью директивы `#pragma omp for schedule(static)`.
При статическом планировании каждый поток получает непрерывный блок итераций,
что минимизирует накладные расходы на управление.

Для накопления частичных сумм используется редукционная переменная `sum`
с оператором `reduction(+ : sum)`, что гарантирует корректное суммирование без состояния гонки.
Каждый поток имеет собственные локальные переменные (`cur_index`, `cur_point`, `local_weight`),
объявленные внутри параллельной области.

В отличие от последовательной версии, где использовался метод `Nextindex()` для последовательного перебора,
в OpenMP версии применяется линейная индексация.
Глобальный индекс `idx` преобразуется в многомерный индекс по формуле:
для каждого измерения `i` значение индекса вычисляется как `cur_index[i] = idx % dimensions_[i]`,
а затем `idx` делится на `dimensions_[i]`.
Это позволяет каждому потоку независимо определять координаты своей точки без необходимости синхронизации.

## 4. Детали реализации

Реализация находится в файлах `tasks\vlasova_a_simpson_method\omp\include\ops_omp.hpp`
и `tasks\vlasova_a_simpson_method\omp\src\ops_omp.cpp` и
представляет собой класс `VlasovaASimpsonMethodOMP`, унаследованный от базового класса `BaseTask`.

**Ключевые компоненты и их функции:**

1. **`ValidationImpl()`**:
    - Проверяет согласованность размерностей векторов `a`, `b` и `n`.
    - Убеждается, что нижний предел меньше верхнего по каждому измерению (`a[i] < b[i]`).
    - Проверяет, что количество шагов `n[i]` является четным и положительным.
    - Проверяет, что указатель на функцию `func` корректен.
2. **`PreProcessingImpl()`**:
    - Вычисляет шаги интегрирования `h_[i] = (b[i] - a[i]) / n[i]`.
    - Определяет размерность сетки `dimensions_[i] = n[i] + 1`.
3. **`RunImpl()`**:
    - Вычисляет общее количество точек `total_points` как произведение всех `dimensions_[i]`.
    - Использует директиву `#pragma omp parallel` с редукцией `reduction(+ : sum)`.
    - Внутри параллельной области применяет `#pragma omp for schedule(static)` для распределения итераций.
    - Для каждого глобального индекса `idx` выполняет преобразование в многомерный индекс
    через операции деления и взятия остатка.
    - Вызывает `ComputeWeight()` и `ComputePoint()` для каждого узла.
    - Накопление результата происходит через редукционную переменную `sum`.
4. **`PostProcessingImpl()`**:
    - Проверяет, что результат `result` является конечным числом (`std::isfinite`).

Методы `ComputeWeight()` и `ComputePoint()` полностью идентичны последовательной версии.

**Рекомендации по использованию памяти:**

Алгоритм не требует выделения больших объемов памяти.
Основное потребление связано с хранением входных векторов (`a`, `b`, `n`)
и небольшого служебного состояния (`h_`, `dimensions_`).
Значения функции не сохраняются, а сразу добавляются в сумму,
что делает этот подход экономичным с точки зрения RAM.
Каждый поток использует собственные локальные векторы (`cur_index`, `cur_point`),
что позволяет избежать конфликтов при доступе к памяти.

## 5. Экспериментальная установка

**Аппаратное обеспечение**:

- Процессор: Intel(R) Core(TM) Ultra 9 285H (2.90 GHz)
- Оперативная память: 32 GB
- ОС: Windows 11

**Инструментарий**:

- Компилятор: clang version 21.1.6
- Тип сборки: Release
- Библиотека: OpenMP

**Данные:** Тестовые данные генерируются в блоке функциональных тестов.
Они задаются в виде набора кортежей, содержащих векторы `a`, `b`, `n`,
аналитическое ожидаемое значение и имя функции.
Сами функции определены в виде лямбда-выражений (`constant`, `linear`, `quadratic`, `product`).

## 6. Результаты и обсуждение

### 6.1 Корректность

Корректность параллельной реализации с использованием OpenMP проверена с помощью набора модульных тестов.

Было выполнено 22 теста, охватывающих различные конфигурации:
одномерные (1D), двумерные (2D) и трехмерные (3D) задачи;
константные, линейные, квадратичные, кубические функции и функции-произведения;
различные сетки разбиения от минимальных до подробных;
симметричные интервалы интегрирования.

**Результат:** Все 22 теста завершились успешно.
Общее время выполнения всех функциональных тестов для OpenMP версии составило 347 мс.

### 6.2 Производительность

Измерение производительности проводилось на трехмерной задаче интегрирования
функции Гаусса в кубе [-2, 2]^3 с разбиением n = {200, 200, 200},
что соответствует более чем 8 миллионам узлов сетки.

Тесты производительности были запущены в двух режимах:

- **pipeline** — измерение времени выполнения полного конвейера обработки
(включая валидацию, предварительную обработку, исполнение и постобработку).
- **task_run** — измерение времени только вычислительного ядра `RunImpl()`.

**Результаты производительности для OMP:**

|     Mode      | Workers |  Time  | Speedup | Efficiency |
|---------------|---------|--------|---------|------------|
| pipeline(seq) |    1    | 0.4246 |  1.000  |   1.000    |
| task_run(seq) |    1    | 0.4191 |  1.000  |   1.000    |
| pipeline(omp) |   20    | 0.0630 |  6.74   |   33.7%    |
| task_run(omp) |   20    | 0.0653 |  6.42   |   32.1%    |

**Анализ результатов:**

Параллельная реализация на OpenMP демонстрирует значительное ускорение
по сравнению с последовательной версией. В режиме `pipeline` ускорение составило 6.74x,
а в режиме `task_run` — 6.42x.

Эффективность параллелизации составляет около 32-34%, что объясняется следующими факторами:
накладные расходы на создание и синхронизацию потоков,
дополнительные вычисления для преобразования глобального индекса в многомерный,
а также накладные расходы на редукционную операцию.

## 7. Выводы

В рамках данной работы была успешно реализована и протестирована последовательная версия алгоритма
вычисления многомерных интегралов составным методом Симпсона.
Алгоритм продемонстрировал высокую точность на наборе тестовых функций и полностью соответствует
теоретическим основам метода. Параллельная версия на OpenMP достигла ускорения около 6.7x
по сравнению с последовательной реализацией,
что подтверждает эффективность распараллеливания на многопоточной системе.

## 8. Приложения

```cpp
bool VlasovaASimpsonMethodOMP::RunImpl() {
  size_t dim = task_data_.a.size();

  size_t total_points = 1;
  for (size_t i = 0; i < dim; ++i) {
    total_points *= static_cast<size_t>(dimensions_[i]);
  }

  double sum = 0.0;

#pragma omp parallel default(none) shared(dim, total_points) reduction(+ : sum)
  {
    std::vector<int> cur_index(dim, 0);
    std::vector<double> cur_point;
    double local_weight = 0.0;

#pragma omp for schedule(static)
    for (size_t idx = 0; idx < total_points; ++idx) {
      size_t temp_idx = idx;
      for (size_t i = 0; i < dim; ++i) {
        cur_index[i] = static_cast<int>(temp_idx % static_cast<int>(dimensions_[i]));
        temp_idx /= static_cast<int>(dimensions_[i]);
      }

      ComputeWeight(cur_index, local_weight);
      ComputePoint(cur_index, cur_point);
      sum += local_weight * task_data_.func(cur_point);
    }
  }

  double factor = 1.0;
  for (size_t i = 0; i < dim; ++i) {
    factor *= h_[i] / 3.0;
  }

  result_ = sum * factor;
  GetOutput() = result_;

  return true;
}
```
