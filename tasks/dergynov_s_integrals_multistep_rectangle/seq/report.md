# Вычисление многомерных интегралов методом прямоугольников — SEQ

- Студент: Дергунов Сергей Антонович, группа 3823Б1ПР4
- Технология: SEQ
- Вариант: 9

## 1. Введение

SEQ-версия реализована как базовый эталон для сравнения с OMP, TBB, STL и ALL.
Алгоритм вычисляет многомерный интеграл методом средних прямоугольников.

## 2. Постановка задачи

- **Вход**:
  - подынтегральная функция `std::function<double(const std::vector<double>&)>`
  - границы интегрирования `std::vector<std::pair<double, double>>`
  - количество шагов разбиения `int`
- **Условие корректности входа**:
  - функция задана корректно
  - количество шагов > 0
  - границы не пусты и для каждого измерения `left < right`
- **Ограничения**: размерность произвольная, но для тестов используется 1-3
- **Выход**: приближённое значение интеграла `double`

## 3. Базовый алгоритм (последовательный)

Алгоритм метода средних прямоугольников:

1. Вычисляется шаг сетки по каждому измерению: `h_i = (right_i - left_i) / n`
2. Объём элементарной ячейки: `cell_volume = ∏ h_i`
3. Перебираются все узлы сетки рекурсивным обходом:
   - Для каждого узла вычисляются координаты средней точки ячейки
   - Вычисляется значение функции в этой точке
   - Сумма накапливается
4. Результат: `integral = sum * cell_volume`

**Асимптотика**: O(n^d) по времени, O(d) по памяти (где d — размерность).

## 4. Схема распараллеливания

Параллелизм отсутствует. Обработка выполняется последовательно.
Это честный baseline для всех последующих сравнений.

## 5. Детали реализации

- Файлы: `seq/include/ops_seq.hpp`, `seq/src/ops_seq.cpp`
- Класс: `DergynovSIntegralsMultistepRectangleSEQ`
- Конвейер: `ValidationImpl`, `PreProcessingImpl`, `RunImpl`, `PostProcessingImpl`

## 6. Экспериментальная среда

Сборка:
``баш(по русски чтобы не ломалось)
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DUSE_FUNC_TESTS=ON -DUSE_PERF_TESTS=ON
cmake --build build --parallel
``

Запуск:
``баш(по русски чтобы не ломалось)
export PPC_NUM_THREADS=1
./build/bin/ppc_perf_tests --gtest_filter="*seq*"
``

## 7. Результаты

### 7.1 Корректность

SEQ-версия проходит все функциональные тесты. Корректность подтверждается сравнением с аналитическими значениями интегралов.

### 7.2 Производительность

Определения метрик:

- `workers` — число исполнительных единиц (для SEQ всегда `1`)
- `time` — wall-clock время, секунды
- `speedup = T_seq / T_mode`
- `efficiency = speedup / workers * 100%`

Результаты:

| mode | workers | time (task_run), s | time (pipeline), s |
|------|--------:|-------------------:|-------------------:|
| seq  |       1 |            0.02584 |            0.02783 |

## 8. Выводы

SEQ-версия используется как эталон корректности и базовое время для расчёта ускорения параллельных версий.

## 9. Источники

1. Course repository: <https://github.com/learning-process/ppc-2026-threads>
2. Course report requirements: `docs/common_information/report.rst`
