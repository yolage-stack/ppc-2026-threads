# Умножение матриц методом Штрассена

- Student: Лазарева Анна, group 3823Б1ПР1
- Variant: 3
- Local reports: seq/report.md, omp/report.md, tbb/report.md, stl/report.md, all/report.md

## 1. Введение

Алгоритм Штрассена для умножения матриц представляет собой идеальный тестовый случай для сравнения
различных моделей параллелизма благодаря своей многоуровневой структуре. Алгоритм естественным
образом декомпозируется на 7 независимых произведений подматриц на каждом уровне рекурсии, что
позволяет эффективно применять как shared-memory параллелизм (потоки), так и distributed-memory
подходы (процессы). Базовые случаи используют наивное матричное умножение с регулярными циклами,
оптимальными для векторизации и кэш-оптимизации.

Асимптотическая сложность O(n^2.807) делает алгоритм вычислительно интенсивным для больших матриц,
что позволяет амортизировать накладные расходы параллелизации и получить измеримое ускорение на
современных многоядерных системах.

## 2. Единая постановка задачи

**Входные данные:** Две плотные квадратные матрицы A и B размером n×n, представленные как векторы
`std::vector<double>` размером n² в row-major порядке.

**Выходные данные:** Матрица C = A×B размером n×n.

**Ограничения:** n > 0, размеры векторов строго равны n².

**Критерий корректности:** Результат должен совпадать с классическим матричным умножением
с точностью ε = 1e-5 для компенсации погрешностей floating-point арифметики.

## 3. Единая методика эксперимента

**Окружение:**

- Platform: Linux LAPTOP-URFRF9PF 6.6.114.1-microsoft-standard-WSL2 #1 SMP PREEMPT_DYNAMIC
Mon Dec  1 20:46:23 UTC 2025 x86_64 x86_64 x86_64 GNU/Linux
- CPU: AMD Ryzen 7 5800H with Radeon Graphics (2 cores, 4 threads)
- RAM: 16 GB DDR4 (WSL allocation: 1.6 GB)
- OS: Ubuntu 24.04.3 LTS на Windows 10 22H2
- WSL Kernel: 6.6.114.1-microsoft-standard-WSL2
- Compiler: gcc (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0
- CMake: 3.28.3, build type: Release

**Переменные окружения:**

- `OMP_NUM_THREADS` - для OpenMP версии
- `TBB_NUM_THREADS` - для Intel TBB версии  
- `STL_NUM_THREADS` - для STL версии
- `mpirun -np N env TBB_NUM_THREADS=M` - для ALL версии

**Генерация данных:** Детерминированные тестовые матрицы по формулам:
`a[i] = (i % 7) + 1`, `b[i] = ((i * 3 + 5) % 11) + 1`.

**Размер тестовой задачи:** 512×512 матрицы (262,144 элементов на матрицу).

**Расчет метрик:**

- Speedup = T_SEQ / T_parallel
- Efficiency = Speedup / Workers  
- Baseline: SEQ версия = 0.0610 с

**Измерения:** Метрика `task_run` как наиболее стабильная для алгоритмических вычислений.

## 4. Сводка корректности

Все backend-ы проверены против SEQ версии на наборе функциональных тестов
с размерами 2, 3, 5, 7, 16, 64, 65, 128, 256, включающих:

- Степени двойки и произвольные размеры
- Граничные случаи (малые матрицы, порог наивного умножения)
- Случаи, требующие паддинга до степеней двойки

**Ограничения применимости:**

- TBB: показывает насыщение производительности после 3 потоков
- OMP: демонстрирует хорошее линейное масштабирование до 4 потоков
- STL: достигает плато производительности уже при 2-3 потоках
- ALL: требует MPI runtime, оптимальна для 3 процессов

## 5. Агрегированные результаты

**Сводная таблица производительности (n=512, task_run метрика):**

| Backend | Configuration | Workers | Время (мс) | Ускорение | Эффективность |
|---------|---------------|---------|------------|-----------|---------------|
| SEQ     | -             | 1       | 61.0       | 1.00      | 1.00          |
| STL     | 3 threads     | 3       | 13.5       | 4.52      | 1.51          |
| STL     | 2 threads     | 2       | 13.6       | 4.49      | 2.25          |
| STL     | 4 threads     | 4       | 13.9       | 4.39      | 1.10          |
| ALL     | 3×2 (MPI+TBB) | 6       | 14.2       | 4.30      | 0.72          |
| ALL     | 2×3 (MPI+TBB) | 6       | 18.3       | 3.33      | 0.56          |
| ALL     | 6×1 (MPI+TBB) | 6       | 17.4       | 3.51      | 0.58          |
| ALL     | 4×2 (MPI+TBB) | 8       | 17.8       | 3.43      | 0.43          |
| ALL     | 1×6 (TBB)     | 6       | 25.6       | 2.38      | 0.40          |
| OMP     | 4 threads     | 4       | 15.1       | 4.04      | 1.01          |
| OMP     | 3 threads     | 3       | 16.6       | 3.67      | 1.22          |
| OMP     | 2 threads     | 2       | 20.7       | 2.95      | 1.47          |
| TBB     | 3 workers     | 3       | 22.4       | 2.72      | 0.91          |
| TBB     | 4 workers     | 4       | 23.3       | 2.62      | 0.66          |
| TBB     | 6 workers     | 6       | 23.5       | 2.60      | 0.43          |

*Дополнительные меры стабилизации не были использованы, поэтому на результаты
могли повлиять фоновые задачи.

## 6. Интерпретация различий

**SEQ — что показывает baseline:**
Время выполнения 61.0 мс демонстрирует производительность итеративной реализации алгоритма
Штрассена без параллелизации. Baseline показывает эффективность порога переключения на наивное
умножение (n ≤ 64) и оптимизацию компилятора для последовательного кода. Служит эталоном для
измерения реального ускорения параллельных версий и выявления overhead различных
технологий параллелизации.

**OMP — сильные и слабые стороны именно на этой задаче:**
*Сильные стороны:* Показывает отличное линейное масштабирование (2.95x → 3.67x → 4.04x)
благодаря простой интеграции `#pragma omp parallel for` в существующую структуру циклов.
Статическое планирование идеально подходит для регулярных матричных операций алгоритма Штрассена.
Директивная модель требует минимальных изменений кода при получении значительного ускорения.

*Слабые стороны:* Относительно высокий overhead OpenMP runtime
(15.1 мс vs 13.5 мс у STL на оптимальных конфигурациях). Эффективность > 1.0 на меньшем
количестве потоков указывает на неоптимальность baseline или measurement artifacts.
Ограниченная масштабируемость beyond 4 потоков из-за структурных
ограничений алгоритма (7 произведений).

**TBB — роль grain size и runtime:**
Work-stealing планировщик с auto_partitioner показывает время 22.4 мс (0.91 эффективность),
демонстрируя роль автоматической балансировки нагрузки. Grain size автоматически адаптируется
для 7 произведений Штрассена, но task creation overhead становится заметным по сравнению
с прямым управлением потоками. Runtime система TBB обеспечивает хорошую утилизацию ресурсов
(0.91 эффективность), но ценой дополнительных вычислительных затрат на управление задачами.
Parallel_invoke эффективно распределяет неоднородные подзадачи, но не может преодолеть
фундаментальные ограничения алгоритма.

**STL — цена ручного управления потоками:**
Демонстрирует лучшую абсолютную производительность (13.5 мс), но за счет полной ответственности
за управление потоками. Round-robin распределение строк требует тщательного проектирования
для предотвращения load imbalance и гонок данных. Отсутствие runtime overhead библиотек
дает преимущество в производительности, но увеличивает сложность разработки.
Необходимость явного управления join() и синхронизацией создает риски ошибок программирования.
Статическая балансировка уступает динамическим планировщикам при неоднородной нагрузке,
но показывает превосходные результаты для регулярных задач.

**ALL — цена коммуникации и выигрыш гибридности:**
Конфигурация 3×2 (14.2 мс, 4.30x ускорение) демонстрирует успешную амортизацию MPI overhead
через достаточно крупные подзадачи. Коммуникационные расходы (~3.6 MB для передачи подматриц)
составляют минимальную долю от времени вычислений благодаря высокой интенсивности вычислений
алгоритма Штрассена.

Выигрыш гибридности проявляется в оптимальном распределении 7 произведений между
3 процессами (2-3-2),
что минимизирует idle time и максимизирует утилизацию ресурсов. Иерархический параллелизм
позволяет преодолеть ограничения pure shared-memory подходов, показывая конкурентоспособность
с лучшими STL результатами. MPI+TBB комбинация обеспечивает масштабируемость beyond
одного узла при сохранении эффективности локальных вычислений через work-stealing планировщик.

## 7. Репродуцируемость

**Команды сборки:**

mkdir build && cd build
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -G Ninja -DCMAKE_C_COMPILER:FILEPATH=/usr/bin/gcc
-DCMAKE_CXX_COMPILER:FILEPATH=/usr/bin/g++
cd build
ninja

**Команды запуска функциональных тестов:**
./build/bin/ppc_func_tests

**Команды получения основных замеров:**

### SEQ baseline

./build/bin/ppc_perf_tests

### OpenMP

OMP_NUM_THREADS=4 ./ppc_perf_tests     --gtest_filter="*omp_enabled*"     --gtest_print_time=true

### Intel TBB

TBB_NUM_THREADS=4 ./ppc_perf_tests     --gtest_filter="*tbb_enabled*"     --gtest_print_time=true

### STL threads

STL_NUM_THREADS=4 ./ppc_perf_tests     --gtest_filter="*stl_enabled*"     --gtest_print_time=true

### MPI+TBB гибрид (оптимальная конфигурация)

mpirun -np 6 --oversubscribe env TBB_NUM_THREADS=1 ./ppc_perf_tests

## 8. Заключение

**Лучшая версия:** ALL (3×2 конфигурация) с ускорением 4.30x и эффективностью 0.72
представляет собой оптимальное решение для данной задачи, демонстрируя превосходство
иерархического параллелизма над чистыми shared-memory подходами.

### Условия применимости

- **TBB (3 workers)** - для задач, требующих автоматической балансировки с минимальными
изменениями кода
- **OpenMP (4 threads)** - для регулярных вычислений с хорошим линейным масштабированием
- **STL (4-6 threads)** - когда требуется полный контроль над управлением потоками
- **ALL (3×2)** - для scaling beyond одного узла и максимальной производительности

**Ограничения сравнения:** Замеры выполнялись в WSL environment с ограниченными ресурсами
(6 ядер, 8GB RAM), что может не отражать поведение на production кластерах
с быстрой межузловой связью.
Алгоритм Штрассена показывает ограниченную масштабируемость beyond 6-8 workers
из-за структурных особенностей (7 произведений на уровень).

### Возможные улучшения

- Гибридная OpenMP+MPI версия для сравнения с TBB+MPI
- Адаптивный порог переключения на наивное умножение в зависимости от количества потоков
- NUMA-aware распределение памяти для больших матриц
- Векторизация базовых операций для дополнительного ускорения

## 9. Источники

- Материалы курса (ННГУ им. Лобаческого, курс "Параллельное программирование",
<https://learning-process.github.io/parallel_programming_course/ru/>)
