# Повышение контраста полутонового изображения посредством линейной растяжки гистограммы

- Студентка: Романова Василиса Дмитриевна, группа 3823Б1ФИ3
- Вариант: 28
- Локальные отчёты: seq/report.md, omp/report.md, tbb/report.md, stl/report.md, all/report.md

## 1. Введение

**Линейное растяжение гистограммы** — это базовый метод улучшения изображений, который преобразует пиксели так, чтобы
выходные значения заполняли весь диапазон [0, 255]. Алгоритм состоит из двух этапов: поиск глобальных минимума и
максимума, затем линейное преобразование каждого пикселя.

Данная задача хорошо подходит для сравнения разных моделей параллелизма по нескольким причинам:

- **Естественный параллелизм:** поиск min/max является редукционной операцией, а преобразование пикселей — независимыми
  вычислениями, что позволяет эффективно распараллелить оба этапа.

- **Низкая вычислительная плотность:** на каждый пиксель приходится всего несколько операций, что делает задачу
  memory-bound. Это позволяет оценить, насколько разные технологии справляются с ограничениями пропускной способности
  памяти.

- **Множество вариантов реализации:** задача может быть решена с использованием OpenMP, TBB, STL Threads и гибридной
  MPI+OpenMP, что даёт возможность сравнить их на одном алгоритме.

- **Простота верификации:** корректность результата легко проверить — выходное изображение должно иметь диапазон [0,
  255] или быть монохромным.

Таким образом, линейное растяжение гистограммы позволяет на реальном примере оценить производительность, удобство
реализации и масштабируемость различных технологий параллельного программирования.

## 2. Единая постановка задачи

**Входные данные:** одномерный массив `uint8_t` пикселей изображения (значения в диапазоне 0–255).

**Выходные данные:** массив того же размера после линейного растяжения гистограммы. Минимальное значение выходного
массива — 0, максимальное — 255.

Растяжение происходит поэлементно в соответствии с формулой $out[i] = \frac{(in[i] - min_v)*255}{max_v-min_v}$.

**Ограничения:** входной массив не пуст.

Результат работы программы считается корректным, если выполняется одного из двух условий:

- Выходное изображение монохромное (`min == max`) — тогда все пиксели остаются неизменными.
- Выходное изображение имеет полный динамический диапазон: минимальное значение равно 0, максимальное значение
  равно 255.

## 3. Единая методика эксперимента

### Окружение

- **CPU:** 11th Gen Intel(R) Core(TM) i5-1135G7 @ 2.40GHz
- **RAM:** 8 ГБ
- **OS:** Windows 10
- **Compiler:** clang 21.1.0 + MSVC 19.37
- **Build type:** Release
- **Переменные окружения:** `PPC_NUM_THREADS`

### Замеры производительности

Для оценки производительности запускались тесты на векторе из **400 000 000** элементов типа `uint_8t`.

**Генерация данных** происходит по формуле `in[i][j] = low + ((j * 13 + i * 109) % range)`, где `low = 75`,
`range = 50`. Т.е. все значения исходного массива данных лежат в диапазоне [75, 124].

Замеры времени производились следующим образом:

1. Производится 5 запусков задачи.
2. Из 5 запусков откидываются лучшее и худшее время.
3. Среднее арифметическое из 3 оставшихся значений записывается в таблицу.

Параметры ускорения и эффективности высчитываются в соответствии со следующими формулами:

- $speedup = T_{seq} / T_th$ — ускорение для установленного числа потоков.
- $efficiency = speedup / threads$ — эффективность использования вычислительных ресурсов.

## 4. Сводка корректности

Результат работы всех параллельных версий проверялся на выполнении одного из двух условий:

- Выходное изображение монохромное (`min == max`) — тогда все пиксели остаются неизменными.
- Выходное изображение имеет полный динамический диапазон: минимальное значение равно 0, максимальное значение
  равно 255. Если ни одно из условий не выполнено, то тест считается не пройденным.

Помимо этого производилось сравнение с работой SEQ версии. Ошибок в логике работы алгоритма выявлено не было.

Набор функциональных тестов:

1. **Изображение 1×1 (один пиксель):** проверка граничного случая минимального размера и ветки `min_v == max_v`.

2. **Изображение 5×5:** проверка правильности растяжения на малом размере.

3. **Изображение 13×13:** проверка работы алгоритма на среднем размере входных данных.

4. **Изображение 70×70:** проверка устойчивости при большем количестве пикселей.

5. **Изображение 200×200:** проверка корректности на значительном объёме данных.

6. **Реальное монохромное изображение:** — проверка ветки `min_v == max_v` на реальных данных, загружаемых из файла.

## 5. Агрегированные результаты

### 5.1 Сводная таблица производительности

| Технология | Кол-во воркеров ($p$) | Время (сек) | Ускорение ($S$) | Эффективность ($E$) |
| :--------- | :-------------------: | :---------: | :-------------: | :-----------------: |
| **SEQ**    |           1           | 0.81182274  |      1.00       |        1.00         |
| **OMP**    |           2           | 0.32275272  |      2.52       |        1.26         |
|            |           4           | 0.28233166  |      2.88       |        0.72         |
|            |           8           | 0.26246314  |      3.09       |        0.39         |
| **TBB**    |           2           | 0.42426248  |      1.91       |        0.95         |
|            |           4           | 0.23717768  |      3.42       |        0.85         |
|            |           8           | 0.14896833  |      5.45       |        0.68         |
| **STL**    |           2           | 0.43470807  |      1.87       |        0.94         |
|            |           4           | 0.26881547  |      3.02       |        0.76         |
|            |           8           | 0.17055099  |      4.76       |        0.59         |
| **ALL**    |   $4\ (2 \times 2)$   | 0.34358652  |      2.36       |        0.59         |
|            |   $8\ (4 \times 2)$   | 0.33587178  |      2.42       |        0.31         |
|            |  $16\ (4 \times 4)$   | 0.32013844  |      2.54       |        0.16         |
|            |  $32\ (4 \times 8)$   | 0.30376334  |      2.67       |        0.08         |

### 5.2 Резюме

| Технология | Макс. ускорение | Оптимальная конфигурация  | Эффективность на оптимуме |
| :--------- | :-------------: | :-----------------------: | :-----------------------: |
| **SEQ**    |      1.00       |          1 поток          |           1.00            |
| **OMP**    |      3.09       |         8 потоков         |           0.39            |
| **TBB**    |      5.45       |         8 потоков         |           0.68            |
| **STL**    |      4.76       |         8 потоков         |           0.59            |
| **ALL**    |      2.67       | 32 воркерf ($4 \times 8$) |           0.08            |

**Предварительный вывод:** Реализация на базе **TBB** показывает наилучшие показатели ускорения для данной задачи на
исследованной аппаратной конфигурации — **5.45×** на 8 потоках при эффективности 68%. **STL Threads** демонстрирует
сопоставимые результаты (4.76×), незначительно уступая TBB. **OpenMP** показал скромное ускорение 3.09×, что, вероятно,
связано с особенностями настройки окружения. Гибридная версия (ALL) нецелесообразна для использования на одном
вычислительном узле из-за высоких коммуникационных накладных расходов (ускорение всего 2.67× при 32 работниках).
Детальный анализ причин полученных различий представлен в разделе 6.

## 6. Интерпретация различий

### 6.1. SEQ

SEQ задаёт точку отсчёта (`T_seq = 0.81` с) и демонстрирует ключевые характеристики задачи: низкую арифметическую
интенсивность, линейный доступ к памяти и независимость пикселей, что делает её потенциально хорошо распараллеливаемой.

### 6.2. OMP

**Сильные стороны:** Минимальные изменения кода (добавление директив `parallel for` и `reduction`), удобный механизм
редукции для поиска min/max.

**Слабые стороны:** Падение эффективности до 39% на 8 потоках объясняется использованием Hyper-Threading (HT). Процессор
имеет 4 физических ядра, каждое из которых выполняет 2 логических потока. При переходе с 4 на 8 потоков начинают
использоваться виртуальные (HT) потоки, которые разделяют ресурсы физического ядра (кэш L1/L2, исполнительные блоки),
что даёт лишь ограниченный прирост производительности.

### 6.3. TBB

**Роль grain size:** Автоматический выбор размера зерна избавляет от ручной настройки и позволяет TBB достичь наилучшего
ускорения 5.45×. Платой являются дополнительные вызовы планировщика, но в данной задаче они окупаются.

**Роль runtime:** TBB показал лучший результат (5.45× на 8 потоках), эффективно балансируя нагрузку между 4 физическими
ядрами и их HT-потоками. Механизм _work stealing_ позволяет сглаживать дисбаланс, возникающий при использовании HT.

### 6.4. STL

**Цена ручного управления:** Увеличенный объём кода, необходимость ручного вычисления диапазонов, явный `join()`,
потенциальные риски гонок, отсутствие динамической балансировки.

**Выгода:** Минимальные накладные расходы, полный контроль над разбиением данных, предсказуемое поведение.

**Почему STL показал хороший результат (4.76× на 8 потоках):** Статическое разбиение данных на 8 равных частей хорошо
работает при использовании HT, так как каждый логический поток получает свой непрерывный блок данных, что минимизирует
конфликты в кэше.

### 6.5. ALL

**Цена коммуникации:**

- `MPI_Scatterv`/`MPI_Allgatherv` требуют передачи всего массива данных через коммуникационную сеть.
- `MPI_Allreduce` (два вызова) заставляет все процессы ждать самого медленного.
- Даже на одном узле межпроцессное взаимодействие создаёт избыточные копирования данных.

**Дополнительный фактор:** Помимо HT-ограничений, добавляются коммуникационные накладные расходы MPI, что делает
гибридную версию наименее эффективной на одном узле.

**Когда ALL оправдан:**

- Данные не помещаются в память одного узла (терапиксельные изображения).
- Реальный многоузловой кластер с высокоскоростной сетью (InfiniBand).
- Задачи с высокой вычислительной сложностью на пиксель, где коммуникации незаметны на фоне вычислений.

### 6.6. Сводная таблица

| Технология | Max ускорение | Эффективность (8 потоков) | Сложность кода | Примечание                       |
| :--------: | :-----------: | :-----------------------: | :------------: | :------------------------------- |
|  **SEQ**   |     1.00      |             —             |     Низкая     | Базовый уровень                  |
|  **OMP**   |     3.09      |            39%            |   **Низкая**   | HT даёт ограниченный прирост     |
|  **TBB**   |   **5.45**    |          **68%**          |    Средняя     | Лучшая адаптация к HT            |
|  **STL**   |     4.76      |            59%            |    Высокая     | Статическое разбиение эффективно |
|  **ALL**   |     2.67      |    8% (на 32 воркерах)    |  **Высокая**   | Оверхед MPI перекрывает выигрыш  |

**Предварительный вывод:** Реализация на базе **TBB** показывает наилучшие показатели ускорения для данной задачи на
исследованной аппаратной конфигурации — **5.45×** на 8 потоках при эффективности 68%. **STL Threads** демонстрирует
сопоставимые результаты (4.76×), незначительно уступая TBB. **OpenMP** показал скромное ускорение 3.09×, что связано с
ограничениями Hyper-Threading на 4-ядерном процессоре. Гибридная версия (ALL) нецелесообразна для использования на одном
вычислительном узле из-за высоких коммуникационных накладных расходов (ускорение всего 2.67× при 32 работниках).

## 7. Репродуцируемость

```powershell
#сборка проекта
cmake -S . -B build -D USE_FUNC_TESTS=ON -D USE_PERF_TESTS=ON -D CMAKE_BUILD_TYPE=Release
cmake --build build --config Release

cd build/bin

#запуск функциональных тестов
$env:PPC_NUM_THREADS = 4
./build/bin/ppc_func_tests.exe --gtest_filter=*vers* #для запуска тестов seq, omp, tbb, stl
mpiexec -n 2 ./ppc_func_tests.exe --gtest_filter=*all* #для запуска тестов all

#получение основных замеров
$env:PPC_NUM_THREADS = 8

./build/bin/ppc_perf_tests.exe --gtest_filter=*vers* #для запуска тестов seq, omp, tbb, stl
mpiexec -n 4 ./ppc_perf_tests.exe --gtest_filter=*all* #для запуска тестов all
```

## 8. Заключение

### Какая версия лучше и когда

На исследованной системе **наилучшую производительность показала версия на TBB** — ускорение 5.45× при использовании 8
потоков.

Выбор подходящей технологии зависит от поставленных задач:

- **OpenMP** — оптимальный выбор для быстрого прототипирования. Требует минимальных изменений в коде (всего несколько
  директив) и даёт ускорение до 3.09×.

- **TBB** — лучший вариант, когда требуется выжать максимум из одного узла. Библиотека самостоятельно балансирует
  нагрузку и хорошо адаптируется к Hyper-Threading, обеспечивая ускорение 5.45×.

- **STL Threads** — подходит для ситуаций, где важен полный контроль над потоками. При статическом разбиении данных даёт
  ускорение 4.76× с минимальными накладными расходами.

- **MPI+OpenMP** — имеет смысл только в распределённых системах при обработке данных, которые не помещаются в память
  одного узла. На одиночной машине коммуникационные издержки делают эту версию неэффективной.

- **SEQ** — служит эталоном для проверки корректности и измерения базовой производительности.

### Что ограничивает достоверность сравнения

Полученные цифры привязаны к конкретному оборудованию (процессор i5-1135G7). На других системах результаты могут
отличаться.

Эксперименты проводились на фиксированном размере данных (400 млн пикселей). При меньших объёмах накладные расходы на
параллелизацию могут свести на нет выигрыш от многопоточности.

Задача является memory-bound, то есть упирается в пропускную способность памяти. Для вычислительно-ёмких алгоритмов
соотношение эффективности технологий может быть иным.

OpenMP и TBB не подвергались тонкой настройке — использовались параметры по умолчанию. Более тщательная настройка
(привязка потоков, подбор grain size, выбор schedule) могла бы улучшить их показатели.

### Что можно улучшить

**Общие для всех версий:** применить SIMD-векторизацию для ускорения на уровне инструкций, использовать кэш-блокировку
для улучшения локальности данных, попробовать объединить два прохода по памяти в один.

**Для OpenMP:** явно задать `schedule` с оптимальным размером чанка, настроить привязку потоков через `OMP_PROC_BIND`,
использовать редукцию с пользовательским типом.

**Для TBB:** подобрать оптимальный `grainsize`, ограничить число потоков через `tbb::global_control`, попробовать
конвейерную обработку через `parallel_pipeline`.

**Для STL:** создать пул потоков для устранения затрат на создание/уничтожение, добавить динамическую балансировку
нагрузки.

**Для гибридной версии:** заменить два вызова `MPI_Allreduce` на один с пользовательской структурой, использовать
асинхронные MPI-вызовы для наложения коммуникаций и вычислений, рассмотреть односторонние операции RMA.

## 9. Источники

1. **Курс «Параллельное программирование для систем с общей памятью».** Сысоев А.В.
2. **Документация курса** методические указания по написанию отчетов и проведению эксперимента.
3. **OpenMP Specification** директивы `parallel for`, `reduction`, `shared`/`private`, `schedule`.
4. **oneTBB Documentation** шаблоны `parallel_for`, `parallel_reduce`, `blocked_range`, планировщик задач.
5. **cppreference.com** `std::thread`, `std::atomic`, управление потоками, потокобезопасность.
6. **MPI Forum Documents** коллективные операции `MPI_Scatterv`, `MPI_Allreduce`, `MPI_Gatherv`, `MPI_Bcast`.
