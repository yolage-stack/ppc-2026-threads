# Умножение плотных матриц блочным алгоритмом Кэннона — STL

- Student: Синев Тимур Викторович, группа 3823Б1ПР5
- Technology: STL (`std::thread`)
- Variant: 1

## 1. Контекст

Параллелизм реализован **вручную**: создаются потоки `std::thread`, работа делится по индексам,
в конце вызывается `join`. Число потоков берётся из `PPC_NUM_THREADS` (функция `GetNumThreads()`).

## 2. Постановка задачи

- **Вход:** кортеж `(b_size, A, B)` — размер блока `b_size` и две квадратные матрицы `A`, `B`
  размера `n×n` (`common/include/common.hpp`).
- **Выход:** матрица `C = A·B` размера `n×n`.
- **Ограничения:** `b_size > 0`; `n` кратно `b_size`; матрицы квадратные (проверяется в `ValidationImpl`).
- **Эталон для сравнения:** SEQ, режим `task_run`, один поток: **\(T_{seq} = 0.0499\) с**
  (матрицы `512×512`, блок `32×32`).

## 3. Базовый алгоритм

**Блочный алгоритм Кэннона:**

1. `grid_sz = n / b_size`.
2. **Раскладка** `DistributeData` — начальное размещение блоков `A` и `B` в сетке.
3. **Цикл шагов** `step = 0 … grid_sz-1`: для каждой пары блоков `(i,j)` — `BlockMultiplyAccumulate`.
4. Сдвиги `RotateBlocksA` / `RotateBlocksB` между шагами.
5. **Сборка** `CollectResult` в итоговую матрицу.

Сложность по времени: \(O(n^3)\). По памяти: \(O(n^2)\).

## 4. Схема распараллеливания

Вспомогательная функция `ParallelFor(work_size, func)`:

- создаёт `num_threads` потоков (не больше `work_size`);
- поток с номером `t` обрабатывает индексы `t, t+num_threads, t+2·num_threads, …`;
- после цикла создания — **отдельный цикл `join`** для всех потоков.

Так параллелятся:

- раскладка блоков (по индексу строки сетки `i`);
- умножение на шаге (по `i`, внутри цикл по `j`);
- сдвиги и сборка.

Запись идёт в разные блоки — **mutex не используется**.

```cpp
// File: stl/src/ops_stl.cpp
for (int thread_id = 0; thread_id < num_threads; ++thread_id) {
  threads.emplace_back([&, thread_id]() {
    for (int index = thread_id; index < work_size; index += num_threads) {
      func(index);
    }
  });
}
for (auto &thread : threads) {
  thread.join();
}
```

## 5. Детали реализации

Файлы: `stl/include/ops_stl.hpp`, `stl/src/ops_stl.cpp`.

## 6. Проверка корректности

8 тестов `timur_a_cannon_stl_enabled_*` — **PASSED**.

## 7. Экспериментальная среда

- **CPU:** AMD Ryzen 5 2600.
- **RAM:** 16 GB.
- **ОС:** Windows 11.
- **Build type:** Release.
- **Compiler:** MSVC 19.44.
- **Размер в perf-тесте:** `n = 512`, `b_size = 32`.
- **Команда:**

```text
set PPC_NUM_THREADS=<p>
build\bin\ppc_perf_tests.exe --gtest_filter=*timur_a_cannon_stl*
```

## 8. Результаты

Замеры **18.05.2026**, режим **`task_run`**, \(T_{seq} = 0.0499\) с.

| Число потоков ($p$) | Время (с) | Ускорение ($S$) | Эффективность ($E$) |
| :-----------------: | :-------: | :-------------: | :-----------------: |
|          1          |  0.0997   |      0.50       |        50.1%        |
|          2          |  0.0513   |      0.97       |        48.6%        |
|          4          |  0.0482   |      1.04       |        25.9%        |
|          8          |  0.0533   |      0.94       |        11.7%        |

_Комментарий:_ на 1 потоке STL **медленнее SEQ** — лишние затраты на создание потоков.
На 4 потоках время близко к SEQ, на 8 снова хуже. При `grid_sz = 16` мало «крупных» частей
работы на поток, поэтому ручные потоки проигрывают OMP и TBB.

## 9. Выводы

Версия на `std::thread` корректна, но по скорости на этой задаче и размере матрицы она **слабее**
OMP и TBB. Имеет смысл как учебная реализация «с нуля», а не как самый быстрый вариант.
