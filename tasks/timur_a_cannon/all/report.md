# Умножение плотных матриц блочным алгоритмом Кэннона — ALL

- Student: Синев Тимур Викторович, группа 3823Б1ПР5
- Technology: ALL (MPI + OpenMP)
- Variant: 1

## 1. Контекст

Сочетаются **два уровня** параллелизма:

1. **MPI** — несколько процессов, каждый считает свою часть строк блоков сетки.
2. **OpenMP** — внутри процесса потоки обрабатывают свои строки.

Запуск обязателен через **`mpiexec`**, иначе тесты `all` пропускаются.

## 2. Постановка задачи

- **Вход:** кортеж `(b_size, A, B)` — размер блока `b_size` и две квадратные матрицы `A`, `B`
  размера `n×n` (`common/include/common.hpp`).
- **Выход:** матрица `C = A·B` размера `n×n`.
- **Ограничения:** `b_size > 0`; `n` кратно `b_size`; матрицы квадратные (проверяется в `ValidationImpl`).
- **Эталон для сравнения:** SEQ, режим `task_run`, один поток: **\(T_{seq} = 0.0499\) с**
  (матрицы `512×512`, блок `32×32`).

## 3. Базовый алгоритм

**Блочный алгоритм Кэннона** с делением работы между процессами:

1. `grid_sz = n / b_size`.
2. Матрицы `A` и `B` рассылаются на все процессы (`MPI_Bcast`), чтобы у каждого был полный вход.
3. Каждый MPI-процесс считает **свои строки блоков** сетки (диапазон `global_i`).
4. Для каждой локальной строки блока и каждого столбца `j` выполняются все шаги Кэннона:
   копирование нужных блоков `A`, `B`, умножение `BlockMultiplyAccumulate`,
   накопление в локальный фрагмент результата.
5. Локальные части матрицы `C` собираются в полный результат на всех процессах (`MPI_Allgatherv`).

Внутри процесса цикл по строкам блоков распараллен OpenMP. Сложность по времени: \(O(n^3)\).
По памяти: \(O(n^2)\) на процесс (плюс обмены MPI).

## 4. Схема распараллеливания

**Между процессами (MPI):**

- процесс `rank` получает свой диапазон строк блоков;
- входные данные разосланы `MPI_Bcast`;
- результат собран `MPI_Allgatherv`.

**Внутри процесса (OpenMP):**

- цикл по локальным строкам блоков — `omp parallel for`.

```cpp
// File: all/src/ops_all.cpp
#pragma omp parallel for default(none) shared(...)
for (int local_i = 0; local_i < local_block_rows; ++local_i) {
  for (int j = 0; j < grid_sz; ++j) {
  // шаги Кэннона для одного блока результата
  }
}
```

## 5. Детали реализации

Файлы: `all/include/ops_all.hpp`, `all/src/ops_all.cpp`.

## 6. Проверка корректности

8 тестов `timur_a_cannon_all_enabled_*` под MPI — **PASSED**:

```text
mpiexec -n 2 build\bin\ppc_func_tests.exe --gtest_filter=*timur_a_cannon_all*
```

## 7. Экспериментальная среда

- **CPU:** AMD Ryzen 5 2600 .
- **RAM:** 16 GB.
- **ОС:** Windows 11.
- **Build type:** Release.
- **Compiler:** MSVC 19.44.
- **Размер в perf-тесте:** `n = 512`, `b_size = 32`.
- Для perf использовалась схема **«процессы × потоки = p»**:

| $p$ | MPI-процессов | Потоков OpenMP в процессе |
| :-: | :-----------: | :-----------------------: |
|  1  |       1       |             1             |
|  2  |       2       |             1             |
|  4  |       2       |             2             |
|  8  |       2       |             4             |

Пример для $p = 4$:

```text
mpiexec -env PPC_NUM_THREADS 2 -env OMP_NUM_THREADS 2 -n 2 `
  build\bin\ppc_perf_tests.exe --gtest_filter=*timur_a_cannon_all*
```

## 8. Результаты

Замеры **18.05.2026**, режим **`task_run`**, \(T_{seq} = 0.0499\) с.

| Всего работников ($p$) | MPI × потоки | Время (с) | Ускорение ($S$) | Эффективность ($E$) |
| :--------------------: | :----------: | :-------: | :-------------: | :-----------------: |
|           1            |    1 × 1     |  0.0844   |      0.59       |        59.1%        |
|           2            |    2 × 1     |  0.0536   |      0.93       |        46.6%        |
|           4            |    2 × 2     |  0.0338   |      1.48       |        36.9%        |
|           8            |    2 × 4     |  0.0253   |      1.98       |        24.7%        |

_Комментарий:_ на одном процессе ALL медленнее SEQ — есть расходы MPI даже при `n=1`.
При росте $p$ время уменьшается; на **8 работников** ускорение почти **2×**.
Часть времени уходит на обмен данными (`Bcast`, `Allgatherv`), поэтому эффективность ниже,
чем у чистого OMP/TBB.

## 9. Выводы

Гибридный вариант имеет смысл на **нескольких ядрах/узлах** и достаточно больших матрицах.
Для маленьких задач проще и быстрее пользоваться OMP или TBB на одной машине.
